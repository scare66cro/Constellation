# Diagnostic: write ONLY the OSPI PHY tuning attack vector via TI's
# canonical sbl_uart_uniflash flow. Doesn't touch SBL at 0x0 or any
# other flash region. Use after our OSPI_PHY_TUNING_WRITER has been
# tried and didn't produce a bootable chip, to test whether TI's
# canonical flow writes something different (e.g. DLPR side-effects).
#
# Background: 2026-05-18 diagnostic on S24L0707 (Nova). Every CFR
# register matches the working STORAGE board, yet ROM cold-boot from
# OSPI fails on Nova. Possible root cause: TI's `flash-phy-tuning-data`
# operation writes more than just the 128-byte vector at 0x2000000 —
# it may also program the Cypress chip's DLPR (Data Learning Pattern
# Register) or similar internal calibration state.

--flash-writer=C:/ti/mcu_plus_sdk_am243x_12_00_00_26/tools/boot/sbl_prebuilt/am243x-lp/sbl_uart_uniflash.release.hs_fs.tiimage

--operation=flash-phy-tuning-data
