# Constellation Firmware â€” Current Alpha Version

> **Status: ALPHA.** Do not ship to customers. All version strings carry
> the `0.A.<n>` prefix until we cut over to beta. Bump `<n>` when a
> meaningful change ships and add a one-line entry below. The same `<n>`
> is read by `Flash-LP.ps1` and baked into the firmware via
> `-DLP_FW_VERSION=...`.

## Current

**`0.A.208`**

## Changelog

| Alpha # | Date | Notes |
|---:|---|---|
| 208 | 2026-05-29 | **🎯 SEVENTH-layer controller self-update brick — SBL preserve in `write_meta_block_atomic`.** Bench-2026-05-29 with 0.A.207 (all 6 prior layers + Phase 4 install fix in place): full multi-orbit install ran clean for STORAGE / GDC / TRITON (TRITON OTA bench-validated for the first time!), then the controller self-update activate STILL bricked Nova. JTAG-flashed 0.A.207 boots fine but OTA-self-update of IDENTICAL bytes bricks — so the brick is in the self-update *process*, not the binary. **Root cause:** the 0.A.200 `write_meta_block_atomic` design comment claimed "Other 252 KB of the block (0x40000-0x5FFFF, 0x61000-0x6FFFF, 0x71000-0x7FFFF) stays 0xFF — currently unused by anything per the canonical OSPI map in nova_fw_update.h." **That assumption is wrong until F2c SBL chooser ships.** We currently run TI's stock `sbl_ospi.release.hs_fs.tiimage` (~311 KB = 0x4BE2D) at OSPI 0x0. Its tail bytes extend into `0x40000-0x4BE2D`. Every `write_meta_block_atomic` erases the 256-KB block at `0x40000-0x7FFFF` and writes back only the 4-KB metadata sectors at `0x60000` / `0x70000` — silently wiping bytes `0x40000-0x4BE2D` of the SBL to `0xFF`. After ACTIVATE, next warm-reset's ROM rejects the truncated SBL, falls into BOOT ROM, Nova OFFLINE forever. This was hidden by every successful JTAG reflash (which re-wrote the SBL at 0x0). **Fix:** read-modify-write — preserve `0x40000-0x5FFFF` (128 KB) via `hal_flash_read_dac` BEFORE the erase, write back via `hal_flash_write_dac` AFTER. 128 KB BSS scratch buffer in MSRAM, per-Activate latency `~450 ms → ~700 ms` (acceptable for OTA-only). Becomes harmless / redundant once F2c chooser lands and 0x000000-0x05FFFF actually has a real chooser SBL with its own erase boundary. Bench-verify pending. |
| 207 | 2026-05-29 | **Phase 4 install — restore Begin recv timeout before `await_status` (`nova_ota_push.c`).** Bench-2026-05-29: `/iot/firmware/install` consistently failed at step 5 with `[OTA-PUSH] Begin step=5 FAIL no Status (timeout/disconnect) elapsed=3246626us` against STORAGE @ `.2`, but `/api/_debug/broker-install-begin-abort` (synthetic `total_size=1024`) consistently succeeded — both code paths use the SAME `NovaOtaPush_BeginToLp` so the difference had to be data-driven. Root cause: at step 3, `sock_set_recv_timeout(sock, PUSH_BANK_INFO_TIMEOUT_S)` (3 s) is set for the BankInfo drain, then steps 4-5 never reset it. The log at step 5 said `recvTo=120s` but the actual socket timeout was still 3 s. Debug synthetic ran clean because total_size=1024 doesn't force a real Bank-B erase, LP responds with FwUpdateStatus in <3 s. Real installs blow through the 3 s because LP's Bank-B erase takes ~5 s. Fix: one line — `sock_set_recv_timeout(sock, PUSH_BEGIN_RECV_TIMEOUT_S)` right before await_status (mirrors the BankInfo timeout set in step=3). PUSH_CHUNK_RECV_TIMEOUT_S reset at end of Begin (line 606) is unchanged. Also patched bridge `apiRoutes.ts` to forward `allowDowngrade` (was destructure-bug at line 609 — `bridge-phase41-windowed-chunk-pump.md` side-observation made real). Memory note will be added after bench-verify. |
| 206 | 2026-05-27 | **Broker `probe_one_host` — non-blocking connect + `lwip_select` with bounded timeout.** Bench-2026-05-27 with 0.A.205 (six-layer controller self-update fix in place): COM9 confirmed `[FwUpd] Begin: pre-erase Bank1 @0x900000 (DAC-STIG)` succeeded — the 6-layer fix IS working — but the install still timed out at chunk offset 0. The actual blocker: fleet probe took 20+ seconds because GDC (.3) is offline and `lwip_connect` doesn't honor `SO_SNDTIMEO` (only blocking TCP-stack timeout applies, ~20 s). During those 20 s, bridge gave up waiting for `FwFleetSnapshot` at its 5-s timeout, sent `FwInstallBegin` + `Chunk 0`. The chunk arrived at broker while broker was still in the synchronous probe — `[OTA-BROKER] OnEnvelope chunk DROPPED — state=1 not INSTALLING`. Broker finished probing at 20 s, processed InstallBegin, ran Begin (success), but the chunk was already gone. Bridge timed out at 30 s. **Fix:** switch `probe_one_host` to non-blocking connect (`lwip_fcntl(F_SETFL, O_NONBLOCK)`) + `lwip_select(NULL, &wfds, NULL, &tv)` so the 300 ms `timeout_ms` actually bounds the connect attempt. After successful connect, restore blocking mode so the subsequent `recv_frame` uses `SO_RCVTIMEO`. Standard POSIX idiom (also used in `orbit_modbus_tcp.c:310`). Probe of 3 orbits with 1 offline now takes ~300 ms instead of ~20 s, bridge's 5-s `FwFleetSnapshot` window comfortably fits, no envelope-arrival-during-probe race. **Closes the open issue in [`memories/repo/broker-install-begin-fleet-probe-race.md`](../memories/repo/broker-install-begin-fleet-probe-race.md).** With this fix, the 6-layer controller self-update should bench-validate end-to-end. |
| 205 | 2026-05-27 | **Controller self-update — bare-metal STIG erase (`hal_flash_erase_block_dac`) replaces SDK `hal_flash_erase_sector` (→ `Flash_eraseBlk`) on the three runtime callsites.** Bench-2026-05-27 controller-only .cfu install (orbits omitted) hit `chunk ack timeout at offset 0 (waited 30000 ms)` — Begin's pre-erase of Bank-B at 0x900000 wedged inside SDK `Flash_norOspiWaitReady` in CPSW-active context, broker never returned from Begin, no chunks could be acked. The earlier full 4-component installs incidentally avoided this hang because orbit OTAs ran first (six minutes of CPSW TX/RX evidently warmed up some controller state — never understood why this masks the wedge), but the bug was always there: same family as the SDK INDIRECT_WRITE / INDIRECT_READ wedges fixed in 0.A.199/0.A.203/0.A.204. `hal.h:187-192` already documents `hal_flash_erase_block_dac` as "the erase equivalent of `hal_flash_write_dac` — used by streaming OTA paths to pre-erase Bank B at BEGIN without closing Enet." The orbit OTA path (lp_ota_task) presumably already uses it — controller self-update inherited the SDK pattern from earlier Phase 3.6 work. **Fix:** swap three runtime erase callsites in `nova_fw_update.c`: (1) `write_meta_block_atomic` (Activate path), (2) `NovaFwUpdate_Begin` pre-erase, (3) `NovaFwUpdate_WriteChunk` lazy block erase. All three now call `hal_flash_erase_block_dac` with block-aligned addresses (the meta block needs explicit alignment from `FW_HEADER_OFFSET=0x60000` down to `0x40000`; the others are naturally block-aligned). `erase_bank_range` and `erase_bank` left alone — both are dead code (no callers) and would need separate review. Boot-time callsites (`NovaFwUpdate_BootValidation` → `write_boot_meta` → `write_meta_block_atomic`) are now using `hal_flash_erase_block_dac` too — STIG is bare-metal MMIO + bounded polling, works pre-scheduler. **All four runtime SDK-flash paths now bypassed:** WriteChunk (0.A.199 DAC write), header rewrites (0.A.200 atomic-block), Finalize CRC read (0.A.204 DAC read), and now Erase (0.A.205 STIG block-erase). **Six-layer cumulative controller self-update fix is now complete.** Bench-verify pending. |
| 204 | 2026-05-27 | **Controller self-update — Finalize CRC scan uses `hal_flash_read_dac` (XIP memcpy) instead of `hal_flash_read_stig`.** 0.A.203 swap from SDK INDIRECT_READ → STIG fixed the brick (proven by bench: chip stays alive, Finalize correctly rejects + Aborts instead of hanging) but the STIG-read returned different bytes than were written: broker accumulated CRC = `0x05b58d15`, Finalize STIG-read CRC = `0xfa07de84` over the same 523666-byte image. Since both CRC32 implementations (`broker_crc32_continue` in `nova_ota_broker.c:1032` and `crc32_update` in `nova_fw_update.c:48`) are bit-identical Ethernet polynomial, and DAC writes are known-reliable (orbit OTAs verify Bank-A CRC bit-exact via XIP memcpy — see 0.A.187 work), the only remaining suspect was STIG-read returning wrong bytes. Hypothesis: `hal_stig_read_n(HAL_STIG_CMD_OPCODE_READ4B, addr, 8 dummy cycles, ...)` doesn't match the chip's current 4S_4D_4D DTR protocol mode (STIG commands may always go single-line SDR or have different dummy-cycle requirements per protocol). lp_ota_task's Bank-A CRC scan uses inline XIP memcpy from `0x60000000+addr` and gets bit-exact matches; `hal_flash_read_dac` is the helper-wrapped version of that pattern. The "alternation hangs" caveat in `hal.h:202-204` applies to interleaved read/write loops (stage-copy); Finalize is a single bulk read AFTER all writes are done, no alternation, caveat doesn't apply. Same swap applied to `NovaFwUpdate_ValidateBank` for consistency. **Verification target:** next Finalize should print `[FwUpd] Finalize: CRC scan done crc=0x05b58d15 (expected=0x05b58d15)` MATCH, broker emits `self-update Activate (warm reset imminent)`, controller goes through warm-reset, post-reboot Bank-A version equals manifest. If matches: 5-layer controller self-update saga finally closed. If still mismatch: bug is in DAC writes themselves (would surprise, but would justify a byte-by-byte diagnostic dump of source vs flash bytes 0-15 for further triage). |
| 203 | 2026-05-26 | **🎯 Controller self-update — Finalize CRC scan uses `hal_flash_read_stig` instead of SDK `hal_flash_read`.** 0.A.202 diagnostics nailed it: COM9 capture stopped at `[FwUpd] Finalize: enter expected_crc=0x57d44fa7 bank=1 total=523346` with NO subsequent `[FwUpd] Finalize: CRC scan done` log — meaning the first `hal_flash_read(0x900000, ...)` call in the CRC scan loop is the wedge. `hal_flash_read` routes to SDK `Flash_read` → `INDIRECT_READ_XFER`, same family as the SDK-write wedge fixed in 0.A.199. After ~500 KB of DAC writes, `Flash_norOspiWaitReady` can't sync the controller back from DAC-write state to INDIRECT-read state in CPSW-active context — the read hangs forever, broker task stalls inside Finalize, watchdog eventually resets, chip is still in 4S_4D_4D DTR mode (chip_soft_reset is in Activate which we never reach), SBL post-reset can't talk to chip in single-line SDR, BOOT ROM brick. **Fix:** swap the two CPSW-active-context `hal_flash_read` callsites (`NovaFwUpdate_Finalize` CRC scan + `NovaFwUpdate_ValidateBank` CRC scan) to `hal_flash_read_stig`, which `hal.h` documents as "the right tool for runtime paths that alternate read/write" — fully synchronous STIG reads, no INDIRECT-mode state, no DAC-mode toggling, never hangs on alternation. ~6× slower (~315 ms vs ~50 ms for 494 KB) but bulletproof. lp_ota_task's Bank-A CRC scan uses the same XIP-read pattern (`lp_ota_task.c:807-820`) — the controller self-update path inherited the broken SDK pattern because Phase 3.6 was never bench-exercised end-to-end. Boot-time callsites (`read_bank_header`, `read_boot_meta`) stay on SDK Flash_read — those run pre-scheduler before CPSW is up, where SDK INDIRECT_READ works fine. **Four-layer cumulative fix to controller self-update across this bench day:** 0.A.199 chunk writes (SDK INDIRECT → DAC); 0.A.200 atomic metadata-block (header sectors share an erase block); 0.A.201 chip-soft-reset before warm-reset (SBL can't talk to chip in DTR); 0.A.203 Finalize CRC scan (SDK INDIRECT_READ → STIG). All four were latent Phase 3.6 bugs surfaced one-at-a-time. 0.A.202's diagnostic prints kept in this build — they paid for themselves and might catch the next regression. Bench-verify pending. |
| 202 | 2026-05-26 | **Controller self-update — diagnostic-only build. Granular bracketing inside `NovaFwUpdate_Finalize` and `write_meta_block_atomic` to pinpoint the actual hang.** Bench-2026-05-26 with 0.A.201 (chip-soft-reset fix in `NovaFwUpdate_Activate`) STILL bricked the controller — COM9 capture ended at `[OTA-BROKER] ComponentFinalize idx=3` with total UART silence (not even CPU-load tick lines), and CRUCIALLY no `[FwUpd] chip soft-reset rc=N` log ever printed. That means Nova wedged INSIDE `NovaFwUpdate_Finalize` (CRC scan + `write_bank_header` → `write_meta_block_atomic`) BEFORE reaching the Activate path where the chip-soft-reset lives. Cumulative 0.A.199 + 0.A.200 + 0.A.201 fixes are correct but the actual brick mechanism is somewhere inside `write_meta_block_atomic` (the 0.A.200 addition). One of: (a) `hal_flash_erase_sector(0x60000)` hanging (256-KB block erase to a different region after ~500 KB of Bank-B DAC writes), (b) first `hal_flash_write_dac(0x60000, ...)` hanging, (c) second `hal_flash_write_dac(0x70000, ...)` hanging (DAC bit toggle between sequential calls — CLAUDE.md invariant #11 risk). This build adds `debug_printf` at: Finalize entry, post-CRC-scan, pre/post `write_bank_header`, Finalize exit; and inside `write_meta_block_atomic`: entry, pre/post each of erase / sec_a write / sec_b write. Next bench will pinpoint exactly which line silences the UART. Pure diagnostic — no behavior change. Also noted: `.4 TRITON` went OFFLINE on 0.A.201 install but COM4 capture shows the LP is alive (CPU-load lines, PHY-fixup activity) — the brick is network-side, not chip-side, on TRITON; separate investigation deferred. Will be removed in the fix commit once the wedge line is identified. |
| 201 | 2026-05-26 | **Controller self-update — add `hal_flash_chip_soft_reset()` before `Sciclient_pmDeviceReset` in `NovaFwUpdate_Activate`.** Bench-2026-05-26 with 0.A.200 (running on .1 — atomic-header-rewrite fix in place): controller self-update STILL bricked Nova at warm-reset, identical symptom to 0.A.199 (`post-reboot version probe failed, no VersionInfo within 60s`, `.1 OFFLINE` permanently, JTAG-recovery required). The atomic-block fix was correct but didn't address the actual brick trigger. **Root cause:** `NovaFwUpdate_Activate` warm-reset path called `Sciclient_pmDeviceReset` directly without first issuing the Cypress chip software reset (`RSTEN 0x66 + RST 0x99` via STIG). After `Board_flashOpen` + ~500 KB of DAC writes, the chip's volatile config is in 4S_4D_4D DTR mode. The next SBL boot tries to issue setup commands in single-line SDR (per its hard-coded boot sequence), but the chip isn't listening on the single-line protocol — comms fail, SBL aborts, board falls into BOOT ROM, `.1 OFFLINE` until JTAG re-flash. **lp_ota_task knew this** since 0.A.163 (`lp_ota_task.c:1471-1483`): it calls `hal_flash_chip_soft_reset()` + `vTaskDelay(100ms)` BEFORE the `Sciclient_pmDeviceReset`. The orbit OTA path has been doing it correctly all along — the controller self-update path missed it because Phase 3.6 (`0.A.193`) was never bench-exercised end-to-end (always wedged at earlier bugs: 0.A.198/199 chunk-write SDK wedge, 0.A.200 atomic-header-rewrite). **Fix:** mirror the lp_ota_task pattern exactly — `hal_flash_chip_soft_reset()` + `[FwUpd] chip soft-reset` diagnostic prints + `vTaskDelay(100ms)` drain, all inside `NovaFwUpdate_Activate(reboot=true)` before the `Sciclient_pmDeviceReset` call. **Three-layer cumulative fix to controller self-update across this bench day:** 0.A.199 fixed the chunk-write hang (SDK INDIRECT → DAC), 0.A.200 fixed the metadata-block destruction (atomic rewrite), 0.A.201 fixed the chip-config-mismatch brick (RSTEN+RST before warm-reset). All three were latent bugs in the Phase 3.6 path that surfaced one-at-a-time as each lower layer was fixed. Memory note will be added if bench-verify confirms .1 survives this round. |
| 200 | 2026-05-26 | **Controller self-update — atomic metadata-block rewrite fixes silent header destruction.** Bench-2026-05-26 with 0.A.199 (right after the DAC migration landed) drove the controller self-update path all the way through CHUNK + FINALIZE + ACTIVATING, then `.1` went OFFLINE and stayed OFFLINE across power-cycles. JTAG-recovery required. **Root cause:** `nova_fw_update.h`'s layout comment claimed each bank header "lives in its own sector so updating one bank's metadata cannot accidentally erase the other's" — but the Cypress S25HL512T in 4S_4D_4D DTR mode only supports the 256-KB BLOCK erase (opcode 0xDC), NOT a 4-KB sector erase (the 0x20/0x21 sector opcodes leave the OSPI controller wedged — known issue, documented in `hal_flash.c::hal_flash_erase_sector` and `memories/repo/lp-am2434-runtime-flashwrite-unresolved.md`). `FW_HEADER_OFFSET=0x60000` and `FW_BANK_B_HDR_SECTOR=0x70000` both sit in the SAME 256-KB block (0x40000-0x80000); `write_bank_header(BANK_A, ...)` erases the WHOLE block (wipes Bank-B header to all-0xFF) then only writes Bank-A's 4 KB back. `Activate`'s three header rewrites left BOTH bank headers wiped in flash; SBL chooser found no valid bank, fell into BOOT ROM. The bug was invisible in RAM cache (`s_bank_a_hdr`, `s_bank_b_hdr` stayed correct) — only visible after warm-reset when SBL re-read flash. **This is fundamental, has always been broken** since Phase 3.6 landed the controller self-update path — only never bench-exercised end-to-end before today because earlier benches always wedged at WriteChunk (now fixed in 0.A.199). **Fix:** new `write_meta_block_atomic()` helper rewrites the entire metadata block (FwBootMeta + Bank-A header + Bank-B header reconstructed from RAM cache, 0xFF-padding for unused 252 KB) in one transaction: erase block → DAC-write both 4 KB sectors. `write_bank_header()` and `write_boot_meta()` now both updates cache + calls `write_meta_block_atomic()`. Cost: per-Activate latency goes from 3 × (erase + 4KB-write) = ~450 ms to 3 × (erase + 2 × 4KB-write) = ~750 ms — acceptable for an OTA-only path. **Generalised lesson:** any "X header sectors are independently writable" assumption in OSPI code must validate against the actual chip's erase granularity. The 4-KB-vs-256-KB mismatch was hidden because nothing else lived in the unused 252 KB and the RAM cache masked the failure. Memory note: `memories/repo/controller-self-update-shared-block-header-wipe.md`. Bench-verify pending after JTAG-recover of .1. |
| 199 | 2026-05-26 | **Controller self-update — migrate `Platform/nova_fw_update.c` from SDK `hal_flash_write` to `hal_flash_write_dac`.** Bench 2026-05-26 with 0.A.198 ran a multi-orbit install end-to-end (all 3 orbits .2/.3/.4 OTA'd 0.A.197→0.A.198 clean, Bank-A bit-exact CRC match, warm-reset boots into new firmware — the previously-suspected Bank-A brick is PROVEN to be a phantom: 0.A.197's stage-copy CRC fix was sufficient). But the controller self-update phase (component idx=3, `is_self=1`) wedged Nova on the FIRST chunk write: COM9 capture cut off after `[Flash] Write ENTER #1 addr=0x900000 len=1024` with no further UART output, broker never emitted `FwInstallProgress`, bridge timed out at 30 s with `chunk ack timeout at offset 0`, controller went `nova.connected:false`, required power-cycle to recover. Root cause is identical to lp_ota_task's 0.A.187 migration: `Platform/nova_fw_update.c::NovaFwUpdate_WriteChunk` called `hal_flash_write(...)` which routes to SDK `Flash_norOspiWrite` → `INDIRECT_WRITE_XFER`, which is broken when invoked in CPSW-active context (the original "INDIRECT broken in CPSW context" wedge `memories/repo/lp-am2434-runtime-flashwrite-unresolved.md` documented for the orbit path). **Fix:** swap the chunk write to `hal_flash_write_dac` with full-pages + tail-padded last page (mirrors `lp_ota_task.c:1156-1201` minus scratch-remap; controller self-update is single-shot and aborts cleanly on write failure). Same swap also landed in `write_bank_header` and `write_boot_meta` — both wrote 4-KB sector buffers via the SDK INDIRECT path and would wedge identically on Finalize (header rewrite) or boot-meta updates. Added `NOVA_FLASH_PAGE=256` constant + 256-B BSS pad buffer. Memory note: `memories/repo/controller-self-update-sdk-write-wedge.md`. **Side findings (this bench):** (a) the `[OTA-PROBE]` instrumentation from 0.A.198 produced clean fleet-snapshot diagnostics — 3-orbit probe completed both times with all 3 reachable; the fleet-probe race did NOT reproduce today (kept instrumentation in place for next surface); (b) the Bank-A post-OTA brick hypothesis is now closed — bench-confirmed STORAGE boots into 0.A.198 from Bank-A after stage-copy + warm-reset (COM5 log lines 620-712 of `uart_COM5_2026-05-26_173428.log`). |
| 198 | 2026-05-26 | **Broker `probe_one_host` step instrumentation — fleet-probe race triage.** Bench 2026-05-26 (with 0.A.197) showed the broker's internal `NovaOtaPush_ProbeFleet` consistently marks orbits 2..N unreachable in the same install where direct probes (`probe_fleet.ts`, `/api/_debug/broker-fleet-probe`) show all healthy. Suspected lwIP TCP PCB exhaustion (TIME_WAIT) on rapid sequential connect/close, but unconfirmed because `probe_one_host` had zero per-step logging. This build adds `[OTA-PROBE]` step-N markers in `Nova_Firmware/lp_am2434/nova_ota_push.c::probe_one_host` covering: enter (target+timeout), socket-alloc (fd or errno), connect (rc + errno + connectUs), recv FwBankInfo (rfrc + tag + len + errno + recvUs), and OK (role + active + version + boot + totalUs). Each failure path logs elapsed-from-probe-entry so adjacent COM9 captures show inter-probe timing. Also added `[OTA-PROBE] fleet enter/exit` and per-slot start markers in `NovaOtaPush_ProbeFleet` so we can correlate slot index → ip → outcome. Cost: ~6 lines per probe (~18 lines per 3-orbit snapshot) on the broker UART (COM9). Pure diagnostic — no behaviour change. Memory note: `memories/repo/broker-install-begin-fleet-probe-race.md`. |
| 197 | 2026-05-26 | **LP stage-copy CRC fix + broker diagnostics + Phase 4 PROVEN end-to-end.** Bench validated the full Phase 4 broker pipeline: all 523 chunks streamed bridge → broker → STORAGE, FINALIZE Bank-B CRC matched, stage-copy completed bit-exact (0 byte mismatches). **But STORAGE wedged at the very last step**: `lp_ota_task.c`'s stage-copy verification compared Bank-A CRC against `s_image_expected_crc` which was set at BEGIN time. Broker passes `image_crc=0` at Begin (it accumulates the running CRC during chunks and doesn't know the final value upfront), so this check always fails. The data was perfect (Bank-A byte-for-byte equal to Bank-B) but the LP self-aborted with "stage copy FAILED — Bank A may be partially overwritten" and didn't trigger the reboot. Net effect: STORAGE + GDC ended up with Bank-A overwritten by 0.A.196 bytes but stuck in lp_ota_task error state — required JTAG recovery via `Recover-Fleet.ps1`. **Fix (1 line):** at FINALIZE handler in `lp_ota_task.c` (line 1367-1376), after `crc_b == expected` is confirmed MATCH, store `s_image_expected_crc = expected` so the subsequent stage-copy verification has the correct reference. Also added diagnostic `[OTA-BROKER] OnEnvelope chunk len=N seq=N state=N ring=N` + `[OTA-BROKER] decode_chunk entered len=N` + `[OTA-BROKER] OnEnvelope chunk DROPPED — state=N not INSTALLING` to disambiguate "chunk never reached broker" vs "chunk arrived but silent-dropped" — these were essential to bench-prove the broker pipeline was actually working all along (today's chunk-not-arriving symptom was masking a different transient + this stage-copy bug). Phase 3.7.2 per-chunk PUSHING emit (from 0.A.196) is retained. Recovery script `Nova_Firmware/lp_am2434/Recover-Fleet.ps1` re-flashes all 3 fleet LPs with 0.A.197 in sequence (controller → storage → gdc). Bench-test pending until orbit recovery + retry. |
| 196 | 2026-05-26 | **Broker progress cadence — emit per-chunk (was every ~5%).** 2026-05-26 bench validated 0.A.195's Bug A fix + diagnostics + 16K stack bump: BeginToLp completed in 3.25s (Bug B fixed by stack bump). STORAGE accepted chunks 0/1024/2048/3072 cleanly (no out-of-order). But bridge timed out at 3s because broker only emits `FwInstallProgress(state=PUSHING)` every 5% of total_size (~25 chunks for 522KB). Bridge's Phase 4.1 closed-loop chunk pump (WINDOW=4) sends 4 chunks, waits for ack, times out because broker doesn't emit progress until chunk 25. Fix: removed the 5%-throttle in `nova_ota_broker.c::decode_chunk`'s push-path emit; broker now emits PUSHING per chunk. UART traffic: ~30 bytes × 522 chunks = 16 KB total = ~170 ms cumulative over the install (vs ~5.6 s for the chunks themselves). Negligible. Old 5%-throttle logic kept as comments for context. Build-only — flash next. |
| 195 | 2026-05-24 | **UART-airgap Phase 3.7.1 — OTA broker regression fixes from 0.A.194 bench.** 2026-05-24 bench evidence: Phase 3.7 task-split (0.A.194) successfully decoupled leaf I/O from the UART heartbeat (bridge stayed connected during install attempts, no "No firmware data for 15s" reconnect loop). But two regressions surfaced. **Bug A (fixed):** fleet snapshot was cached across installs within one boot session — `ensure_fleet_snapshot()` early-returned on `s_ctx.fleet_probed=true`, set on first probe but never explicitly cleared between installs. The `memset(&s_ctx, 0, sizeof(s_ctx))` at install boundaries already cleared it, but bench observed stale snapshots being reused (an orbit marked unreachable in attempt N stayed unreachable for attempt N+1 even after a real probe would have found it healthy). Added explicit `s_ctx.fleet_probed = false; s_ctx.fleet_count = 0u;` at install boundaries in `decode_install_begin` / `decode_complete` / `decode_abort` — belt-and-suspenders that survives any future refactor of the wholesale memset, with inline comment documenting the invariant. **Bug B (triage step 1 — diagnostics + stack bump, root cause TBD):** `NovaOtaPush_BeginToLp` fast-failed in ~2.3 s to a known-reachable STORAGE LP (`10.47.27.2`, role=STORAGE, bankA=0.A.192, fully healthy per immediate post-failure `/api/_debug/broker-fleet-probe` and direct `npx tsx src/probe_fleet.ts` confirmation). The 2.3 s timing doesn't fit any single timeout cleanly. Triage: (1) added per-step `[OTA-PUSH] Begin step=N` diagnostics to `nova_ota_push.c::NovaOtaPush_BeginToLp` covering enter / connect / BankInfo-drain / send-FwBeginUpdate / await-Status / OK with elapsed-microsecond and errno logged at every fail point — next bench will pinpoint which sub-step trips. (2) Bumped `BROKER_TASK_STACK_BYTES` from 8 KB to 16 KB with an inline worst-case stack-budget calculation (chunk path ~7.5 KB, cmd path ~6.5 KB — 8 KB was at 85-95% utilisation, consistent with stack-overflow corruption inside `lwip_recv` recursion). (3) Audited every error path in `BeginToLp` / `WriteChunkToLp` / `FinalizeAndActivateLp` / `AbortLp` for `close_active_socket()` — all paths covered, no socket-leak hypothesis. No protocol changes, no envelope contract changes, no concurrency-model changes. Build-only — flash deferred. Next bench: capture COM9 `[OTA-PUSH] Begin step=N` lines during install to root Bug B. |
| 194 | 2026-05-23 | **UART-airgap Phase 3.7 — OTA broker concurrency split (leaf I/O off the UART task).** Phase 3.5/3.6 wired the broker leaf operations (TCP push to remote LP via `nova_ota_push.c` + controller self-update via `Platform/nova_fw_update.h`) but ran them inline on `bridge_uart_task` — the same task that owns UART RX dispatch and the 100 ms heartbeat emit. Bench evidence from 2026-05-23: a real install wedged the task inside synchronous `lwip_send`/`lwip_recv` during chunk push for ~6 minutes; heartbeat starved; bridge tripped its 15 s "no firmware data" timeout and entered a tight 20 s reconnect loop. Fix is pure internal restructure — wire protocol, envelope contracts, `nova_ota_push.c` / `nova_fw_update.c` public APIs are unchanged. `NovaOtaBroker_Init()` now also creates a recursive mutex, a binary wake-semaphore, a 16-slot 2048-B/slot chunk ring (~32 KB MSRAM headroom for future larger chunks; today's 1024-B chunks use half), a 4-slot 256-B/slot command queue, and spawns `ota_broker` task (8 KB stack, priority `configMAX_PRIORITIES - 6` mirroring `lp_ota_task`). `NovaOtaBroker_OnEnvelope` (called from `bridge_rx_callback` on the UART task) now does only validate + copy + signal — sub-ms per call. The broker task drains chunks first (high-rate path), commands second, and ticks itself on the 100 ms semaphore timeout. Backpressure: ring-full → broker emits `FwInstallProgress(state=FAILED, error_code=104, "broker chunk ring full")` and aborts cleanly. `NovaOtaBroker_Tick()` retained as a no-op stub; the per-tick call in `main.c`'s bridge loop is removed. `NovaOtaBroker_IsInInstallMode()` now reads a `volatile bool s_install_active` flag lock-free (updated by broker task on state transitions). No protocol changes, no behaviour visible to the bridge except heartbeat keeps flowing during install. Build-only — flash deferred until next bench session (current 0.A.193 controller is wedged mid-install awaiting warm-reset). |
| 193 | 2026-05-22 | **UART-airgap Phase 3.6 — controller self-update path wired in the OTA broker.** Phase 3.5 (commit `bb1a016`) wired the remote-orbit-LP push leaf via `nova_ota_push.c`; this build wires the matching self-update leaf via `Platform/nova_fw_update.h`. `nova_ota_broker.c` now picks between the two leaves through a small target-agnostic dispatch layer (`target_begin / target_chunk / target_finalize_and_maybe_activate / target_abort`) keyed off `s_ctx.cur_is_self`, which is set in `decode_component_begin` when the bundle component's `role` equals `LpDeviceConfig_Get()->role`. The same `FwInstallChunk` byte stream that previously routed to `NovaOtaPush_WriteChunkToLp` now routes to `NovaFwUpdate_WriteChunk` for the controller, with the running image CRC accumulated the same way. Activate is special-cased: the broker emits `FwInstallProgress(VERIFYING)` after Finalize, `ACTIVATING` immediately before `NovaFwUpdate_Activate(true)`, and `REBOOTING` immediately after — the warm SoC reset typically fires before the REBOOTING envelope clears the UART, so Pi5 reconciles overall success from the post-reboot Heartbeat / VersionInfo broadcasts (Phase 4 work). `decode_complete` short-circuits when `cur_rebooting=true` so an in-flight Complete envelope doesn't corrupt the post-reboot framer. `decode_abort` rolls back `NovaFwUpdate_Abort()` if a self-update is mid-stream. Bundle-ordering constraint (controller-component-must-be-last) is documented inline in `decode_component_begin` — Pi5's `firmwareInstaller.ts` enforces it on the bundle side, not the broker. No flash yet — build verification only. |
| 192 | 2026-05-21 | **Layer-4 root cause + fix.** Granular per-step pre-scheduler diagnostics from 0.A.191 revealed: `bank_erase` succeeds (`Flash_eraseBlk rc=0`) but **`Flash_write` returns -1** for an 80-byte write at `0x600000`. Same root issue as the OTA path documented in `memories/repo/lp-am2434-ota-dac-mode-fix.md` — SDK `Flash_norOspiWrite` (INDIRECT_WRITE_XFER) is broken in this firmware's runtime/init context. The OTA path migrated to `hal_flash_write_dac` for the same reason in 0.A.141. `bank_program` was still using SDK `Flash_write` — silent failure. **Fix:** switched `bank_program` in `lp_device_config.c` to use `hal_flash_write_dac`. Required pad of the 80-byte `LpDevCfgBank` record to a full 256-byte PP page with 0xFF (erased state). The helper is no-op pre-scheduler-suspend, so the same code path works both at boot (where the factory-IP-override fires) and at runtime (when the bridge issues a re-provision). Memory note: `memories/repo/devcfg-flash-write-broken-needs-dac.md`. |
| 191 | 2026-05-21 | **Layer-3 root cause + fix.** Pre-scheduler hex dump from 0.A.190 immediately revealed: OSPI device-config banks A and B at `0x600000`/`0x610000` were **fully blank** (all 0xFF) on a fresh JTAG-boot of the STORAGE binary. The factory-IP-override block in `LpDeviceConfig_Init` was never persisting — and never had been since the new bank layout shipped. **Bug:** `apply_compile_time_defaults()` sets `s_cfg.ip = CONFIG_NOVA_LP_IP`. The override block then checks `if (s_cfg.ip != CONFIG_NOVA_LP_IP) { save }` — which is always false on a fresh board because `apply_compile_time_defaults` already set them equal. Banks stay blank forever. Every boot since "bank migration to 0x600000" went through this same path. RAM cache masked the failure (probe showed correct STORAGE/.2 from defaults, never from OSPI). **Fix:** change the condition to `if (s_active_bank < 0 \|\| s_cfg.ip != CONFIG_NOVA_LP_IP)` — also fire the save when no valid bank was found (i.e. fresh board). Kept the diagnostic `[DevCfg-Diag]` + added `[DevCfg-Override]` confirmation logs to validate the save actually persists. Removes the diagnostic in 0.A.192 once persistence is bench-confirmed. See `memories/repo/devcfg-blank-banks-override-never-fired.md`. |
| 190 | 2026-05-21 | **Diagnostic-only build for layer-3 hunt** (post-0.A.189). Adds pre-scheduler `[DevCfg-Diag]` hex-dump of first 32 bytes of OSPI lp_device_config bank A (`0x600000`) and bank B (`0x610000`) right after `bank_read()` in `LpDeviceConfig_Init`. Includes the `bank_validate` result so we can tell at-a-glance whether the magic + CRC checks passed. No behavior change; only adds UART output during the pre-scheduler window. Goal: capture COM5 across a JTAG-boot vs OTA-boot and compare to definitively answer whether OSPI device-config record survives the OTA reboot. Will be removed in the fix commit once layer 3 is rooted. See `memories/repo/ota-bench-2026-05-20-three-layers.md`. |
| 189 | 2026-05-20 | **OTA root-cause: WCC auto-poll chip-reset corrupted boot-time OSPI saves.** Bench thrash: storage LP JTAG-flashed correctly, came up as STORAGE/.2 (probe confirmed). Then OTA'd a universal binary (no `CONFIG_NOVA_LP_IP` baked in, so the factory IP override block in `lp_device_config.c:309-339` was compiled out). Universal binary boots and... comes up as **CONTROLLER on 10.47.27.1**, in IP conflict with the real controller. **Root cause:** `Board_flashOpen` (SDK Cypress driver) leaves `WRITE_COMPLETION_CTRL_REG = 0x000340FF` where opcode field [7:0] is `0xFF` — on Cypress S25HL512T that's *"Reset to Default I/O Mode"*. Every auto-poll cycle the OSPI controller issues to wait for WIP=0 actually sends a chip-reset command. The `lp_ota_task.c:1511-1518` fix neutralised this for the OTA path, but `LpDeviceConfig_Init` runs in `main()` BEFORE the OTA task spawns and can invoke `LpDeviceConfig_Save` for factory IP override or legacy bank migration — those saves silently fail mid-PP, RAM cache stays correct (so probe shows the expected role) but OSPI ends up with a corrupted/blank record. Next OTA boot reads corrupted OSPI, both new banks fail validate, legacy migration scan finds nothing useful, falls through to compile-time `CONFIG_NOVA_LP_ROLE` default (CONTROLLER). **Fix:** moved the WCC=0 write into `hal_flash_init()` in `Platform/hal_flash.c`, which runs in main right after `Board_flashOpen` and BEFORE `LpDeviceConfig_Init` / `LpSettings_Init` / `NovaFwUpdate_Init`. The `lp_ota_task.c` startup fix is kept as belt-and-suspenders. Memory note: `memories/repo/wcc-auto-poll-corrupts-boot-saves.md`. |
| 188 | 2026-05-20 | **OTA hardening — firmware-side role + downgrade gates (Gap 1-LP + Gap 2-LP).** Bridge-side G3 + G2-bridge + G4 landed in 0.A.187 (commit 6042c59). This build adds the matching LP-side defenses: `FwBeginUpdate` now carries optional `expected_role` (proto field 5, force-encoded to survive 0=CONTROLLER) and `allow_downgrade` (field 6). The BEGIN handler rejects ROLE_MISMATCH (err 27) when bundle role ≠ `lp_device_config.role`, and DOWNGRADE (err 28) when incoming `0.A.<N>` < running N and the bridge didn't opt in. Both gates fire BEFORE the ~5 s Bank-B erase, so a wrong-role push aborts in <100 ms instead of burning a flash cycle. `FwBankInfo` also emits `current_role` (field 11, force-encoded) so the bridge can cross-check before sending Begin. Bridge installer threads the manifest's role through `pushImage(..., expectedRole)` automatically. Older firmwares without these fields → graceful skip on both sides (back-compat). See `docs/lp-am2434-ota-hardening-plan.md`. |
| 187 | 2026-05-15 | **🎯 OTA stage-copy — switch from SDK `hal_flash_write` back to `hal_flash_write_dac`.** 0.A.186 bench: **Bank B CHUNK writes are NOW PERFECTLY CLEAN** — `remap entries: 0`, CRC MATCH on first attempt. Leaving DAC enabled across sub-PPs was the missing fix. But stage-copy fails with the historical CPSW-broken SDK signature (`sdk=-1 sr1=0x02 ctrl_post=0x60 verify=FF`) — INDIRECT_WRITE_XFER is broken in our runtime context regardless of CPSW state (re-confirmed; see `lp-am2434-runtime-flashwrite-unresolved.md`). **Fix:** switch stage-copy to `hal_flash_write_dac` (same path as CHUNK), which is now reliable thanks to 0.A.186. End-to-end OTA should finally complete: BEGIN → CHUNK (clean DAC writes) → FINALIZE (Bank B CRC match) → ACTIVATE → stage-copy via DAC → warm-reset → new firmware boots. |
| 186 | 2026-05-15 | **OTA write-path — leave DAC + IND_AHB_ADDR_TRIGGER enabled across sub-PPs (match SDK pattern).** 0.A.185 with word-loop still gave `first_diff @20`, same as memcpy version. So it's not the memcpy strategy — must be something in the per-sub-PP DAC enable/disable cycle. Looking at SDK `OSPI_lld_writeDirect` (ospi_v0_lld.c:1615): **it enables DAC + sets `IND_AHB_ADDR_TRIGGER_REG=0x04000000` but never disables/clears them at the end.** Our hal_flash_dac_pp_one was disabling both after every sub-PP. Hypothesis: disabling DAC mid-sequence puts the controller in a state where subsequent sub-PPs' AHB writes are silently dropped — chip sees PP commands (WIP cycles fine) but receives 0 data bytes. **Fix:** in dac_pp_one, only enable DAC if not already on; never disable. Subsequent sub-PPs find DAC already ON and skip the enable. Matches SDK pattern. |
| 185 | 2026-05-15 | **🎯 OTA write-path — replace memcpy with explicit word-loop in `hal_flash_dac_pp_one`.** 0.A.184 bench (post-power-cycle): **`first_diff @20` exactly** — 5 32-bit words land, everything after byte 20 is 0xFF erased state. Same offset whether writing to Bank B at 0x900000 OR scratch at 0xA00000, same offset across power-cycle, same offset on two different OTA images. So it's deterministic and structural, not stochastic chip state. **Hypothesis:** `memcpy` in our toolchain emits a 5-word head of individual STR instructions followed by an AHB burst that the OSPI controller's DAC write path doesn't accept. The first 20 bytes land because they go through the single-store path; the rest get dropped on the burst. **Fix:** replace `memcpy(dst, src, len)` with an explicit `volatile uint32_t` word-store loop so every 4-byte chunk is its own AHB transaction. The first 20 bytes always landed correctly with the memcpy path, so single-word stores work. If this fixes the 5-word cutoff, we're back to the "stochastic ~8 bytes per 500 KB" regime where scratch-remap converges in one OTA attempt. |
| 184 | 2026-05-15 | **OTA scratch-remap diagnostic — dump first-mismatch bytes + CacheP_inv before verify.** 0.A.183 bench: chunk 0 failed verify on ALL 4 attempts (1× Bank B + 3× fresh scratch pages). Suspicious — yesterday's stochastic byte-loss was ~8 bytes per 500 KB, not enough to fail every chunk. Either (a) byte-loss rate is much higher today, (b) verify is reading stale cache, or (c) chip is in a degraded post-test state where ALL writes are silently dropped. **Diagnostic:** added `CacheP_inv` before each verify read (kills any stale-cache-line theory) and an LP serial dump of the first-mismatch byte position + 16 bytes of chip read-back vs source for the first chunk's first 2 attempts. Result tells us exactly which mode we're in: all-FF readback → writes not landing; mostly-correct-with-tail-FF → familiar partial-PP pattern at high rate; garbage → protocol/cache bug. |
| 183 | 2026-05-15 | **🎯 OTA write-path — scratch-region remap (Option 2).** 0.A.182 bench showed full-image retry can't converge: byte-loss rate is essentially 100% per pass (5/5 attempts all CRC-mismatched in 163 s). New strategy: per-chunk verify after write; on mismatch, redirect that chunk to a fresh page in a dedicated scratch region (`LP_OTA_SCRATCH_OFFSET=0xA00000`, 256 KB block — erased at BEGIN alongside Bank B). Different chip address → no same-page back-to-back PP → no wedge. Build a small remap table (`s_remap[]`, max 64 entries) recording `image_offset → scratch_offset`. FINALIZE CRC and stage-copy iter reads both consult the remap. Typical OTA needs 1-10 redirects, well within 64-entry limit and 256-byte-per-redirect headroom. Up to 3 scratch-retry attempts per chunk in case the scratch write also has byte loss. Bridge (`orbitOtaPush.ts`) unchanged from 0.A.182 — it still does up-to-5 full-image retries if FINALIZE returns `LP_OTA_ERR_BANK_B_REDO`, but in practice scratch redirect should make every attempt clean. |
| 182 | 2026-05-15 | **OTA write-path — image-level retry on Bank B CRC mismatch (BEGIN re-erases for fresh slate).** 0.A.181 confirmed: any second PP to a partially-failed page wedges the chip, even with TCP delay between writes from the bridge side. New approach: LP does single-shot CHUNK writes (no per-chunk verify, no retry — chip never sees a second PP to the same page until Bank B is erased again). At FINALIZE, LP computes full Bank-B CRC; on mismatch sends new error code `LP_OTA_ERR_BANK_B_REDO=26`. Bridge (`orbitOtaPush.ts`) recognizes this, restarts BEGIN→CHUNK→FINALIZE (BEGIN re-erases Bank B = fresh chip slate, no wedged-page bytes), up to 5 full attempts. Each fresh-erase pass has a random chance of being clean enough to pass full-image CRC; statistics over our debug runs suggest 1-3 passes should converge typically. Cleaned up the LP_OTA_ERR_CHUNK_RETRY (25) path — kept in header for protocol reference but no longer used. |
| 181 | 2026-05-14 | **🎯 OTA write-path — bridge-side per-chunk retry protocol.** 0.A.180 confirmed: single-shot DAC + image-level CRC abort works without wedging the chip, but image-level retry would converge slowly (need a totally clean 500 KB pass). **0.A.181 fix:** LP verifies each chunk via XIP memcpy after writing. On mismatch, sends `FwUpdateStatus { state=FW_ERROR, error_code=LP_OTA_ERR_CHUNK_RETRY=25, bytes_written=offset }` and does NOT advance `s_image_bytes_written`. Bridge (`orbitOtaPush.ts`) recognizes error code 25, resends the SAME chunk (up to 8 retries per chunk). New error code added to `lp_ota_task.h`. NOR flash 1→0 is idempotent on already-correct bytes, so re-sending the same chunk programs only the dropped bytes from the previous attempt. No chip-side retry → no chip wedging. Expected: most chunks first-try OK, occasional 1-2 retries per ~500 chunks, fast OTA convergence. |
| 180 | 2026-05-14 | **OTA write-path — strategy shift: single-shot DAC writes + image-level CRC verify.** 0.A.179 bench: SDK-based CLPEF returned st=0 sr1=0x00 (chip looks clean) but retry STILL hit WIP-stuck, and the chip wedged so badly that subsequent erase also failed. Every chip-side retry approach we've tried wedges the chip — whether via raw STIG CLPEF (0.A.178) or SDK CLPEF (0.A.179). The chip enters some latched state after a partial DAC write that nothing clears short of power-cycle. **Strategy shift:** stop retrying at the LP side. Accept that DAC has stochastic byte loss. (1) CHUNK handler: single-shot DAC write per chunk, no retry. (2) FINALIZE: compute full Bank B CRC32, compare to expected; if mismatch, abort OTA gracefully and let the bridge re-OTA. (3) LP stays alive, chip stays usable. (4) Stage-copy unchanged (uses SDK INDIRECT since CPSW is closed there). The bridge can iterate on the protocol side until an OTA attempt has a clean Bank B. Bridge-side per-chunk retry protocol is the proper long-term fix. |
| 179 | 2026-05-14 | **OTA write-path — switch CLPEF to SDK `OSPI_writeCmd` (was raw STIG).** 0.A.178 bench: first chunk write succeeded with random byte loss (verify mismatch), then retry hit WIP-stuck DESPITE the CLPEF call between attempts. The STIG-based CLPEF was apparently being silently rejected by the chip — likely sent in wrong protocol for QPI+DTR mode. **This build:** `hal_flash_clpef` now uses SDK `OSPI_writeCmd` with cmd 0x30 — same path the SDK's internal error-recovery uses. Also logs SR1 after CLPEF for diagnosis. If this also fails, we'll need a bigger architectural change (chip mode swap to SDR, or pre-Enet-close OTA strategy). |
| 178 | 2026-05-14 | **OTA write-path — CHUNK back to DAC + verify+CLPEF+retry; stage-copy stays on SDK INDIRECT.** 0.A.177 reproduced the historical "WEL stuck, target reads FF" bug — SDK INDIRECT_WRITE genuinely doesn't work in CPSW-active context (the original justification for the DAC mode workaround in 0.A.139 is real). **For CHUNK:** revert to `hal_flash_write_dac`, but wrap with per-chunk verify-via-XIP-memcpy + retry (up to 3 attempts) + CLPEF (cmd 0x30) between attempts. CLPEF clears Cypress's PRGERR latch — without it, the second write to a partially-failed page is silently rejected (manifests as WIP-stuck — was the 0.A.173 bench failure). **For stage-copy:** keep `hal_flash_write` (SDK INDIRECT). At activate time CPSW is closed, so SDK should work fine and gives reliable writes to Bank A. Added `hal_flash_clpef()` helper in `Platform/hal_flash.c`. |
| 177 | 2026-05-14 | **🎯 OTA write-path — switch from DAC writes to SDK INDIRECT_WRITE_XFER (`hal_flash_write`).** 0.A.166-176 spent 10 versions tuning DAC writes; the corruption never went to zero. **Reading the SDK source revealed we've been off the beaten path the whole time:** `Flash_norOspiWrite` uses `OSPI_writeIndirect` (SRAM FIFO with proper handshaking), NEVER `writeDirect` (DAC). DAC is for reads only. The SDK has a real INDIRECT_WRITE state machine that polls SRAM_FILL and DONE_STATUS — none of which DAC mode does. Switching to `hal_flash_write` (wraps SDK `Flash_write`) for both CHUNK and stage-copy. The 0.A.78-108 era's "INDIRECT broken in CPSW context" was likely the same WCC=0xFF auto-poll issue we found in 0.A.175 — with WCC=0 disable at OTA task entry (also kept), INDIRECT should now work in CPSW context too. Also discovered (via SDK header `cslr_ospi.h`): bit 14 is DISABLE_POLLING and was ALREADY SET in default WCC (0x000340FF has 0x40 → bit 14 = 1) — but the OPCODE 0xFF still gets transmitted in some controller paths. WCC=0 nukes the opcode field too. |
| 176 | 2026-05-14 | **🎯 OTA write-path — disable WCC auto-polling at OTA task ENTRY (fixes CHUNK writes too).** 0.A.175 bench confirmed: (1) WCC=0 write took effect (`after = 0x00000000`); (2) **Bank B CRC also MISMATCH** — CHUNK writes are corrupt, not just stage-copy. The 0xFF reset opcode in WCC affects EVERY `hal_flash_write_dac` call. **Fix:** move WCC=0 disable from stage-copy block to `lp_ota_task` entry, BEFORE the listen loop. Now CHUNK writes happen with auto-polling already disabled, so Bank B is clean, and stage-copy reads clean source. This should be the real fix — if not, we'll add CLPEF between retries (Cypress PRGERR latch theory). |
| 175 | 2026-05-14 | **OTA write-path — write WCC=0, also CRC Bank B to see if CHUNK writes are corrupt too.** 0.A.174 bench: `WCC after = 0x000340FF` — same as before! Bit 14 was ALREADY SET in 0x000340FF (the 0x40 in byte 1 IS bit 14). So bit 14 = "enable polling" not "disable". My OR-with-bit-14 was a no-op. **This build:** write WCC = 0x00000000 (everything off, including OPCODE field which currently holds the dangerous 0xFF), read it back to see what bits stick. Also added: compute Bank B's full CRC32 vs expected BEFORE stage-copy. If Bank B doesn't match either, the CHUNK handler's DAC writes are also being corrupted by the auto-poll cycle, and stage-copy can never produce a correct Bank A no matter how clean its own writes are. |
| 174 | 2026-05-14 | **🎯 OTA write-path — disable controller auto-polling (root cause of all corruption).** 0.A.173 bench: iter 24 retry triggered, second write hit "WIP stuck after PP" (rc=-3). The smoking gun is the diagnostic from 0.A.169-0.A.173: **`WRITE_COMPLETION_CTRL_REG = 0x000340FF`** — the OPCODE field [7:0] is **`0xFF`**, which on Cypress S25HL512T is "Reset to Default I/O Mode". Every controller auto-poll cycle silently sends a chip-reset command, putting the chip in inconsistent protocol state and causing both the random byte losses AND the WIP-stuck-after-retry failures across 0.A.166-0.A.173. **Fix:** at stage-copy start, set `WRITE_COMPLETION_CTRL_REG` bit 14 (DISABLE_POLLING) — controller stops auto-polling; our own STIG-based `hal_wait_wip_clear` handles the wait correctly. This is THE bug. |
| 173 | 2026-05-14 | **🎯 OTA write-path — per-iter verify + retry (up to 3 attempts).** 0.A.172 bench: 32 B sub-PP + HwiP_disable still produced 20 random byte losses. The 8-20 mismatches across runs are STOCHASTIC timing noise we can't fully eliminate via single-shot writes. **Fix:** stop trying to make a single write perfect — instead, verify each 8 KB iter via XIP memcpy after writing, and re-write if any mismatch. NOR flash allows 1→0 transitions on any erased bit, so re-writing the SAME target bytes is idempotent: already-correct bytes stay correct (no bit changes), and FF holes get programmed to their target values. Up to 3 retries per iter; abort if still failing. Logs retry count per iter so we see the noise rate. Removed HwiP_disable (made things slightly worse, suggests OSPI controller depends on something interrupt-related). |
| 172 | 2026-05-14 | **OTA write-path — revert to 32 B sub-PP (16 B wedges chip), keep HwiP_disable.** 0.A.171 with 16 B sub-PP: chip wedged on the FIRST CHUNK write to Bank B — "WIP stuck after PP" + subsequent erases also failing. **Root cause:** 16 B is a PARTIAL cache line (R5F D-cache line = 32 B). Writing < 1 line to a cacheable region triggers the cache controller's read-allocate behavior — it READS the line from OSPI before merging the new bytes — and that mid-cycle read confuses both the controller's pipeline and the chip's WIP state. **So 32 B = 1 cache line is the FLOOR.** Keeping the HwiP_disable around each stage-copy iter from 0.A.171 — that addresses the 8 residual mismatches differently (ISR window elimination). If HwiP_disable + 32 B sub-PP hits zero mismatches, we're done. If still ~8 bytes, next iteration adds whole-image retry-on-CRC-mismatch (3 attempts). |
| 171 | 2026-05-14 | **OTA write-path — 16 B sub-PP + HwiP_disable per iter.** 0.A.170 with 32 B sub-PP: mismatch 44→8. The 32 B run also BROKE the constant-40-byte-loss pattern (only 4 bytes lost in the captured page), suggesting 32 B = atomic AXI burst boundary. Remaining 8 mismatches are likely an ISR-induced timing race — bench showed CPU load 98.6% during stage-copy with a `[SAFEMODE] comm lost` task firing. **Fix:** (1) halve sub-PP to 16 B (half-cache-line, safest single AXI transaction), (2) wrap each `hal_flash_write_dac` call in `HwiP_disable`/`HwiP_restore` so no ISR can run between sub-PPs and corrupt the controller's mid-burst pipeline state. Trajectory 1322→413→44→8 → expect 0. |
| 170 | 2026-05-14 | **OTA write-path — drop sub-PP to 32 B (= 1 cache line / 1 AXI burst).** 0.A.169 bench: SRAM_PARTITION_REG=0x3F (rd=252 B, wr=772 B — plenty of room, my earlier theory wrong), WRITE_COMPLETION_CTRL_REG=0x000340FF (auto-polling enabled), and mismatch went from 413→**44** with 64 B sub-PP. The CRITICAL pattern: **always exactly 40 bytes lost at the tail of every affected PP**, regardless of sub-PP size (256→216, 128→88, 64→24 — always −40). That constant 40 B is the controller's in-flight pipeline depth that gets dropped when DAC is disabled mid-burst. R5F D-cache line = 32 B; 32 B sub-PP = exactly one cache line = one AXI burst with no inter-burst idle, so the controller's auto-PP trigger can't fire mid-burst. Mismatch trajectory: 1322 → 413 → 44 (10× per halving) — 32 B should hit zero. |
| 169 | 2026-05-14 | **OTA write-path — drop DAC sub-PP to 64 B, dump WRITE_COMPLETION_CTRL_REG.** 0.A.168 with 128 B sub-PP: mismatch count went from 1322→**413** (3× better, but not zero). Still byte 216 corruption pattern. Within each affected page, sub-PP 1 (bytes 0-127) wrote all 128 cleanly, but sub-PP 2 (bytes 128-255) only programmed 88 of 128 before losing bytes 216-255. So the actual controller threshold is ~88 bytes per burst, not 216 (216 = SRAM partition; 88 = auto-PP threshold within that partition). 64 B sub-PPs sit safely below 88. Also added WRITE_COMPLETION_CTRL_REG dump @0x38 to confirm the threshold value. |
| 168 | 2026-05-14 | **🎯 OTA write-path — split 256 B PP into two 128 B sub-PPs (SRAM partition fix).** 0.A.167 DSB barrier didn't fix the corruption. 0.A.167 bench: corruption at byte 24024 = byte **216** of page 93. **Exactly the same byte 216 as the 0.A.166 run** — the bug is deterministic, not a race. 216 bytes ≈ 54 32-bit words = the OSPI controller's SRAM write partition. The SDK Cypress driver leaves only ~216 bytes of SRAM for DAC writes; any AHB write past that boundary gets silently dropped by the controller. **Fix:** split each 256-byte PP into TWO 128-byte sub-PPs in `hal_flash_dac_pp` (which now wraps a new `hal_flash_dac_pp_one` helper). Each sub-PP is well below the 216 B limit and is functionally identical to one full PP — Cypress S25HL512T supports 1-256 byte PPs that program contiguous bytes from the given address. Also added a SRAM_PARTITION_REG diagnostic at stage-copy entry so we can see the actual rd/wr split in bench output. |
| 167 | 2026-05-14 | **🎯 OTA write-path — DSB barrier in hal_flash_dac_pp.** 0.A.166 bench gave the smoking gun: **first_diff at image[+25560] / chipA @0x0863D8, byte 216 within page 99, mid-page non-aligned. 1322 of 497266 bytes mismatch (0.27%).** Bytes 0-215 of page 99 are correct, bytes 216-255 are 0xFF (erased). This pattern is the textbook **write-buffer race signature** on ARM R5F. `memcpy` issues STR instructions that queue in the CPU's write buffer; stores commit to cache asynchronously. `CacheP_wbInv` flushes cache lines to memory — but if it runs BEFORE the tail-end memcpy stores have committed, those late stores land in cache AFTER wbInv invalidated, never reach the chip. **Fix:** add `__asm__ volatile("dsb sy" : : : "memory")` after `memcpy` (and after `CacheP_wbInv` for good measure) in `hal_flash_dac_pp`. The DSB forces all pending stores to drain before wbInv runs. Also: 0.A.166's CRC-mismatch abort SAVED the user — LP didn't brick this time, just aborted stage-copy and kept running normally (CPU 1.6%, heartbeat normal). |
| 166 | 2026-05-14 | **OTA stage-copy — find first-diff byte after CRC32 mismatch.** 0.A.165 bench was decisive: Bank-A CRC = 0xB426EE09, expected = 0x0917F6C0 — **MISMATCH**. So Bank A is NOT bit-exact to Bank B; the 3-probe verify of 0.A.162-0.A.164 missed corruption that lives outside those 3 sample offsets. SBL was right to reject all along — the image really is corrupted. **This build:** on CRC mismatch, scan Bank A vs Bank B byte-by-byte, report (1) total mismatch count, (2) offset of first differing byte (image-relative + chip-address-relative), (3) 16 bytes of context from both A and B around the first diff, (4) boundary-alignment diagnosis (8 KB iter / 4 KB / 1 KB / 256 B page / 64 B cache-line / mid-page). This tells us whether the corruption pattern is per-iter, per-page, cache-line related, or scattered. Also: ABORT stage-copy and return -1 on CRC mismatch BEFORE warm-reset — prevents bricking Bank A on every bad attempt. |
| 165 | 2026-05-14 | **OTA stage-copy — full-image CRC32 of Bank A vs expected.** 0.A.164 bench: dual-protocol chip reset + watchdog probe ran but SBL still failed. Watchdog @0x180000 is intact (`looks like mcelf.hs_fs`), 3-probe Bank-A verify shows A==B at start/middle/near-end. **But 3-probe is not enough — could miss single-byte errors elsewhere.** This build replaces 3-probe with **full CRC32 of Bank A** (read every byte 0..image_size via XIP memcpy, accumulate CRC32, compare to `s_image_expected_crc` from the FwBeginUpdate message). If CRC matches → image is bit-exact bit-for-bit, SBL failure is downstream (chip state, OSPI controller state, CPSW residue, or SBL's parse logic). If CRC mismatches → narrows down which write was corrupt. Also confirms Flash-LP.ps1 uses the same warm-reset path (Sciclient_pmDeviceReset == TISCI_MSG_SYS_RESET = SoC-level reset). So warm-reset mechanism is identical; the difference must be SoC/peripheral state our prior firmware left behind. |
| 164 | 2026-05-14 | **OTA activate — dual-protocol chip reset + watchdog-region diagnostic.** 0.A.163 bench: chip soft-reset reported rc=0 but SBL STILL failed. Either the RSTEN+RST opcodes weren't actually recognized by the chip (sent in wrong protocol), or the failure isn't chip-mode at all. **This build:** (1) `hal_flash_chip_soft_reset` now does TWO attempts — first in current protocol (4-line DTR, catches chip-in-QPI-DTR case), then re-configures DEV_INSTR_RD_CONFIG to single-line SDR and retries (catches chip-already-in-1S case). 10 ms recovery wait. Also explicitly clears DAC bit, IND_AHB_ADDR_TRIGGER, INDIRECT_RD/WR CTRL regs. (2) Added watchdog-region probe: read 16 bytes from `LP_OTA_WATCHDOG_OFFSET` (0x180000) via DAC memcpy and tag as "looks like mcelf.hs_fs" / "ALL 0xFF (erased!)" / "UNKNOWN". If watchdog is 0xFF, that's the SBL failure source (SBL boots watchdog FIRST as R5FSS1_0; if it can't load that partition, "Some tests have failed!!" fires before our main image is even attempted). |
| 163 | 2026-05-14 | **OTA activate — chip software reset before warm-reset.** 0.A.162 bench was a breakthrough diagnostic: verify probes at start (0x080000), middle (0x0C0000), and near-end (0x0F9242) **all showed `A==B`** — DAC writes preserve bytes correctly at the destination address. So writes are fine; the SBL was failing for a different reason. **Root cause:** the chip's volatile config (CFR2V) still says "4S-4D-4D DTR (QPI+DTR)" from our `Flash_open`, and the warm-reset doesn't power-cycle the chip — it stays in QPI+DTR. The next SBL boot tries to talk to the chip in single-line SDR (its default assumption), the chip ignores those commands, reads return garbage, partition parsing fails → "Some tests have failed!!". This is why Flash-LP.ps1 + power-cycle works (chip resets to single-line SDR on power-on) but warm-reset doesn't. **Fix:** new `hal_flash_chip_soft_reset()` in `Platform/hal_flash.c` — issues WRDI (0x04) + RSTEN (0x66) + RST (0x99) via STIG with a ~1.5 ms calibrated busy-wait for tRPH. Cypress S25HL512T software-reset clears volatile config and returns the chip to factory single-line SDR — same state as a power-cycle. The activate handler calls this right before `Sciclient_pmDeviceReset`. If this works, OTA finally completes end-to-end. |
| 162 | 2026-05-14 | **OTA stage-copy — post-copy Bank-A vs Bank-B verification.** 0.A.161 bench: all 61 iters completed, `iter0 source` showed the correct x509 cert header `30 82 06 72 …` — so reads via DAC memcpy from Bank B work — but warm-reset still landed at SBL "Some tests have failed!!". Three possibilities remain: (a) DAC writes silently fail (chip ignores them), (b) DAC writes corrupt bytes (byte-swap / mode mismatch vs SDK INDIRECT path), (c) writes succeed correctly and SBL fails for a different reason (signature, address, header). **This build:** after copy completes, re-enable DAC and read Bank A + Bank B at THREE probe offsets (start / middle 256 KB in / near-end image_size-64) via XIP memcpy. Log each as "A==B" or "A!=B"; on mismatch, log the differing Bank-B bytes too. 5-second `vTaskDelay` before warm-reset so the bench operator can read the verdict before screen wipes. Tells us conclusively whether the write path is preserving bytes correctly. |
| 161 | 2026-05-14 | **OTA stage-copy — enable DAC bit before diagnostic xip read.** 0.A.160 bench: confirmed `OSPI CONFIG_REG = 0x80083841` with DAC bit OFF after Board_flashOpen (LP build's syscfg keeps DAC disabled by default — the existing hal_flash.c comment is correct). The diagnostic xip read at 0x60000000+0x900000 with DAC OFF hung the AHB bus. **Fix:** enable DAC bit (CONFIG_REG[7]) BEFORE the diagnostic xip read; verify it stuck by re-reading and logging. The main loop already enables DAC per-iter, but we never got there because the diag wedged first. Adds pre/post enable log so we can see the bit transition. |
| 160 | 2026-05-14 | **OTA stage-copy — bare-metal DAC XIP memcpy for reads.** 0.A.159 bench resolved two key questions: (1) SDK `Flash_read` returns valid bytes (`30 82 06 72 30 82 04 5A …` — x509 cert header confirmed) for a 16-byte read; (2) STIG cmd 0x13 returns all zeros (chip in QPI+DTR mode doesn't recognize single-line SDR opcodes — confirms our 0.A.158 root cause). BUT the 8 KB iter-0 SDK `Flash_read` hangs — 16 B fits in one OSPI SRAM FIFO burst; 8 KB needs multiple burst cycles where the wedge lives after a prior DAC write. **Fix:** bypass SDK reads entirely. Stage-copy reads now use plain word-at-a-time memcpy from the XIP read window at `0x60000000 + bank_b_offset`. The OSPI controller's DEV_INSTR_RD_CONFIG was set up by Board_flashOpen for the chip's QPI+DTR mode (cmd 0xEC, 4-line DTR, 8 dummy) — XIP reads just work transparently. `hal_flash_write_dac` clears the DAC bit on exit so we re-enable it at the top of each iter. Diagnostic now prints the DAC bit state + 16 bytes via XIP memcpy so we can verify the read path before committing. Abort-on-all-0x00/0xFF safety prevents bricking Bank A. |
| 159 | 2026-05-14 | **OTA stage-copy — diagnostic + revert read to SDK with defensive INDIRECT-state clears.** 0.A.158 bench: stage-copy COMPLETED all 61 iters cleanly (STIG-read + DAC-write didn't wedge!) and warm-reset triggered DMSC firmware load — but SBL reported "Some tests have failed!!" meaning Bank A at 0x080000 doesn't validate as a signed image. Hypothesis: STIG cmd 0x13 (READ4B, single-line SDR fast-read) doesn't return chip data in 4S-4D-4D DTR (QPI+DTR) mode — chip only recognizes 4-line DTR opcodes like 0xEC. So stage-copy wrote 0xFF (or garbage) to Bank A, SBL rightly rejected it. **This build:** (1) add diagnostic before stage-copy that reads first 16 bytes via BOTH SDK `Flash_read` AND `hal_flash_read_stig`, prints both — confirms which method actually returns valid bytes. (2) revert stage-copy read path to SDK `hal_flash_read` (works fine post-CPSW-close, 0.A.143 readback proved it). (3) add `hal_flash_clear_indirect_state()` (now clears both RD+WR CTRL since 0.A.158) before each Flash_read. (4) add 100 µs settle delay between read and write phases. (5) iter-0 source bytes printed + all-FF check that aborts copy if read returned nothing (prevents bricking Bank A). |
| 158 | 2026-05-14 | **OTA stage-copy — STIG-based bare-metal read replaces SDK Flash_read.** 0.A.157 bench: 4 KB chunks reached iter 48 (of 121) before hanging. The R↔W alternation between SDK `Flash_read` (INDIRECT_READ_XFER) and `hal_flash_write_dac` (DAC mode) is wedging the OSPI controller after enough mode switches, regardless of chunk size. **Fix:** added `hal_flash_read_stig()` to `Platform/hal_flash.c` — reads flash 8 bytes at a time via FLASH_CMD_CTRL_REG (STIG path, opcode 0x13 READ4B, 4-byte address, 8 dummy cycles). STIG operations complete in a single MMIO write so they can't accumulate "in-progress" controller state between calls. Stage-copy now uses STIG-read + DAC-write — both bare-metal, both proven to work in isolation. Also bumped `hal_flash_clear_indirect_state()` to clear INDIRECT_READ_XFER_CTRL_REG (0x60) in addition to the write side. Stage-copy buffer up to 8 KB (62 iters for 494 KB). STIG read throughput ~1.6 MB/s vs SDK ~10 MB/s but never wedges. |
| 157 | 2026-05-14 | **OTA stage-copy — 4 KB chunked copy to reduce mode-switch frequency.** 0.A.156 bench: stage-copy iters 0-8 succeeded, iter 9 hangs (read OR write of iter 9 stalls). The 256-byte R/W alternation pattern (1932 mode switches between INDIRECT_READ for SDK Flash_read and DAC mode for our write) accumulates something in the OSPI controller that locks up around the 9th switch. **Fix:** bump stage-copy chunk size 256 B → 4 KB. SDK `hal_flash_read` handles arbitrary lengths, `hal_flash_write_dac` internally breaks the chunk into 16 page programs. Net: ~121 iters instead of ~1932, much less rapid mode-switching. Pattern is now `R(4KB)-W(4KB×16 pages in one suspendAll block)-R-W-...` instead of `R(256)-W(256)-R-W-...`. Buffer grew 256 B → 4096 B (still tiny). |
| 156 | 2026-05-14 | **OTA stage-copy — wider iter-logging window.** 0.A.155 bench: iters 0, 1, 2 all rc=0 OK, then silence. Hang is between iter 3 and iter 256. **This build:** verbose log for iters 0-15 + every 32nd iter (iters 32, 64, 96, ...) so we can pinpoint the exact iter that hangs. Single-line per-iter (combined r@/w@) to keep UART overhead manageable. |
| 155 | 2026-05-14 | **OTA stage-copy — per-iteration diagnostic to localize copy hang.** 0.A.154 bench: stage-copy completed both block erases cleanly, logged `erase done, starting B->A copy`, then hung BEFORE the first 64 KB progress milestone. So the first 256 iterations of the read+write loop contains the hang. **This build:** added verbose logging for the first 3 iterations showing `read @0xNNNNNN`, `read rc=N, write @0xNNNNNN`, `write rc=N` — pinpoints exactly which call hangs. If iter 0 read hangs → SDK Flash_read still has issues post-Bank-A-block-erase. If iter 0 read succeeds and write hangs → some interaction between bare-metal erase-block and subsequent DAC write. |
| 154 | 2026-05-14 | **OTA stage-copy — revert read to SDK `hal_flash_read`, add per-step logging.** 0.A.153 bench: stage-copy started but wedged before completing (silence after `[OTA] staging Bank B -> Bank A (494162 B)`). The new `hal_flash_read_dac` (XIP memcpy + cache invalidate) hangs in stage_copy the same way it hung in FINALIZE — root cause not yet localized. **Pragmatic fix:** revert stage-copy's READ path to the SDK `hal_flash_read` which works fine post-CPSW-close (proven in 0.A.143's old readback flow). Keep the WRITE bare-metal (`hal_flash_write_dac`) since SDK `Flash_write` doesn't program the chip. Also keep the bare-metal block-erase (`hal_flash_erase_block_dac`) — only 2 calls for 494 KB image. Added explicit progress logs: per-block erase result, "erase done, starting copy", and 64 KB progress milestones during the copy so we can tell exactly where any future hang occurs. |
| 153 | 2026-05-14 | **OTA stage-copy — fix Bank A erase O(N²) bug + use bare-metal paths.** 0.A.152 bench: streaming OTA worked end-to-end for `reboot=1`, the stage-copy started ("staging Bank B -> Bank A (494146 B)"), then LP wedged at 100 % CPU for 10+ seconds. **Root cause:** `lp_ota_stage_copy_b_to_a` was calling `hal_flash_erase_sector` in a loop of 256 iterations (4 KB step × 1 MB region). The SDK internally aligns each call to a 256 KB block boundary and erases the WHOLE BLOCK — so each of 4 blocks got erased ~64 times = up to ~10 minutes worst case. Legacy comment said "256 sectors at ~50 ms each ≈ 13 s" assuming sector-granularity erase that the chip/SDK don't actually support. **Fix:** use bare-metal `hal_flash_erase_block_dac` and erase only the blocks needed for the image (ceil(image_size / 256 KB) = 2 for the 494 KB main image). Also: stage-copy's read+write loop now uses `hal_flash_read_dac` + `hal_flash_write_dac` (bare-metal everywhere; no SDK Flash driver in the activate-time hot path). Bank A erase for a 494 KB image now takes ~2 × 150 ms = ~300 ms instead of minutes. Stage-copy total time: ~1 second. |
| 152 | 2026-05-14 | **OTA streaming — skip FINALIZE readback temporarily (per-chunk CRCs are enough).** 0.A.151 bench: `hal_flash_read_dac` (XIP memcpy + cache invalidate) ALSO hung with CPSW active. The readback hang isn't specific to the SDK Flash driver — even bare-metal XIP memcpy stalls. Root cause not yet localized (cache config / DAC toggle ordering / AHB contention all candidates). **Pragmatic shortcut:** skip whole-image readback entirely in FINALIZE. Per-chunk CRCs already verify what the bridge sent matches what reached the chip — defense-in-depth full-image CRC is nice but not required to ship. Can be re-added later via STIG reads (cmd 0x13 + addr, returns up to 8 bytes per STIG; 4096 STIG reads × 5 μs = 20 ms for 32 KB, fast enough). Streaming OTA flow now: BEGIN → erase Bank B → CHUNK × N → FINALIZE (just ack ok) → ACTIVATE → optional stage-copy + reboot. |
| 151 | 2026-05-14 | **🎯 OTA streaming — bare-metal `hal_flash_read_dac()` for FINALIZE readback.** 0.A.150 bench gave a perfect diagnostic: all 32 chunks logged (off=0 through 31744), then `[OTA] FINALIZE entered (bytes_written=32768 of 32768)` — then silence + bridge ECONNRESET. **The FINALIZE readback loop calling `hal_flash_read` hangs** with CPSW active, same SDK bug class as `Flash_eraseBlk` / `Flash_norOspiWaitReady`. **Fix:** new `hal_flash_read_dac(addr, buf, len)` in `Platform/hal_flash.c` — enables DAC mode if disabled, invalidates cache range, memcpy from `0x60000000 + addr` (XIP read window), restores DAC state. Direct AHB read, no SDK Flash driver involved, no scheduler suspend. FINALIZE handler now uses this instead of `hal_flash_read`. The third leg of the bare-metal trio (write/erase already done; now read). Expected outcome: `[OTA] FINALIZE entered ...` → `[OTA] FINALIZE ok crc=0x0509f484 (Bank B verified)` → `[OTA] ACTIVATE` → `[OTA] reboot=0 — Bank B image committed, no promotion`. |
| 150 | 2026-05-14 | **OTA streaming diagnostic — log every chunk + FINALIZE entry.** 0.A.149 bench: same partial-stream stall as 0.A.147 (chunks logged at off=0, 1024, 2048, 8192, 16384, 24576 then silence). New diagnostics (buffer full / lwip_recv error / push_status send fail) didn't fire, so the failure is elsewhere. **This build logs EVERY chunk (32 lines total for 32 KB) so we can see exactly which chunk is the last one processed.** Also logs `[OTA] FINALIZE entered ...` so we can tell if FINALIZE arrives and the readback loop is the hang point. Decision tree from bench output: (a) all 32 chunks logged + FINALIZE entered + then silence → FINALIZE readback hangs; (b) chunks stop at some N < 32 → hal_flash_write_dac hangs on that chunk; (c) chunks log all 32 but no FINALIZE entered → bridge doesn't send FINALIZE / lwIP doesn't deliver it. |
| 149 | 2026-05-14 | **OTA — fix stack overflow caused by 0.A.148's buffer bump.** 0.A.148 raised `LP_OTA_MAX_FRAME` 2048 → 4096, growing each `ota_conn_t` to ~4108 B. The OTA task allocates `conns[LP_OTA_MAX_CONNS=2]` on its stack — 8216 B for the array alone, exceeding the 8 KB task stack (`gLpOtaStack[8192]` in `main.c:303`). Result: silent stack smash → no `[OTA] Phase 1A listening` log → LP appears dead after the boot banner. **Fix:** declare `conns` as `static` so it lives in BSS instead. Net memory cost: 8 KB BSS (unchanged net since the stack space is reclaimed). |
| 148 | 2026-05-14 | **OTA streaming — diagnostics + buffer bump.** 0.A.147 bench: streaming worked for chunks 0-24576 (~78 % of 32 KB image), stalled before completion. Suspected causes: (1) LP's per-conn rx buffer (`LP_OTA_MAX_FRAME = 2048` + 4 = 2052 B) is just barely smaller than 2 chunk frames (each ~1040 B), so when chunks burst in we might hit the "buffer full → close" path; (2) `lwip_send` from `push_status_progress` may fail silently under TCP backpressure. **This build:** (a) bumped `LP_OTA_MAX_FRAME` 2048 → 4096 so 3-4 chunk frames fit; (b) `push_status_progress` now logs when `send_frame` returns non-zero; (c) the rx-buffer-full + lwip_recv-error close paths now log before closing the connection, so we can tell from COM5 whether the LP is the one resetting the TCP. Bench should answer: either (1) full clean stream now, or (2) we see the buffer-full / send-fail / recv-fail diagnostic at the exact failure point. |
| 147 | 2026-05-13 | **OTA streaming — add chunk-arrival diagnostic logging.** 0.A.146 bench: erase completed (BEGIN ack sent fine) but bridge got ECONNRESET 21 s later. COM5 silent between erase complete and the bridge giving up. Unknown if chunks arrive + the chunk handler runs at all. Added one-line log on every CHUNK handler entry for the first 3 chunks (off=0, 1024, 2048) + every 8 KB after. This will tell us: (1) chunks ARE arriving but write hangs / fails → see first CHUNK log then silence; (2) no chunks at all arriving → no CHUNK logs at all (LP's lwIP isn't delivering them or the OTA service loop is stuck); (3) several CHUNK logs then a stall at some specific offset → know exactly where it dies. |
| 146 | 2026-05-13 | **OTA — drop `vTaskSuspendAll` from `hal_flash_erase_block_dac` so lwIP keeps running during the 911 ms erase.** 0.A.145 bench evidence: erase ITSELF worked (`BEGIN erase complete in 911 ms`) but the bridge then hit `ECONNRESET` 21 s later because lwIP couldn't ACK incoming TCP segments during the 911 ms suspended-scheduler window — bridge's TCP timeout fired and reset the connection. **Fix:** `hal_flash_erase_block_dac` no longer wraps the loop in `vTaskSuspendAll`. The bounded-poll STIG operations are safe to be preempted (each is a single atomic MMIO write at the controller register level; no FIFO state, no SRAM partition state, no inter-instruction sequencing that an IRQ could disrupt). lwIP TCP RX/ACK keeps running during the erase. The chip continues its internal erase regardless of CPU activity. `hal_flash_write_dac` keeps its suspendAll because writes are only ~75 ms and benefit from atomicity. |
| 145 | 2026-05-13 | **OTA — bare-metal block erase to fix streaming hang.** 0.A.144 bench result: streaming BEGIN logged `BEGIN erasing 1 Bank-B block(s) (256 KB)` then hung. Root cause: `hal_flash_erase_sector` calls the SDK's `Flash_eraseBlk` → `Flash_norOspiWaitReady` which has a documented `\|\|` vs `&&` bug — when called while CPSW + lwIP are active, the chip's WIP-poll status comes back inconsistently and the infinite loop never exits. The same SDK function works fine when called with Enet closed (which is why 0.A.143's activate-time erase worked). **Fix:** new `hal_flash_erase_block_dac(addr)` in `Platform/hal_flash.c` — bare-metal STIG block-erase (cmd 0xDC, 4-byte address). Uses our bounded `hal_wait_wip_clear` (max-polls capped, no infinite loop). Mirrors `hal_flash_write_dac`'s approach. Streaming BEGIN handler now calls `hal_flash_erase_block_dac` instead of `hal_flash_erase_sector`. Block erase typ 150 ms / max 2.7 s per 256 KB block on the Cypress S25HL512T; LP stays responsive throughout. |
| 144 | 2026-05-13 | **OTA — switch from buffered to streaming protocol.** 0.A.143 used a 64 KB MSRAM buffer to hold the whole image before writing; this caps images at 64 KB (real LP firmware is ~485 KB). **This build:** stream each CHUNK directly to Bank B as it arrives via `hal_flash_write_dac`, no intermediate buffer. Flow: BEGIN erases Bank B (block-erase, 256 KB granularity) up-front, returns ok; CHUNK writes its 1 KB payload directly to `FW_BANK_B_OFFSET + chunk.offset` (per-chunk CRC verified first); FINALIZE reads back the whole image incrementally (256 B at a time via `s_verify_buf`) and re-computes CRC32 to verify against the expected; ACTIVATE on `reboot=0` is a no-op (Bank B already committed and verified) — on `reboot=1` closes Enet + stage-copy B→A + warm-reset. **Memory savings:** dropped 64 KB `s_image_buf`; new staging scratch is two 256-byte buffers (`s_page_pad` for partial-last-page fixup, `s_verify_buf` for readback CRC), net -64 KB MSRAM. **Risk:** streaming means `hal_flash_write_dac` runs while CPSW + lwIP are still active during BEGIN/CHUNK/FINALIZE. The DAC path itself doesn't depend on Enet, but each call still does `vTaskSuspendAll` internally so lwIP can't be scheduled during the ~2 ms 4-page write. lwIP RX should buffer the next TCP chunk in the CPSW hardware queue. If this races, the symptom would be TCP retransmits, not a wedge. |
| 143 | 2026-05-13 | **OTA — make `EnetApp_isPortLinked` tolerant of closed Enet.** 0.A.142 bench result: `hal_flash_write_dac: rc=0, 32768 B in 72619 us` — the write SUCCEEDED, but the activate SUCCESS line + 5 readback verifies didn't print because the `test_enet.c:391` assertion fired *between* hal_flash_write_dac returning and the readback loop running. Root cause: `EnetApp_isPortLinked` is registered as `isPortLinkedFxn` callback in `ti_enet_lwipif.c:310`; lwIP polls it periodically. After `EnetApp_driverClose`, `Enet_getHandleInfo` on the closed handle fails and the original `EnetAppUtils_assert(status == ENET_SOK)` wedges CPU at 100 %. **Fix:** changed the assert to a graceful `return false` (link down) when `Enet_getHandleInfo` fails. lwIP just sees "no link" — no crash, no wedge. The OTA activate flow can now complete its readback verification + SUCCESS log + B→A staging copy without the assertion handler grabbing the CPU. |
| 142 | 2026-05-13 | **OTA — restore CANCEL guard BEFORE the erase loop (regression fix vs 0.A.141).** 0.A.141 refactor moved the `INDIRECT_WR_CTRL CANCEL+OPS_DONE` write from inline-in-`lp_ota_task.c` (where it ran BEFORE the erase loop) into `hal_flash_write_dac` (where it runs AFTER the erase). Bench result: erase loop hangs forever at 100 % CPU. The CANCEL guard was load-bearing for erase reliability even though the register was already `0x00000000` on entry — the W1C writes apparently nudge controller state in a way the SDK's `Flash_eraseBlk` depends on. **This build:** added public `hal_flash_clear_indirect_state()` to `Platform/hal_flash.c` (exposes the same write); OTA activate handler calls it before the erase loop. Expected outcome: same as 0.A.140 — erase completes in ~5 s, `[OTA] starting DAC-mode write loop` prints, `hal_flash_write_dac: rc=0` in ~75 ms, 5 readback `OK` lines, `activate SUCCESS`. |
| 141 | 2026-05-13 | **🚀 OTA DAC mode — promote `lp_dac_pp` to `hal_flash_write_dac()` public API.** Moves the runtime OTA write path from experimental code in `lp_ota_task.c` into the proper HAL at `Platform/hal_flash.c`. Public signature: `int hal_flash_write_dac(uint32_t addr, const uint8_t *src, uint32_t len)` declared in `Platform/hal.h`. Caller-side requirements: page-aligned `addr` and `len` (256-byte multiples), region pre-erased. Internally: wraps the whole write loop in `vTaskSuspendAll` to prevent CPSW preemption, does WREN STIG + DAC memcpy + **`CacheP_wbInv`** + RDSR WIP poll + WEL-cleared verification per page. Also: (1) `lp_ota_stage_copy_b_to_a` updated to use `hal_flash_write_dac` instead of the broken-in-runtime `hal_flash_write` (B→A copy path is now functional). (2) Activate handler now does staging copy + warm-reset when bridge sends `reboot=1` (true production OTA flow); `--no-reboot` keeps the bench-POC behavior (Bank B scratch, no reset). (3) Bench-test target stays at `FW_BANK_B_OFFSET (0x900000)` for safety — when `reboot=1` triggers, the validated Bank B content gets copied to Bank A live offset. (4) `lp_ota_task.c` dropped ~437 lines (OSPI register defines + STIG helpers + lp_bare_pp + lp_dac_pp all moved to `hal_flash.c`). |
| 140 | 2026-05-13 | **OTA — silence CPSW PHY-poll task before `EnetApp_driverClose`.** 0.A.139 confirmed DAC PP loop completes reliably (`128 pages in 73374 us, err=0` — 2nd consecutive clean run). Remaining wart: the CPSW PHY-poll background task (created by `EnetApp_createPhyRegisterPollingTask` in `test_enet_cpsw.c:351`) keeps firing after Enet is closed, asserting on the invalid Enet handle (`test_enet.c:391`, `test_enet_cpsw.c:215`) and eventually wedging CPU at 100 %. **This build:** added `EnetApp_stopPhyRegisterPollingTask()` to `test_enet_cpsw.c` (calls `ClockP_stop` on the polling timer; task then blocks on its semaphore forever — harmless). Declared in `test_enet_extphy.h`. The OTA activate handler calls this BEFORE `EnetApp_driverClose`. Should eliminate all post-activate CPSW assertions and let the readback verification + activate SUCCESS log print cleanly. **Target outcome:** full clean activate path — `[OTA] stopping PHY-poll task` → close Enet → re-open OSPI → DAC PP loop done → 5 readback `OK` lines → `[OTA] activate SUCCESS via DAC-MODE PP: 32768 B flashed to 0x900000 (128 pages)`. LP stays at low CPU after activate. |
| 139 | 2026-05-13 | **OTA DAC mode — revert in-suspend readback (was causing hang); accept best-effort verification.** 0.A.138 bench result: LP hung at 100 % CPU right after `[OTA] starting DAC PP loop` — the in-suspend memcpy readback from XIP region (re-enabling DAC briefly + reading 5×8 bytes) stalled the AHB bus. Reverted to 0.A.137's pattern: DAC PP loop runs inside `vTaskSuspendAll`, readback runs outside via `hal_flash_read` (best-effort — CPSW assertion may preempt). The DAC writes themselves are PROVEN working from 0.A.137 (128 pages in 74.7 ms, err=0, page 1 readback from 0.A.133 matched source `A5 A6 A7 A8...`). This build accepts that automated readback may not always complete due to CPSW preemption, but the writes are reliable. **Status:** runtime OTA write path via DAC mode is functional. Next steps: silence CPSW PHY-poll task before activate so readback completes reliably; then promote `lp_dac_pp` to `hal_flash_write_dac()` as the shippable runtime write path. |
| 138 | 2026-05-13 | **OTA DAC mode — readback verification inside suspended region.** 0.A.137 bench result: 🎉 `[OTA] DAC PP loop done: 128 pages in 74725 us, err=0` — ALL 128 PAGES PROGRAMMED CLEANLY. The `CacheP_wbInv` was the bug. But the CPSW assertion (`test_enet.c:391`) fired 6 ms after the loop done log, before the readback verification (outside the suspended region) could run, wedging the LP at 100 % CPU. **This build:** capture the 5 readback samples INSIDE the same `vTaskSuspendAll()` region as the DAC PP loop. After the loop completes, re-enable DAC mode briefly, memcpy 8 bytes from each sample offset at `0x60000000 + flash_addr` (XIP read with DAC enabled), disable DAC, resume scheduler. Log results from the stack buffer after resume — no risk of CPSW preemption mid-readback. Cache lines covering the written region were already invalidated by per-page `CacheP_wbInv`, so XIP reads fetch fresh data from OSPI. **Target outcome:** the loop done log + 5 `verify @0xNNNNNN ... OK` lines + `[OTA] activate SUCCESS via DAC-MODE PP`. If the readbacks all match source, the OTA path is shippable and `lp_dac_pp` graduates to `hal_flash_write_dac()`. |
| 137 | 2026-05-13 | **🎯 OTA DAC mode — THE BUG: missing `CacheP_wbInv` after memcpy.** Found by user-pushed re-examination of the SDK. The SDK's `OSPI_writeDirect` (`ospi_v0.c:994`) does: `OSPI_lld_writeDirect(...); CacheP_wbInv((void*)(attrs->dataBaseAddr + trans->addrOffset), trans->count, CacheP_TYPE_ALL);` — i.e., **cache writeback+invalidate AFTER the memcpy** to ensure CPU dirty cache lines flush to the OSPI controller's AHB window. Our bare-metal `lp_dac_pp` was missing this. The OSPI data region at `0x60000000` is mapped Normal Cacheable in our MPU config for fast XIP reads; CPU memcpy writes were going to the data cache, NOT to the OSPI controller. Without flush, the chip never saw the bytes → WEL stuck on subsequent RDSR. **This perfectly explains the 0.A.133 vs 0.A.135 regression**: in 0.A.133 the verbose `DebugP_log` calls between memcpy and the next chip operation gave the cache time to evict naturally → write reached chip → page 1 readback `A5 A6 A7 A8...` matched source. In 0.A.135 the silent loop ran fast enough that dirty cache lines were still in cache when the chip's RDSR was issued → chip got nothing → WEL stuck. **This build:** added `#include <kernel/dpl/CacheP.h>` and `CacheP_wbInv(dst, len, CacheP_TYPE_ALL);` immediately after the memcpy in `lp_dac_pp`. **Target outcome:** all 128 pages succeed deterministically, 5 readback verifies all OK, `[OTA] activate SUCCESS via DAC-MODE PP: 32768 B flashed to 0x900000 (128 pages)`. If this works we have a SHIPPABLE OTA write path. |
| 136 | 2026-05-13 | **🔑 OTA DAC mode — add `CONFIG_REG.IDLE` wait after STIG; explains 0.A.133→0.A.135 regression.** Diagnosis: in 0.A.133 page 1 worked (verified readback `A5 A6 A7...`), but in 0.A.135 page 1 failed with `WEL STUCK rc=-4 sr1=0x02` after only 11 μs. **The difference was DebugP_log calls** — 0.A.133 had verbose per-step logging which inadvertently inserted ~30 ms of UART output between WREN STIG and DAC memcpy. 0.A.135 stripped those logs, exposing a latent timing bug: our `lp_stig_wait` only checks `CMD_EXEC_STATUS` clear, but the SDK's `OSPI_flashExecCmd` also waits for `CONFIG_REG.IDLE_FLD` (bit 31) — without that wait, the controller's serial interface may still be clocking the tail of the STIG command when we switch to DAC mode, garbling the PP byte stream and the chip rejects it. **This build:** extend `lp_stig_wait` with a 2nd phase that polls `CONFIG_REG` bit 31 (IDLE) until set, mirroring SDK behavior. Every STIG now returns only after the controller is fully settled. Expected outcome: DAC PP loop completes all 128 pages in ~50 ms with `err=0`, 5 readback verifies all match source, `[OTA] activate SUCCESS via DAC-MODE PP`. |
| 135 | 2026-05-13 | **OTA DAC mode — strip all logging from inside suspended region.** 0.A.134 bench result: LP hung silently inside `vTaskSuspendAll`. Root cause: `lp_dac_pp` still had `DebugP_log` calls on failure paths. With scheduler suspended, `DebugP_log` (which uses a FreeRTOS sync primitive internally) blocks waiting for a wakeup that can't happen → deadlock the moment any page failed (likely page 3 hitting the same WEL STUCK issue we saw in 0.A.132). **This build:** strip ALL `DebugP_log` calls from `lp_dac_pp` — function is now totally silent. Return codes (-1, -3, -4) + `*out_sr1_after` encode failure state. The caller (activate handler) captures `fail_rc` and `fail_sr1` from the loop break, resumes scheduler, then logs the failure details with a human-readable reason. Loop should now either (a) complete all 128 pages → `[OTA] DAC PP loop done: 128 pages in N us, err=0` + 5 readback verify lines + activate SUCCESS, or (b) fail at page N → `[OTA] DAC PP FAIL @0xNNNNNN page#N rc=N sr1=0xNN (...)`. Either way the LP stays responsive — no more silent hang. |
| 134 | 2026-05-13 | **🎉 OTA DAC mode genuinely writes — full-loop test with scheduler suspended.** 0.A.133 bench result is the breakthrough: `[DAC] page #1 VERIFIED — bytes match source` with readback `A5 A6 A7 A8 A9 AA AB AC` exactly matching `s_image_buf[0..7]`. **DAC mode genuinely programs flash in our runtime context where INDIRECT_WRITE_XFER fails.** The only remaining problem is that the CPSW background task at `test_enet.c:391` fires async assertions ~5 s after Enet teardown and wedges CPU at 100 %, preventing the full 128-page loop from completing. **This build:** (1) `lp_dac_pp` is now SILENT on success (no per-page logging) — logs only on failure. (2) Activate handler wraps the entire DAC PP loop in `vTaskSuspendAll() / xTaskResumeAll()` so the CPSW background task can't preempt; with zero per-page UART overhead, all 128 pages should complete in well under 200 ms — way inside the 5 s CPSW crash window. (3) Readback verification moved outside the suspended region: sample 5 page offsets (0x000, 0x1000, 0x2000, 0x4000, 0x7F00) and confirm each readback matches source. (4) Total timing measured via `ClockP_getTimeUsec` and logged. **Target output:** `[OTA] DAC PP loop done: 128 pages in N us, err=0` followed by 5 `verify @0xNNNNNN ... OK` lines, then `[OTA] activate SUCCESS via DAC-MODE PP`. If we cross that finish line, we promote `lp_dac_pp` to `hal_flash_write_dac()` and ship DAC as the runtime write path. |
| 133 | 2026-05-13 | **OTA DAC mode — readback verification after each PP.** 0.A.132 bench result: pages 1 and 2 reported `WEL cleared — PP COMPLETED via DAC`, but page 3 failed with `WEL STUCK sr1=0x02`. The failure mode is *different* between pages 1+2 vs 3 — pages 1+2 chip consumed WEL (`sr1=0x00`), page 3 chip refused WEL consumption (`sr1=0x02`). Real successful PPs should be consistent — the inconsistency suggests pages 1+2 may not have *actually* programmed flash; WEL might have been consumed by some other chip operation. **This build:** add readback verification for first 2 pages. After each successful `lp_dac_pp` call, read 8 bytes from the just-written address via SDK `hal_flash_read` (XIP path is unaffected by the INDIRECT_WRITE wedge — XIP reads have always worked in our runtime) and compare against the source. New log lines: `[DAC] page #N src=NN NN ...` / `rdb=NN NN ...` / `VERIFIED — bytes match source` or `MISMATCH — WEL was consumed but bytes did not land!`. **Outcomes:** (a) **VERIFIED for pages 1+2** → DAC mode genuinely writes; page 3 failure is a chip-side rate-limit or per-burst limit. Need to investigate (delay between PPs? CFR3V config bit?). (b) **MISMATCH for pages 1+2** → DAC mode WEL clear is illusory; the chip never actually programs flash. Pages 1-2 success was a false positive; bug is below all software paths. File TI E2E ticket. |
| 132 | 2026-05-13 | **🎉 OTA Flash_write BREAKTHROUGH — DAC mode works.** 0.A.131 bench evidence: `[DAC] step7 done WEL cleared — PP COMPLETED via DAC` for pages #1 and #2. **Same chip + same controller + same runtime context where SDK `Flash_norOspiWrite` and bare-metal `INDIRECT_WRITE_XFER` fail, DAC mode (memcpy to `0x60000000 + flash_addr`) writes successfully.** The bug is conclusively isolated to the OSPI controller's `INDIRECT_WRITE_XFER` SRAM-FIFO subsystem; the DAC transfer path is fully functional in our runtime. Also confirmed: `INDIRECT_WR_CTRL pre-erase = 0x00000000` — the CANCEL guard was a no-op, not the fix. **Open issue:** page #3 hangs at 100% CPU after only 2 successful pages — async CPSW background task (`test_enet.c:391` link-status check) asserts ~5 s after `EnetApp_driverClose` and eventually wedges the OTA task. At 8 verbose log lines × 35 ms UART per page, all 128 pages would take 4.5+ s of pure UART output, racing the CPSW crash window. **Fix in this build:** reduce log output. `lp_dac_pp` prints full `[DAC] step1..step7` lines only for first 2 pages (verbose proof); subsequent pages print nothing on success, only `[DAC] FAIL ...` on error. Outer loop prints `[DAC] page #N ok @0xNNNNNN` for pages #1, #2, then every 32nd page. Net per-page UART drops from ~400 bytes to ~0 in steady state. **Outcome target:** complete all 128 pages (32 KB → 0x900000) before CPSW callbacks crash us. Expect `[OTA] activate SUCCESS via DAC-MODE PP: 32768 B flashed to 0x900000 (128 pages)`. If we cross the finish line, the bench POC is complete and we can roll DAC mode into a proper `hal_flash_write_dac()` for the runtime path. |
| 131 | 2026-05-13 | **OTA Flash_write Experiment E-guard — INDIRECT_WRITE_XFER CANCEL before erase.** 0.A.130 bench result (Experiment E DAC mode): same code as 0.A.129 except activate handler calls `lp_dac_pp` instead of `lp_bare_pp`. **Erase loop hangs at 100 % CPU forever, never reaches `[DAC] step1`.** Same behavior reproducible across cold power-cycles. Identical erase code worked in 0.A.129. **Hypothesis:** 0.A.129's failed bare-metal PP left `INDIRECT_WRITE_XFER_CTRL_REG` with `WR_STATUS=1` (bit 2, transfer-in-progress stuck high) and possibly `IND_OPS_DONE_STATUS=1` (bit 5) as a stale latch. The SDK Flash driver never cancels stuck transfer state. Warm-reset of the SoC clears CPU/scheduler state but the OSPI controller's CTRL_REG may retain bits across the reset (only a true PM reset of FSS0_OSPI_0 would fully clear it, and we proved that path is harmful). Subsequent `Flash_eraseBlk` calls then hang because the controller thinks it's still mid-INDIRECT-transfer. **Fix in this build:** before the erase loop, log current `INDIRECT_WRITE_XFER_CTRL_REG` value, then write `CANCEL_FLD (bit 1) | IND_OPS_DONE_STATUS (bit 5, W1C)` = `0x22` to force-abort any stuck transfer + clear stale done status. Log post-write value. New UART lines: `[OTA] INDIRECT_WR_CTRL pre-erase = 0xNNNNNNNN` / `post-CANCEL = 0xNNNNNNNN`. **Outcomes:** (a) pre-erase shows `WR_STATUS=1` (`0x04` or `0x60`) → confirms hypothesis. Post-CANCEL clears it. Erase proceeds. DAC PP runs. Bench validates either DAC works (ship) or DAC fails too (final TI E2E ticket data). (b) pre-erase shows `0x00` → not stuck state; regression is elsewhere; revert to 0.A.129 baseline for clean diagnostic. |
| 130 | 2026-05-13 | **OTA Flash_write Experiment E — DAC-mode Page Program (different transfer mechanism).** 0.A.129 bench evidence pinpointed the wedge with surgical precision: `[BareE] step5 done done_wait=500000 ctrl_post=0x00000004` — the bare-metal INDIRECT_WRITE_XFER pushes all 64 words (256 B) into the SRAM FIFO successfully, controller accepts START, but then stalls forever in `WR_STATUS=1` (bit 2 set, "transfer in progress" stuck high) and never asserts DONE_STATUS. Meanwhile STIG path (FLASH_CMD_CTRL_REG) works perfectly — WREN sets WEL, RDSR returns clean 0x02. **Conclusion: the bulk-transfer SRAM/INDIRECT subsystem of the OSPI controller is broken in our runtime context; the command subsystem (STIG) is fine; the chip is fine.** This build tests the controller's OTHER write path — DAC (Direct Access Controller) mode, where CPU `memcpy` to `0x60000000 + flash_addr` lets the controller intercept AHB writes and turn them into PP commands at the chip via `DEV_INSTR_WR_CONFIG`, with **no SRAM FIFO involved**. Added `lp_dac_pp(addr, src, len, *sr1_after)` mirroring the SDK's `OSPI_lld_writeDirect` pattern (which the SDK Flash driver never actually uses for writes — only for reads / XIP). ACTIVATE handler now calls `lp_dac_pp` instead of `lp_bare_pp`. New `[DAC] step1..step7` checkpoint logs. **Outcomes:** (a) **DAC works** → SRAM/INDIRECT subsystem alone is broken; we have a shippable workaround AND a precise repro for TI. Ship a wrapper that uses DAC for runtime writes. (b) **DAC fails same way** (WEL stuck after PP, or memcpy bus-faults) → wedge is below ALL software-reachable OSPI controller paths; file TI E2E ticket with rock-solid evidence; customer OTA = bridge-side flash. Brick-safe: Bank B `0x900000` scratch only. |
| 129 | 2026-05-12 | **OTA Flash_write Experiment D-cleanup — drop `IRQ_STATUS = 0xFFFFFFFF` write, add granular checkpoint logging.** 0.A.128 bench result: `wrcmp=0x000340FF` PRE-write already at safe value (so SDK is correctly configuring `WRITE_COMPLETION_CTRL_REG` — "IP limitation" comment was a red herring for our symptom), `irq_stat=0x00000000`, `irq_mask=0x00000000`, `sram_cfg=0x0000003F` (all SDK-default). But: `lp_bare_pp` then hangs at 100% CPU indefinitely after `[BareD] POST`, never returns, never logs any `[BareC] page #N rc=...` line. Hypothesis for regression vs 0.A.127: my `lp_wr32(LP_OSPI_IRQ_STATUS, 0xFFFFFFFFu)` wrote 1 to all 32 bits including reserved bits — could have triggered a controller-internal wedge that 0.A.127 didn't hit. **This build:** (1) drops the `IRQ_STATUS` force-write entirely (diagnostic dump is enough; force-write was a no-op anyway since both regs were already safe). (2) Adds checkpoint logs at every step boundary inside `lp_bare_pp`: `[BareE] step1 enter/done`, `step2 enter/done` (WREN), `step2b enter/done` (RDSR verify WEL), `step3 enter`, `step3 kick START`, `step4 enter` (FIFO drain), `step5 enter/done` (DONE poll). Also adds an outer-iter runaway guard in the FIFO drain loop (returns `-2` after >1000 iters with diagnostic state). **Goal:** localize exactly which step hangs. The last `[BareE]` line we see in COM5 tells us where to look. |
| 128 | 2026-05-12 | **OTA Flash_write Experiment D — WRITE_COMPLETION_CTRL_REG forced-safe + IRQ_STATUS clear.** User-pasted SDK 2024 source for `ospi_v0_lld.c::OSPI_programInstance` exposed a load-bearing TI comment: *"Set and invalid opcode in the WRITE_COMPLETION_CTRL_REG due to IP limitation"* — TI documents that the OSPI controller has a silicon-level quirk requiring auto-polling to be disabled (`DISABLE_POLLING=1`) and the auto-poll opcode field set to invalid (`0xFF`). If those bits aren't set, the controller will auto-poll the chip with a wrong command after every PP, decide "done" prematurely, and return `DONE_STATUS=1` — **exactly our `ctrl_post=0x60` symptom from 0.A.118-0.A.124**. SDK 12.00 has this same code in `OSPI_programInstance`, so `Drivers_ospiOpen` should be setting it correctly — but maybe runtime state is drifting. **This build:** at the start of every `lp_bare_pp` call, (1) dump the current values of `WRITE_COMPLETION_CTRL_REG (0xFC40038)`, `IRQ_STATUS_REG (0xFC40040)`, `IRQ_MASK_REG (0xFC40044)`, `SRAM_PARTITION_CFG_REG (0xFC40018)`, (2) force `WRITE_COMPLETION_CTRL_REG` to `0x000340FF` (DISABLE_POLLING=1, OPCODE=0xFF, POLL_COUNT=3), (3) W1C all of `IRQ_STATUS_REG`, (4) re-read both and log POST values, (5) THEN run the bare-metal PP. **Outcomes:** (a) PRE wrcmp shows DISABLE_POLLING=0 or non-0xFF opcode → SBL leaves bad state and our `Drivers_ospiOpen` doesn't fix it; this build's force-write IS the fix; bare-metal PP should succeed. (b) PRE wrcmp already shows `0x000340FF` (safe) → register state isn't the issue; the wedge is somewhere else; the SDK 2024 comment was a red herring for our specific symptom. (c) PRE wrcmp shows bad state AND bare-metal PP still fails after the force-write → state matters but more state needs fixing (IRQ_MASK_REG, SRAM_PARTITION_CFG_REG, or something undiscovered). |
| 127 | 2026-05-12 | **OTA Flash_write Experiment C — bypass SDK Flash driver entirely; bare-metal Page Program via direct OSPI register pokes.** 0.A.124-0.A.126 conclusively ruled out: CPSW state, all syscfg driver software state, OSPI driver software state, Flash driver state, OSPI peripheral hardware reset (made things strictly worse — erase wedged silently). Web search surfaced [TI E2E thread #984456](https://e2e.ti.com/support/processors-group/processors/f/processors-forum/984456) describing the exact symptom: "OSPI_OPER_MODE_IND_XFER: no error during the write process but the flash state remains in Write in progress" — same wedge pattern on AM64x R5. **This build** adds `lp_bare_pp(addr, src, len, …)` in `lp_ota_task.c` — a self-contained bare-metal Page Program that bypasses `Flash_norOspiWrite` / `OSPI_lld_writeIndirect` entirely. Direct register pokes mirror the SDK sequence: STIG WREN (same path our working `ospi_nv_recover.js` uses), disable DAC, set `INDIRECT_WRITE_XFER_START_REG`/`NUM_BYTES_REG`/`WATERMARK_REG`, kick START bit, poll `SRAM_FILL_REG` and push 32-bit words to `0x60000000` (OSPI DATA FIFO = `CSL_FSS0_DAT_REG1_BASE`), poll `IND_OPS_DONE_STATUS`, poll chip WIP via RDSR, verify WEL cleared. ACTIVATE handler now calls `lp_bare_pp` 256 bytes at a time instead of `hal_flash_write`. **Outcomes:** (a) bare-metal PP succeeds → SDK's `Flash_norOspiWrite` is the bug; ship the bare-metal path and report SDK bug to TI. (b) bare-metal PP fails identically (rc=-4 WEL stuck, or rc=-2 DONE timeout) → wedge is below SDK level (OSPI silicon or FSS bus arbiter); customer path is bridge-side flash and we file a targeted TI E2E ticket with the bare-metal repro. Brick-safe: Bank B 0x900000 scratch only; Bank A untouched. PM-reset code from 0.A.125/0.A.126 reverted (was strictly harmful). |
| 126 | 2026-05-12 | **OTA Flash_write Experiment B-fix — add `Sciclient_pmSetModuleState(SW_STATE_ON)` after the FSS0_OSPI_0 reset to bring the clock tree back.** 0.A.125 bench result: PM reset deassert returned success (`rs1=0 rs2=0`), driver re-open succeeded (`ospi=0x701CF0A0 flash=0x701CEF30 flash_rc=0`), but the very first `hal_flash_erase_sector` call wedged silently — no `[Flash] busyTimeout clamp`, no `[Flash] Write ENTER`, 33 % CPU spin for ~30 s then async Enet assertion + safemode. The peripheral came back register-readable but not actually functional. **Root cause hypothesis:** `Sciclient_pmSetModuleRst` toggles the reset line through DMSC, but does NOT re-assert the device's SW state — after deassert the OSPI peripheral can sit clock-gated even though its registers respond. `OSPI_open` writes register init but never reaches the actual flash bus. **Fix in this build:** after the reset-deassert, call `Sciclient_pmSetModuleState(75U, TISCI_MSG_VALUE_DEVICE_SW_STATE_ON=2U, TISCI_MSG_FLAG_AOP=2U, …)` to formally re-request the device on with ack-on-processing — DMSC re-enables the module clock and we get a real return code (`state_on=N`) confirming it landed. **Two outcomes:** (a) `[Flash] BARE attempt1 sdk=0` → hardware peripheral state was the missing piece; ship the PM-reset+state-on sequence as the OTA fix. (b) `sdk=-1 ctrl_post=0x60` again → peripheral state isn't the cause; even cycling the OSPI block doesn't help. Pivot to Experiment C (bypass SDK Flash_write via direct STIG register pokes, adapted from working `ospi_nv_recover.js`). |
| 125 | 2026-05-12 | **OTA Flash_write Experiment B — full driver teardown + FSS0_OSPI_0 hardware PM reset + selective re-open.** 0.A.124 bench result (Experiment A): `[Flash] BARE attempt1 sdk=-1 sr1=0x02 ctrl_pre=0x00 ctrl_post=0x60 cfr2v=0xCF` — identical signature, complete teardown of Board+Drivers (Flash/EEPROM/I2C/OSPI; UART kept) and selective re-open of OSPI+Flash made zero difference. The wedge persists across full driver re-init. Driver software state is definitively NOT the cause. **This build:** between `Drivers_ospiClose` and `Drivers_ospiOpen`, hardware-reset the OSPI peripheral itself via `Sciclient_pmSetModuleRst(TISCI_DEV_FSS0_OSPI_0=75U, 1U, …)` then deassert. This cycles the OSPI peripheral's reset signal at the SoC PM level — closer to what the auto-flasher gets when JTAG cold-loads it. We tried this alone in 0.A.104 (without driver teardown) and it didn't help; combining it with Experiment A's full teardown has never been tested. Brick-safe: resets the OSPI peripheral hardware, not the chip. **Possible outcomes:** (a) `BARE attempt1 sdk=0` → SBL's XIP setup left the OSPI peripheral in a state our driver close+open couldn't undo, but PM reset does; ship this fix. (b) `sdk=-1` same signature → the wedge isn't in the OSPI peripheral either; pivot to Experiment C (bypass SDK Flash driver, direct register pokes for one PP — adapts the working `ospi_nv_recover.js` STIG sequence). |
| 124 | 2026-05-12 | **OTA Flash_write Experiment A — full driver teardown + selective OSPI/Flash re-open.** 0.A.123 confirmed CPSW-close + `Sciclient_pmSetModuleRst(CPSW0)` alone wasn't enough — `Flash_write` still failed with the same `ctrl_post=0x60` signature. Hypothesis to test next: **some non-CPSW syscfg driver** (I2C, EEPROM, OSPI's own boot-time PHY-tune state, or the way `Board_flashOpen` configures Flash on top of OSPI) leaves the controller in a state our runtime can never escape from, while the auto-flasher's far smaller NoRTOS init footprint never enters that state. **This build:** in `lp_ota_task.c`'s ACTIVATE handler, after `EnetApp_driverClose(ENET_CPSW_3G, 0)`, tear down everything we can: `Board_flashClose()` → `Board_eepromClose()` → `Drivers_i2cClose()` → `Drivers_ospiClose()`. UART is intentionally left open because `CONFIG_UART_CONSOLE=1` is what `DebugP_log` writes through — closing it would blind the diagnostic. Then re-open ONLY OSPI + Flash via `Drivers_ospiOpen()` + `Board_flashOpen()` and resume the bench POC: erase Bank B `0x900000`, drain the 32 KB MSRAM buffer through `hal_flash_write` in 1 KB chunks, read back the first 8 bytes. **Possible outcomes:** (a) write succeeds → some non-CPSW driver in our syscfg is the culprit; next step is Experiment B to bisect which one. (b) write still fails same way → Drivers_close approach is no better than the prior CPSW-only attempts; pivot to Experiment C (Option 2: custom SBL chooser + dedicated flasher image). Brick-safe: writes to non-bootable Bank B; Bank A untouched; if re-open fails the LP halts with explicit log + `while(1)` so a JTAG operator can recover via `Flash-LP.ps1`. |
| 123 | 2026-05-12 | **OTA runtime Flash_write investigation CLOSED — pivoted customer OTA to bridge-side flash.** After 4 days (0.A.78–0.A.122) and direct bench measurement ruling out every plausible software-level cause (CPU IRQs via `HwiP_disable`, task switches via `vTaskSuspendAll`, CPSW TX UDMA HW pause via `Udma_chPause(tx->hUdmaCh)`, CPSW RX UDMA HW pause via `Udma_chPause(&drv->rxChObj[0].udmaChObj)` with `rc=0 numRxCh=1`, stale `DONE_STATUS`, chip-controller protocol mismatch confirmed consistent via `CFR2V=0xCF` + `DEV_INSTR_WR=0x22002`, address protection refuted by auto-flasher writing 64KB to `0x900000` successfully), the runtime `Flash_write` failure remains unreproduced from the NoRTOS auto-flasher and unfixable from software in our FreeRTOS runtime context. Root cause not isolable without logic-analyzer scope on the OSPI bus. **Decisions in this build:** (1) Customer OTA architecture pivoted to bridge-side flash via the auto-flasher (already verified working at every offset including OTA Bank B `0x900000`), documented in [`memories/repo/lp-am2434-runtime-flashwrite-unresolved.md`](../memories/repo/lp-am2434-runtime-flashwrite-unresolved.md) + [`docs/LP-AM2434-OTA-Update-Plan.md`](LP-AM2434-OTA-Update-Plan.md) header. (2) Removed `lp_enet_dma_pause()` / `_resume()` calls from `hal_flash_write`'s hot path — they work as intended but don't fix the underlying bug, so they're just runtime cost. Helper file [`Nova_Firmware/lp_am2434/lp_enet_dma_pause.c`](../Nova_Firmware/lp_am2434/lp_enet_dma_pause.c) stays as reference. (3) Kept the layered `HwiP_disable + vTaskSuspendAll` wrap around the SDK `Flash_write` call — these are cheap and brick-safe; if a future SDK update or chip-firmware fix makes runtime writes reliable, the path is ready. (4) Phase 1A LP `:5503` listener stays (version reporting). NovaFwUpdate state machine stays (the chunk-receive path works; the chunk-write path returns -1 cleanly without bricking). **Verification:** flash 0.A.123; LP boots STORAGE; chip stays intact; OTA test continues to fail at chunk@0 with `firmwareError code=26` (expected, brick-safe). Next OTA work: bridge-side `flash-lp.sh` hardening — programmatic boot-mode, programmatic power-cycle, signature verification, progress reporting. |
| 122 | 2026-05-12 | **OTA Flash_write: pause CPSW DMA event delivery around the `Flash_write` call.** 0.A.121 bench confirmed `vTaskSuspendAll + HwiP_disable` together still fails the write the same way (`ctrl_post=0x60`, chip never programs). Auto-flasher in NoRTOS context (no Enet/CPSW init at all) writes the same offset successfully. Remaining suspect is CPSW UDMA autonomous activity. **Plumbing added in this build:** (1) `Nova_Firmware/lp_am2434/patch_enet_accessor.py` patches the syscfg-generated `ti_enet_open_close.c` to expose `EnetApp_lookupTxCh` / `EnetApp_lookupRxFlow` accessors against the static `gEnetAppSysCfgObj` — idempotent, runs after every regen via the makefile. (2) `Nova_Firmware/lp_am2434/lp_enet_dma_pause.c` provides `lp_enet_dma_pause()` / `lp_enet_dma_resume()` that loop over open TX/RX channels and call `EnetDma_disableTxEvent` / `disableRxEvent` (and inverse). Brick-safe — touches CPSW event subsystem only, no chip writes. (3) `Platform/hal_flash.c::hal_flash_write` wraps the SDK `Flash_write` call as: `lp_enet_dma_pause() → HwiP_disable → vTaskSuspendAll → Flash_write → xTaskResumeAll → HwiP_restore → lp_enet_dma_resume`. Outermost layer pauses autonomous DMA; middle layer masks IRQs; inner layer suspends scheduler. **Verification:** flash 0.A.122, ping, OTA test, capture COM5 — `[Flash] BARE attempt1 sdk=0` means the fix landed and we ship. If `sdk=-1` still, plumbing is good but pause-event isn't enough — would need full `Udma_chDisable` (heavier; halts UDMA channel hardware, not just events). |
| 121 | 2026-05-12 | **OTA Flash_write: try HwiP_disable INSIDE vTaskSuspendAll for strictest possible isolation.** 0.A.120 bench confirmed the stale-DONE clear lands (`ctrl_pre=0x00` instead of 0x60) but `Flash_write` still fails the same way and `ctrl_post=0x60` again — controller eventually asserts DONE late, chip still doesn't program (WEL stays set, page stays FF). Auto-flasher at 0x900000 in NoRTOS context works fine (Experiment 1, 2026-05-12). Difference between auto-flasher's working init and our runtime is: auto-flasher's syscfg has no Enet/CPSW/lwIP — only OSPI. Hypothesis: ISR servicing during runtime (CPSW RX/TX, system tick, watchdog client) interferes with the OSPI controller's INDIRECT_WRITE_XFER state machine, either via AHB arbitration contention or by stealing CPU cycles during the SDK's polled FIFO fill. 0.A.87 tried `HwiP_disable` ALONE pre-0.A.111 (before `vTaskSuspendAll` was added) and the bug looked the same, but back then the brick-risky NV recovery code path was also obscuring symptoms. Worth a clean retry. **This build:** wrap `Flash_write` call with `HwiP_disable` INSIDE `vTaskSuspendAll` — masks both task switches and IRQs. Brick-safe (no chip writes; SDK Flash_write uses spin-loops not `ClockP_usleep` so masking IRQs doesn't break SDK timing). **Two outcomes:** (a) `sdk=0` → CPU IRQ servicing was the disruption; ship this fix as-is. (b) `sdk=-1` still → CPSW DMA is running autonomously without CPU IRQs; need a heavier fix to actually pause DMA. |
| 120 | 2026-05-12 | **OTA Flash_write: clear stale `INDIRECT_WRITE_XFER_CTRL_REG` bits before each PP.** 0.A.119 bench traces confirmed chip + controller protocol are consistent (chip `CFR2V[6]=QPI` + `CFR2V[7]=4-byte-addr` matches controller `DEV_INSTR_WR` 4S-SDR + `obj.numAddrBytes=4`; Cypress S25HL512T's PP cmd 0x02 is supposed to work in QPI-SDR so the chip-controller mismatch theory was wrong). Also confirmed via Experiment 1 (`uniflash_run.js` with `UNIFLASH_OFFSET=0x900000`) that the **auto-flasher CAN write to 0x900000 successfully in NoRTOS context** — so the address is fine and the bug is specific to the runtime FreeRTOS context. **Key clue from 0.A.119:** `ctrl_pre = 0x60` going INTO every PP attempt — controller's `INDIRECT_WRITE_XFER_CTRL_REG` carries stale `DONE_STATUS=1` + `NUM_IND_OPS_DONE=1` from prior failed transfers. The SDK's `OSPI_lld_writeIndirect` does NOT clear those on entry, and may not handle stale state correctly. **Fix:** before each `Flash_write` in `hal_flash_write`, write 1 to `INDIRECT_WRITE_XFER_CTRL_REG` bits 5 (`IND_OPS_DONE_STATUS`) and 1 (`CANCEL`) — both write-1-to-clear, both leave other fields untouched. Surgical, brick-safe (controller-only, no chip access). **Diagnostic:** keeps 0.A.119's CTRL_MODE dump so we can see the BEFORE/AFTER state of the clear (`ctrl_pre=0x60` should change to `ctrl_pre=0x00` after the clear lands). **Verification:** flash 0.A.120, ping, run OTA test (`tsx src/test_ota_push.ts ... 10.47.27.2 256 --no-reboot`), capture COM5: expect `[Flash] BARE attempt1 sdk=0` (success!) — if so, OTA fully works. If `sdk=-1` still, the stale state isn't the root cause and we look at runtime peripheral interference (CPSW DMA, etc.). |
| 119 | 2026-05-12 | **OTA Flash_write protocol-mismatch diagnostic (read-only build).** 0.A.118 bench evidence (`memories/repo/ota-flash-write-still-fails-0a113.md`) showed `ctrl_post=0x60` after a single PP fail: controller signals DONE_STATUS=1 + NUM_IND_OPS_DONE=1 + no error, but chip's WEL stays set and the target page reads 0xFF. Translation: controller completed clocking 256 bytes out the pins, chip silently rejected the PP. Hypothesis (from SDK source audit): the AM2434 OSPI controller hardware has DDR_EN in `DEV_INSTR_RD_CONFIG_REG` (writes use polling-only SDR — no DDR bit) — so when Flash_open puts the chip into 4S-4D-4D DTR mode for performance reads, every PP we send is clocked out SDR while the chip expects DTR → garbage → silent reject. This build adds a SECOND log line right after `[Flash] BARE attempt1` printing the OSPI controller's `CONFIG_REG`, `DEV_INSTR_RD_CONFIG_REG`, `DEV_INSTR_WR_CONFIG_REG` registers + the chip's volatile config registers `CFR2V`/`CFR3V`/`CFR4V` (read via RDAR cmd 0x65) + the SDK's `obj->numAddrBytes` + `obj->currentProtocol`. Decode keys baked into the source comments. All reads, zero brick risk. If `rd_cfg` shows DDR_EN=1 (bit 10) AND `wr_cfg` shows no equivalent AND `CFR2V[7]=1` (chip DTR enabled), hypothesis confirmed and the 0.A.120 fix is to either disable chip-side DTR via volatile-only CFR2V write before each PP (no NV writes, no brick risk) OR switch Flash_open's protocol config to a non-DTR variant (lose read perf, gain write correctness). **Verification:** flash 0.A.119, ping, run OTA test, capture COM5 — paste `[Flash] BARE attempt1 ...` AND the new `[Flash] CTRL_MODE ...` line. |
| 118 | 2026-05-11 | **OTA Flash_write retry path: strip all chip-NV / OSPI-peripheral-PM-reset / Flash_close+open recovery — back to a brick-safe minimum.** Today the dev CONTROLLER (S24L0707) got bricked when 0.A.115's `hal_flash_try_nv_recovery` fired during a failed OTA Flash_write retry and rewrote the chip's NV config registers (`CFR3N=0x00` typo). Several hours of bench time were lost trying to recover via JTAG-direct OSPI STIG pokes. Root cause is upstream of the typo: **the runtime firmware should never write to the OSPI chip's NV or volatile config registers during a Flash_write retry.** A failed Flash_write on a fielded panel should abort the OTA and leave Bank A running — the customer is no worse off. Chip-level recovery (WRRSB / WRARG / SafeBoot) is the auto-flasher's job (NoRTOS, JTAG-supervised). Deleted from `Platform/hal_flash.c`: `hal_flash_try_nv_recovery`, `hal_flash_force_chip_dtr`, the `Sciclient_pmSetModuleRst` (FSS0_OSPI_0) peripheral-reset path, the `Flash_close+Flash_open` retry dance in both `hal_flash_write` and `hal_flash_erase_sector`, and `hal_flash_clear_errors` (CLPEF, now dead). What's kept: the `vTaskSuspendAll` wrap (0.A.111 — the real fix for post-scheduler `Flash_write` preemption), the 0.A.116 OSPI controller register snapshot diagnostic (read-only, can't brick anything), three plain retries with 100 ms gaps, post-fail verify read-back. **Net behavioral change:** Flash_write/erase that fails three times now returns -1 cleanly to the OTA caller — no chip mutation, no brick risk. **Verification:** flash 0.A.118 on a healthy LP, run `tsx src/test_ota_push.ts ... 10.47.27.X 1024 --no-reboot`, watch UART for the `[Flash] BARE attempt1 ...` line. If OTA succeeds, that's a green light. If it still fails, the failure is now safely contained — chip is unchanged, retry / abort is up to the bridge. Build clean (no SDK warnings re: unused symbols since `-Wno-unused-function` is set). |
| 117 | 2026-05-11 | **OTA Flash_write diagnostic build + CFR3N brick-bug fix (DIAGNOSTIC).** Bundles two changes on top of 0.A.116: (1) Keeps 0.A.116's bare-attempt-1 + OSPI register snapshot diagnostic (stripped recovery envelope on attempt 1, full INDIRECT_WRITE_XFER_CTRL_REG/SRAM_FILL_REG/NUM_BYTES_REG dump before+after the Flash_write call) — still need bench data to localize the OTA failure mode. (2) **Fixes the brick-bug in `hal_flash_try_nv_recovery` (`Platform/hal_flash.c:240`)** that took out the dev CONTROLLER on 2026-05-11. The recovery WRRSB was writing 5 bytes `00 00 08 00 08` to STR1N/CFR1N/CFR2N/CFR3N/CFR4N, but `CFR3N=0x00` makes the chip require non-zero command dummy cycles on every command — and the SDK never sends any, so subsequent `Flash_open` calls hang forever in `Flash_norOspiWaitReady`. The SDK's own `Flash_quirkQSPIEarlyFixup` at `flash_nor_ospi.c:1716-1730` documents the correct factory-default value: `CFR3N=0x08` (comment: "Restore CFR3N from 0xC0 to 0x08 — This restores the cmd dummy cycles to 0"). Changed to `00 00 08 08 08`. Recovery procedure documented in [`memories/repo/lp-am2434-ospi-nv-recovery.md`](../memories/repo/lp-am2434-ospi-nv-recovery.md) — SW4 NOBOOT mode (1+2+3+4 ON) + new `ospi_flash/ospi_nv_recover.js` pokes OSPI STIG registers via JTAG DAP to issue WRRSB with correct values. Generalized lesson + cross-references captured. **Verification:** flash 0.A.117 via Flash-LP.ps1, run OTA test, capture `[Flash] BARE attempt1 ...` register snapshot line — that decides 0.A.118. |
| 116 | 2026-05-11 | **OTA Flash_write attempt-1 stripped + OSPI controller register snapshot (DIAGNOSTIC build).** 0.A.114/115 left the recovery envelope wrapping every Flash_write call — CLPEF, SR1-pre read, and (after the first fail) `hal_flash_try_nv_recovery` + Sciclient PM-reset of FSS0_OSPI_0. 0.A.115 bench evidence shows that envelope mutates chip-side state (CFR2N writes via WRRSB) that may itself be causing the silent-PP-reject. This build peels **attempt 1** down to the bare minimum — just `vTaskSuspendAll → Flash_write → xTaskResumeAll` — and snapshots the OSPI controller's `INDIRECT_WRITE_XFER_CTRL_REG (0x70)`, `SRAM_FILL_REG (0x2C)`, and `INDIRECT_WRITE_XFER_NUM_BYTES_REG (0x7C)` just before+after the call. Bit layout of `ctrl_post` tells us which failure mode we're in: `[0]START=1, [5]DONE=0` → controller wedged mid-transfer (waitIndWriteComplete timeout); `[5]DONE=1, [2]WR_STATUS=1` → controller reported error; `[5]DONE=1, [2]WR_STATUS=0` → controller thinks it succeeded but chip didn't program (protocol mismatch). The retry envelope (CLPEF + SR1 + NV recovery + PM-reset + Flash_close/open) still fires on attempts 2-3 as a fallback. **Two possible outcomes:** (a) stripped attempt 1 succeeds → our recovery code was breaking the chip; pull it out in 0.A.117. (b) stripped attempt 1 still fails → ctrl_post register pattern points us at the exact controller failure mode, no more guessing. **Verification recipe:** same as 0.A.114/115 OTA test (`tsx src/test_ota_push.ts ... 10.47.27.1 1024 --no-reboot`). New log line: `[Flash] BARE attempt1 sdk=N sr1=0xNN ctrl_pre=0xNNNNNNNN ctrl_post=0xNNNNNNNN sram_pre=0xNNNNNNNN sram_post=0xNNNNNNNN num_bytes=0xNNNNNNNN`. |
| 115 | 2026-05-11 | **OTA Flash_write fix attempt #2: PARTIALLY REVERTED.** Added `hal_flash_force_chip_dtr()` (volatile `CFR2V=0x82` write via 1S commands) and wired it before every Flash_write attempt; also disabled the harmful 0.A.107 `hal_flash_try_nv_recovery` whose `CFR2N=0x08` was permanently putting the chip into 1S NV mode. **Bench result:** force-DTR is the right idea but the implementation leaves the OSPI controller's `cmdDummyCycles` and other state misaligned when switching protocols, so `Flash_open`'s internal quirk function (`Flash_quirkQSPIEarlyFixup:1764`) fails on the next call (`Failed to complete flash recovery`). The function definition stays (referenced as `(void)` to keep symbol) but the call sites are reverted; nv_recovery re-enabled with a warning comment. Net: 0.A.115 = 0.A.114 behavior. The smoking-gun trace from this build (`cfr2v_pre=0xFF` consistently = 1S RDAR read returns garbage with dummyBits=64; chip mode-detection needs more work) is captured in `memories/repo/ota-flash-write-still-fails-0a113.md` for the next iteration. |
| 114 | 2026-05-11 | **OTA Flash_write diagnostic instrumentation (DEBUG build).** 0.A.113 bench verification (`memories/repo/ota-flash-write-still-fails-0a113.md`) showed the 0.A.112 vTaskSuspendAll wrap is in place but OTA `Flash_write` to Bank B 0x900000 still fails silently — `post-fail verify FF`, no PRGERR / ERSERR. To localize the failure mode, added a per-attempt log line in `Nova_Firmware/Platform/hal_flash.c::hal_flash_write` immediately after the wrapped `Flash_write` call that prints both the SDK return status (`sdk_status=N`) and the chip's `SR1` register (`sr1_after=0xXX`). Watching `sr1_after` evolve across attempt 1 → 2 → 3 will tell us whether the chip is stuck in WIP, in P_ERR, or in some other state the recovery path can't clear. This is a diagnostic-only build; no behavioral change. **Verification recipe:** same as 0.A.113 OTA test (`tsx src/test_ota_push.ts ... 10.47.27.1 1024 --no-reboot`). Watch firmware UART for the new `[Flash] Write call result attempt=N sdk_status=... sr1_after=0xNN` line per attempt. |
|---:|---|---|
| 113 | 2026-05-09 | **Burner setpoint mirror: stop aliasing `Burner.TempSet` to `Plenum.TempSet` (clobbered onion-mode operator entries).** AS2 archaeology confirmed `Settings.Plenum.TempSet` and `Settings.Burner.TempSet` are fully independent settings in every mode — factory defaults 46° vs 75°, separate POST handlers (`StorePostData.c:2035-2056`), separate downstream readers (cure burner PID at `Controls.c:601` reads `Burner.TempSet` exclusively; `Plenum.TempSet` drives cavity heat / climacell / fan ref / CO2 purge band / ramp / outside-air control even in onion mode). The Level 1 plentemp UI already routes the operator's onion-mode "Plenum Setpoint" entry into proto field 4 (`constellation-ui/src/routes/level1/plentemp/+page.svelte` `plensetup[3]` binding when `onionMode`) — but `mirror_burner` was unconditionally aliasing `Settings.Burner.TempSet = lp->plenum.temp_setpoint` (proto field 1), clobbering the UI's onion-mode write to proto field 4 every tick. Net effect: in onion-cure mode, the cure burner PID was running against `Plenum.TempSet` (the field the UI never wrote in onion mode) — operator's saved cure setpoint was silently ignored. Fix: `mirror_burner` now mirrors `Settings.Burner.TempSet = lp->plenum.burner_temp_setpoint` (proto field 4) unconditionally, matching AS2's "two independent variables" semantics. `mirror_basic_setpoints` continues to mirror proto field 1 → `Plenum.TempSet` for the non-cure consumers. Build clean. **Verification:** in onion mode, edit "Plenum Setpoint" on Level 1 plentemp page, save, observe cure burner PID actually targets the saved value (vs. the dormant Plenum.TempSet field which non-onion paths use). |
| 112 | 2026-05-09 | **OTA Flash_write post-scheduler wedge — root cause and fix.** Yesterday's session 0.A.78–0.A.108 chased every chip/SDK/peripheral angle for `Flash_write` failing post-scheduler in FreeRTOS. The session-resume doc concluded "OSPI hardware bug or silicon errata, file TI E2E ticket." Audit today found the smoking gun was hiding in **a comment in our own `Platform/hal_flash.c:26-37`** written months ago by the original author, who already understood the bug *and the fix*: the TI Board Flash driver's polled FIFO-fill loop in `OSPI_lld_writeIndirect` is not preemption-safe. A FreeRTOS scheduler tick mid-fill drains the FIFO while the calling task is suspended; the chip silently aborts the Page Program (no PRGERR flag set, since it never received an explicit error condition) and the post-call read-back shows 0xFF. This precisely matches the empirical symptoms: WREN reaches chip via STIG → WEL=1 ✓, PP attempted via INDIRECT_WRITE_XFER → no data lands ✗, chip stays clean (no error flags) ✓, only cold power-cycle clears it ✓. The author had written `vTaskSuspendAll/Resume keeps the operation atomic w.r.t. other tasks while still allowing the R5F's IRQs (incl. tick) to fire so the OSPI driver's own interrupt-driven completion still works` — but the actual code never wrapped the `Flash_write` / `Flash_eraseBlk` calls. The 0.A.87 attempt at `HwiP_disable` failed because it ALSO blocked the R5F's clock-tick interrupt, breaking the OSPI driver's internal `ClockP_usleep` and adjacent retry-loop sleeps. **Fix:** introduced `hal_flash_atomic_enter()` / `hal_flash_atomic_exit()` in `Platform/hal_flash.c` that wrap with `vTaskSuspendAll()` / `xTaskResumeAll()` (no-op if scheduler isn't running, so pre-scheduler `LpDeviceConfig_Save` keeps working as before). Wrapped both the `Flash_write` call in `hal_flash_write` and the `Flash_eraseBlk` call in `hal_flash_erase_sector` (cheap insurance — same wedge class). The wrap delivers exactly what the file-header comment promised. Build: clean (0 errors); `nova_lp.release.mcelf.hs_fs` + `watchdog.release.mcelf.hs_fs` both produced. **Verification:** flash 0.A.112, exercise OTA via bridge — `Flash_write` should now succeed on the first attempt with no recovery loop. Removes the entire dependency on chip-side WRRSB SafeBoot recovery, OSPI peripheral PM-reset, and the 3-attempt retry envelope. If this fix lands cleanly, `docs/session-resume-ota-flash-write.md` graduates from "open" to "RESOLVED" and the TI E2E ticket draft can be deleted instead of sent. |
| 111 | 2026-05-09 | **Watchdog reset chain: fix the silent NO-OP that broke field-OTA recovery.** The watchdog core (`Nova_Firmware/lp_am2434/watchdog_core/main.c`) was relying on DWWD expiry to trigger a SoC warm reset when main R5 hung. Audit revealed that `Watchdog_configureWarmReset()` is a literal NO-OP stub on AM64x/AM243x — the SDK source at `C:/ti/mcu_plus_sdk_am243x_12_00_00_26/source/drivers/watchdog/v1/watchdog_rti.c:309-311` says "Reset is not supported on AM64x" and the call is commented out. The platform-specific implementation (`watchdog_soc.c:53`) is `void Watchdog_configureWarmReset(...) { return; }`. Net effect: even with `Watchdog_RESET_ON` configured, DWWD expiry sets a status flag but **asserts no reset signal**. The entire watchdog reset chain was a NO-OP. Symptom in the field: main R5 hangs (FreeRTOS panic, infinite loop, stack overflow, CPSW deadlock — anything that stops the heartbeat), watchdog core correctly detects the stall, stops strobing, DWWD expires after 10 s, and the chip just sits there hung forever. Customer panel becomes a brick that needs onsite power-cycle to recover. Specifically devastating for OTA: a bad image that boots but hangs would leave the panel unrecoverable without an operator-physical-reset, defeating the whole point of OTA-with-bank-rollback. **Fix:** in `watchdog_main` stop-strobe path, call `Sciclient_pmDeviceReset(SystemP_WAIT_FOREVER)` directly. This sends `TISCI_MSG_SYS_RESET` to DMSC; DMSC orchestrates a real SoC warm reset → ROM re-runs → SBL re-evaluates bank A/B and loads the freshly-saved (or rolled-back) image. Same path as the bridge's `POST /iot/system/reboot` (`memories/repo/lp-am2434-remote-reset-solved.md`) which is already known to work. Sciclient is already initialized in this core's `Drivers_open()` (`Sciclient_init(CSL_CORE_ID_R5FSS1_0)` per `ti_drivers_config.c:141`) so no syscfg changes needed. Header comment + the no-strobe spin-loop comment updated to document the SDK-stub gotcha. Fall-through after Sciclient (which shouldn't return) logs the rc and spins so a JTAG operator can see the failure mode instead of a wedge. Build: clean (`watchdog.release.out` Done). **Verification (pending Monday flash):** with main R5 deliberately wedged (e.g. `for(;;);` injected into `lp_engine_task`), verify SoC warm-reset within ~13 s of stall (5 s grace + 8 s DWWD expiry-equivalent + Sciclient round trip). UART trace should show `[WD] STROBE STOPPED reason=N — issuing TISCI reset` followed by the SBL boot banner from main core. Pre-fix: the chip would hang at the `STROBE STOPPED` log line and never reset. |
| 110 | 2026-05-09 | **Mirror gap audit (`memories/repo/lp-engine-shadow-mirror-rule.md` + `ui-stub-audit-2026-05-09.md`) — port 7 missing mirrors that were silently no-opping legacy features.** Triggered by the 0.A.109 ramp port — that fix exposed the broader pattern of `lp_engine_shim.c` not copying operator-saved values into the legacy `Settings.*` struct, causing `nova_states.c` / `nova_failures.c` / `nova_controls.c` to read BSS-zero defaults and silently no-op entire features. **High-impact gaps fixed:** (1) **`Settings.Refrig.Stage[i].{On,Off,Diagnostic}` loop in `mirror_refrig`** — multi-stage compressor staging was completely dead. With BSS zeros, `nova_controls.c:1799-1856` saw all stages at threshold 0 → first stage tripped at any refrig demand and higher stages never engaged. Now mirrors `lp->refrig.stages[]` (up to LP_REFRIG_MAX_STAGES=NUM_REFRIG_STAGES=8) into legacy. (2) **`Settings.Refrig.{DefrostPeriod, DefrostDuration, FailMode, Purge}` in `mirror_refrig`** — defrost scheduling never ran (`DefrostPeriod==0` is the legacy "off" sentinel at `nova_states.c:1191`), refrig sensor-failure fallback path was undefined, CO2-purge-pump-down was permanently inhibited. (3) **NEW `mirror_co2(lp)`** — entire CO2 page was dead. `Settings.Co2.Purge.Mode==0` short-circuited every CO2 path in `nova_modes.c:282/510` regardless of operator config. Mirrors `lp->co2.{mode, min_temp, max_temp, duration_minutes, fan_output, door_output}` into legacy + handles the mode-dependent `cycle_or_set` (Timer mode → `Settings.Co2.CylceTime`; Sensor mode → `Settings.Co2.Set`). Also wires `lp->failure2.co2_setpt` → `Settings.Co2.HighFailure` so the CO2 high-failure threshold is real instead of always-trip-at-1ppm. (4) **NEW `mirror_cavity_heat(lp)`** — entire cavity-heat feature was dead. `nova_controls.c:683` short-circuits `CtrlCavityHeat` when `Mode<2`; BSS zero meant cavity heat never engaged regardless of operator config. Mirrors `lp->misc.{cavity_mode, cavity_diff, cavity_duty_or_sensor, cavity_standby_on}`. (5) **`Settings.Plenum.HumidSetpointRef` in `mirror_basic_setpoints`** — onion-mode humidity-source select (0=plenum sensor, 1=return-air) read at `nova_controls.c:1248`. BSS zero meant return-air pick was silently ignored. (6) **`Settings.Plenum.{HumidLowFailure, SensorDiff}` in `mirror_basic_setpoints`** — failure thresholds from `LpFailure2.{low_humid_set, plen_sen_diff}`. BSS zero leaves them in latent-trip state, masked today only by upstream gates. **Build:** clean (0 errors, 2 SysConfig pre-existing warnings). **What's still UNCOVERED (tracked, not fixed):** `Settings.Refrig.Defrost[i].Diagnostic` (proto schema doesn't model per-defrost-stage diagnostic — needs a proto field add); `Settings.Co2.Purge.RefrigThresh` (no `LpCo2.refrig_thresh` proto field); `Settings.Burner.TempSet` continues to alias `lp->plenum.temp_setpoint` per the AS2-faithful comment in `mirror_burner` — `LpPlenum.burner_temp_setpoint` (field 4, default 75°) is plumbed through OSPI/proto but never consumed; this is a **design decision** awaiting user sign-off — switch to dedicated burner setpoint, OR remove the unused proto field. **Verification:** flash 0.A.110, exercise multi-stage refrig (stage 2/3 should engage at higher demand), CO2 purge cycle, cavity heat Mode≥2, return-air humid setpoint pick. Each was a no-op on 0.A.108-0.A.109. |
| 109 | 2026-05-09 | **Plenum-setpoint ramp activation: port the missing mirror + snap-guard.** Symptom on 0.A.108 — the legacy `SetRamp()` (Platform/nova_states.c:865-936) is compiled in and runs every control tick, but it always reads `Settings.Ramp.{RatePerDay,UpdatePeriod,TempDiff,TempRef,TargetTemp}` as BSS-zero defaults because `lp_engine_shim.c::lp_engine_tick()` had no `mirror_ramp(lp)` to copy `LpSettings.ramp_rate` (the wire/UI side) into the legacy struct. With those zeros, the activation gate `target!=setpoint && period!=0 && rate!=0` never trips, `Ramp.Status` stays `RAMP_OFF`, `CurrentMode` never becomes `UI_RAMPCOOL/UI_RAMPREFRIG`, and the SystemStatus `current_mode` field 16 never emits 10 or 11 — so the UI's "COOLING (RAMPING)" / "REFRIG (RAMPING)" labels (already wired in `+layout.svelte:83-84` modeToColor) are dead code on the customer panel. Fix #1: add `mirror_ramp(lp)` (lp_engine_shim.c) modeled on `mirror_burner`, called from `lp_engine_tick()` alongside the other mirrors. Field-name mapping is non-obvious — proto's `rate_per_day` carries AS2's `UpdateTemp` (degrees per *step*, not per day); `update_period==255` is the "automatically" sentinel; both round-trip cleanly through `(char)` cast under ARM AAPCS unsigned-char default. Fix #2: AS2 save-path snap-guard (`StorePostData.c:557`) — when the operator manually sets a new `Plenum.TempSet`, force `Settings.Ramp.TargetTemp = newTempSet` so a stale non-zero rate doesn't immediately re-arm a ramp toward the OLD target. Ported into `LpSettings_ApplyPlenum` (lp_settings.c:542) with one extra line `s_data.ramp_rate.target_temp = f` inside the `field==1` branch. No proto schema change required — the rampified mode encoding (UI_RAMPCOOL=10, UI_RAMPREFRIG=11 in States.h:114-115) was already in `SetMode()` and already emitted via SystemStatus field 16 by main.c:3053. UI mapping at `modeToColor[10]/[11]` and i18n keys `mode.cooling-ramping`/`mode.refrig-ramping` already exist. **Verification recipe:** check "Use ramp rate" on Level 1 plentemp, save with target ≠ current setpoint and rate>0; firmware should immediately broadcast `current_mode=10` (Cool Ramp) or 11 (Refrig Ramp) and the panel header should change to "COOLING (RAMPING)". Saving a fresh manual `tempSetpoint` should snap target to match and exit ramp mode. |
| 108-VALIDATED | 2026-05-09 | **0.A.108 protocol-switched WRRSB recovery validated functional on probe B (CONTROLLER 10.47.27.1, hardware S24L0707).** Bench result: WRRSB recovery code path executes cleanly — all 6 BB checkpoints print (`enter` → `DTR RSTEN+RST` → `ctrl -> 1S-1S-1S` → `WREN(1S)` → `WRRSB(1S)` → `WIP poll` → `1S RSTEN+RST done`). Critical comparison vs 0.A.107: `wip_polls=2 sr1=0x00` (vs 0.A.107's `wip_polls=200 sr1=0x55`) — chip accepted WRRSB, NV write completed in ~10 ms, status read clean. The protocol-switching fix is **correct**: switching OSPI controller to 1S-1S-1S before issuing WRRSB makes the chip respond. **However:** the OTA Flash_write still fails at chunk 0 (`attempts=3`, `verify=FF`, identical to 0.A.107), and SR1=0x00 going INTO attempt 1 means the chip was NOT in SafeBoot to begin with on probe B. Implication: the post-scheduler INDIRECT_WRITE_XFER wedge is a **separate root cause** from SafeBoot, and our 0.A.107/108 SafeBoot-recovery hypothesis was off-target for the actual OTA bug. WRRSB recovery is still useful (a chip really in SafeBoot would now recover), but it's not the OTA fix. Real fix lives elsewhere — likely in how `Flash_norOspiWrite`'s INDIRECT_WRITE_XFER state machine is set up after `Sciclient_pmSetModuleRst` of the OSPI peripheral. **Side finding:** original Probe N hardware (S24L0957) was bricked during 0.A.105 testing — chip won't boot from OSPI even after full uart_uniflash erase+reflash with TI prebuilt SBL. Likely the 0.A.105 WRARG writes (cmd 0x71 in 4S-4D-4D DTR mode) DID land in unintended NV registers and put the chip in a state ROM bootloader can't recover from. Recovery requires hardware intervention. Flash-LP.ps1 `-Probe N` now points to S24L0707 (Probe B's hardware) until the original board is recovered. WRRSB recovery on probe B's chip was harmless (clean chip, factory-default writes are idempotent). |
| 108 | 2026-05-08 | **OTA: switch OSPI controller to 1S-1S-1S BEFORE WRRSB recovery (protocol mismatch fix).** 0.A.107 bench result `wip_polls=200 sr1=0x55` revealed why WRRSB didn't clear SafeBoot: `0x55 = 0b01010101` is the DTR-bit-doubled echo of an `0x55`-pattern unresponsive bus, and `wip_polls=200` (1 s timeout never seeing WIP=0) means `RDSR` was reading garbage. Both the WRRSB write and the RDSR poll were sent in **4S-4D-4D DTR** because the OSPI controller's protocol config never changed. Chip in SafeBoot answers in 1S-1S-1S; controller talks DTR; result is bytes mismatch and chip ignores commands. The SDK's own `Flash_quirkQSPIEarlyFixup` (`flash_nor_ospi.c:1673-1689`) handles this exact protocol-switch dance. New `hal_flash_try_nv_recovery` mirrors that sequence: (1) RSTEN+RST in DTR (chip drops to 1S default), (2) `OSPI_setProtocol(1,1,1,0)`, (3) `OSPI_disableDdrRdCmds`, (4) `OSPI_setCmdDummyCycles(0)` + `obj->numAddrBytes = 3` + `obj->currentProtocol = FLASH_CFG_PROTO_1S_1S_1S`, (5) WREN in 1S, (6) WRRSB cmd 0x01 with 5 bytes in 1S, (7) WIP poll in 1S, (8) RSTEN+RST in 1S to apply the new NV config. Caller's existing `Flash_close+Flash_open` sequence then re-applies 4S-4D-4D DTR over the now-clean chip. **Expected outcome:** if the chip really is in SafeBoot, this version's NV recovery prints `sr1=0x00` and `wip_polls < 100`, and the immediately-following Flash_write attempt 1 succeeds. |
| 107 | 2026-05-08 | **OTA: S25HL512T-specific WRRSB SafeBoot recovery (correct opcode for our chip family).** Infineon KB #696732 / AN226393 (accessed via user's Infineon login) reveals the SDK's `Flash_quirkOSPIEarlyFixup` SafeBoot recovery is using the **wrong recovery command for our chip**. Table 1 of the KB lists per-family recovery procedures: the **S25HL256/512/01GT family** (which includes our S25HL512T) requires **WRRSB / Write Register SafeBoot (cmd 0x01) with 5 consecutive bytes** `STR1N=0x00 CFR1N=0x00 CFR2N=0x08 CFR3N=0x00 CFR4N=0x08`. The S26/S28 family — which is what TI's SDK quirk implements — uses WRARG (cmd 0x71) per-register with `CFR4N=0xA8`. That mismatch is why TI's stock `ospi_flash_file_io` example printed `Flash_quirkOSPIEarlyFixup:1892 Failed to complete flash recovery` on our chip, and why every recovery path we've tried (Flash_close+open, OSPI_close+open, Sciclient PM reset) failed to clear the SafeBoot state — they all eventually call back into the SDK's wrong-opcode quirk. New `hal_flash_try_nv_recovery` body: `WREN (0x06)` → `WRR (0x01) 5 bytes` → poll WIP=0 (NV write up to 500 ms) → `RSTEN (0x66) + RST (0x99)` → 1.5 ms settling. Wired into `hal_flash_write` retry loop on `attempt==1` (BEFORE the existing PM-reset+driver-tear-down block, while `obj->ospiHandle` is still valid). The Flash_close+Flash_open that follows re-applies the 4S-4D-4D DTR protocol over the now-clean chip. `bb_uart0_puts` checkpoints around each OSPI command so a hang during recovery is visible on UART. **Expected outcome:** if the chip really is in SafeBoot 0x41 / 0x61, this should clear it on the first failed Flash_write (attempt 1 succeeds) and OTA completes. If still fails, the wedge is below register-config (silicon-level), and the TI E2E ticket draft in `docs/session-resume-ota-flash-write.md` is the next move. |
| 106 | 2026-05-08 | **Revert hal_flash_init to no-op (0.A.105 broke boot).** 0.A.105's NV-recovery-at-boot hung the LP — pre-scheduler WRARGs to a chip in SafeBoot 0x61 (MCU_INIT_FAILURE) appear to time out the OSPI command path, which then blocks SBL handoff. LP became unresponsive (no ping, no UART). Reverting `hal_flash_init` to no-op so the LP boots cleanly again. The `hal_flash_try_nv_recovery` helper is retained for the post-scheduler retry path if needed, but right now we trust the SDK's `Flash_quirkQSPIEarlyFixup` (which runs inside Flash_open via bootQuirksFxn) for SafeBoot recovery — it can fix 0x41 CORRUPTION but not 0x61 MCU_INIT_FAILURE. Diagnostic conclusion (decisive): **the chip is in SafeBoot 0x61 — Cypress states this requires hardware reset of the RESET# pin, which on LP-AM2434 is not separately exposed.** Path forward is now off-software: TI E2E ticket OR replace OSPI chip OR add hardware reset signal access. |
| 105 | 2026-05-08 | **OTA: brute-force Cypress NV config restore at boot (root-cause cracked open).** TI's stock `ospi_flash_file_io` example (FreeRTOS, same chip, no watchdog interfering) printed the smoking gun: `ERROR: Flash_quirkOSPIEarlyFixup:1892: Failed to complete flash recovery`. **The chip is in Cypress SafeBoot mode** — its non-volatile config registers (CR1NV-CR4NV) are in a state the SDK considers unrecoverable. We've been chasing this all day with the WRONG diagnostic: cmd 0x05 reads regular SR1 (returns 0x00 in SafeBoot — chip lies), but SafeBoot status lives in STR1V via cmd 0x65 + addr 0x800000 + 2 dummy cycles + 4-byte addressing. Looking at the SDK's recovery code (`Flash_quirkOSPIEarlyFixup:1820-1873`): for STR1V=0x41 (CONFIG_CORRUPTION) it does WRARG to CFR2N=0x08 / CFR3N=0x00 / CFR4N=0xA8 then chip RST — that's a working recovery. For STR1V=0x61 (MICRO_CONTROLLER_INIT_FAILURE_VALUE) it's marked "Requires hardware reset" and SDK gives up. **Our chip is in one of these states, persisting through cold power-cycles because it's NV.** Fix: in `hal_flash_init`, run the SDK's 0x41 recovery sequence UNCONDITIONALLY (CLPEF + WRARG ×3 + RSTEN+RST), then Flash_close+Flash_open to re-apply the protocol-enable sequence. If the chip is 0x41 with our QSPI-quirk variant somehow missing this path → fixes it. If the chip is 0x61 (truly MCU-failure) → still fails, but we have ironclad evidence for TI E2E ticket. |
| 104 | 2026-05-08 | **OTA: hardware-level OSPI peripheral reset via Sciclient PM on Flash_write failure.** 0.A.103 with full OSPI driver re-init (OSPI_close → OSPI_open → Flash_open) still failed identically. The wedge is in the OSPI **peripheral hardware** state, below SDK driver level. Lowest software escalation: `Sciclient_pmSetModuleRst(TISCI_DEV_FSS0_OSPI_0=75, assert=1, ...)` then deassert — toggles the OSPI peripheral's reset signal at the SoC PM (system manager) level. Equivalent to power-cycling just the OSPI block. After reset, OSPI_open + Flash_open re-runs all chip-protocol setup against a hardware-fresh peripheral. If 0.A.104 still fails, the trap is in silicon (e.g., the FSS bus matrix has wedged) and only an SoC reset / power-cycle clears it — at that point we file a TI E2E ticket with all collected evidence. |
| 103 | 2026-05-08 | **OTA: full OSPI driver re-init (OSPI_close+OSPI_open) on Flash_write failure.** 0.A.102 with FW_BANK_B at 0x900000 fails IDENTICALLY to 0x400000 — `SR1=0x02` after attempt 1 (WEL set), post-fail read 0xFF (data didn't land). Address is decisively NOT the issue. The wedge is purely in the OSPI controller's **INDIRECT_WRITE_XFER state machine** (the only OSPI path Flash_write uses that Flash_read and Flash_eraseBlk don't — those use INDIRECT_READ_XFER and STIG respectively, both confirmed working). Flash_close+Flash_open only resets Flash driver state, not OSPI controller. New recovery: OSPI_close → OSPI_open → Flash_open. Auto-flasher works because every JTAG invocation runs Drivers_open which fully re-inits OSPI. This brings the runtime path to that same level of reset. If 0.A.103 still fails, the trap is below OSPI driver init too — likely needs power-cycle of the OSPI peripheral itself or a bus matrix issue. |
| 102 | 2026-05-08 | **OTA: move FW_BANK_B from 0x400000 to 0x900000 (test 4-byte-only addressing region).** 0.A.101 was super informative: `SR1=0x02` after attempt 1 = WEL is set after WREN reaches chip; CLPEF fires (st=0); BUT post-fail read-back shows `FF FF FF FF FF FF FF FF` — data did NOT land. So WREN works (STIG path), PP doesn't (INDIRECT_WRITE_XFER path). Chip command path is fine; OSPI controller's bulk-data path is wedged. We've also realized **no runtime Flash_write has EVER succeeded** in this firmware — auto-flasher works only at 0x80000 (bank A) in NoRTOS, lp_device_config writes only pre-scheduler. Both prior failure addresses (0x200000, 0x400000) were inside the 16 MB / 3-byte addressable region. Try 0x900000 — well past 8 MB, forces 4-byte addressing path, fresh territory. If 0x900000 still fails, address doesn't matter and the trap is purely controller-state. If it works, something about lower-region addressing is wrong in our config. |
| 101 | 2026-05-08 | **OTA: drop Flash_close+open recovery, plain retries + post-fail read-back.** 0.A.100 with 8 retries + 100 ms settling + recovery between each attempt: ALL 8 still failed identically (SR1=0x00 each time). Recovery cycle isn't helping at all. Two new diagnostic vectors: (1) **plain retries** without recovery — Flash_close+open might be putting the OSPI controller in a transition state that doesn't recover cleanly (we'd be making things worse); (2) **read-back after failure** — if `Flash_read` shows our data at the target address despite Flash_write reporting -1, the SDK is reporting false-failure and the data actually landed. That'd be a HUGE finding (means OTA could work; we just need a different success-detection path). If read-back shows 0xFF (still erased), write genuinely failed and we'd escalate to OSPI_lld_deInit/init full controller reset. |
| 100 | 2026-05-08 | **OTA: increase retries to 8 + 100 ms settling after Flash_open recovery.** 0.A.99 SR1 diagnostic was decisive: chip reads SR1=0x00 before every attempt — completely clean state, no P_ERR / E_ERR / BP / WEL stuck. CLPEF fires successfully (st=0). Flash_close+Flash_open recovery succeeds. Yet Flash_write returns -1. **Wedge is NOT in the chip — it's in the OSPI controller hardware state or SDK driver internal state.** Flash_close+Flash_open resets the FLASH driver software but may not fully reset the OSPI controller's INDIRECT_WRITE_XFER state machine. Adding 100 ms settling delay after each Flash_open recovery to let the controller stabilize, plus bumping retries from 3 to 8 (variant-D probe success was on attempt=2; maybe controller needs more attempts to align). If 0.A.100 still fails, escalation is to fully re-init OSPI controller via OSPI_lld_deInit/init — much more invasive. |
| 99 | 2026-05-08 | **Diagnostic: read SR1 before/after CLPEF on each Flash_write attempt.** 0.A.98 with CLPEF before every Flash_write produced the SAME exact failure pattern as 0.A.96/97 — `Write ENTER #1` → 2 recovery cycles → `Write FAIL attempts=3 st=-1`. Either CLPEF didn't actually fire (OSPI_writeCmd silent failure), CLPEF fired but the chip ignored it, or PRGERR/ERSERR aren't the issue and something else is set. We need to SEE the chip's state. New `hal_flash_read_sr1()` reads SR1 (cmd 0x05) directly. Per Cypress datasheet: SR1[7]=SRWD, [6]=P_ERR, [5]=E_ERR, [4]=BP3, [3]=BP2, [2]=BP1, [1]=WEL, [0]=WIP. Logs `[Flash] SR1 before=0xXX after_CLPEF=0xYY` per attempt. Outcomes: (a) before=0x40+ → P_ERR/E_ERR set, expect after_CLPEF=0x00 if CLPEF works. (b) before has BP bits set (0x1C etc.) → block protect is the trap, need different recovery. (c) before=0xFF → chip not responding to RDSR (deeper wedge). (d) before=0x00 → chip looks fine, write fails for some other reason (controller-level, not chip-level). |
| 98 | 2026-05-08 | **OTA: clear Cypress sticky PRGERR/ERSERR (CLPEF cmd 0x30) before each Flash_write/erase attempt.** 0.A.97 bench showed: with both write + erase recovery, the chip's wedge no longer prevents Begin (erase succeeds 981 ms), but Flash_write at chunk 0 STILL fails — even after Flash_close+Flash_open recovery fires twice. Strong indicator: prior failed PPs left **PRGERR (Program Error flag) latched in SR1**. The Cypress S25HL512T rejects all subsequent Page Program commands until that flag is cleared via the **CLPEF (Clear Program/Erase Failure flags)** command (opcode 0x30, no addr, no data). The SDK's `Flash_norOspiWrite` does WREN+PP but never clears these flags. `Flash_quirkQSPIEarlyFixup` only sends CLPEF when SafeBoot detection triggers (specific STR1V values 0x41 / 0x61) — our state may have PRGERR set without entering full SafeBoot. Fix: new `hal_flash_clear_errors()` helper issues CLPEF via raw `OSPI_writeCmd` before each Flash_write AND Flash_eraseBlk attempt. CLPEF needs no WREN, takes few µs, harmless on a clean chip. If 0.A.98 unblocks the writes, we have functional OTA. |
| 97 | 2026-05-08 | **OTA: extend Flash_close+Flash_open recovery to hal_flash_erase_sector.** 0.A.96 bench result confirmed the recovery mechanism works in `hal_flash_write` — UART showed `[Flash] recovered via Flash_close+open before retry 1` and `before retry 2`, and the failure mode shifted from infinite hang to fast-explicit (1.6 s, attempts=3, st=-1). But each test cycle wedges the chip enough that the NEXT test's `Begin` fails because `hal_flash_erase_sector` doesn't have the same recovery — it called `Flash_eraseBlk` once, got SystemP_FAILURE in 501 ms (OSPI command timeout), and returned err=3 → bridge saw `code=23 NovaFwUpdate_Begin`. Added the same 3-attempt Flash_close+Flash_open retry path to `hal_flash_erase_sector`. Now both write AND erase get the SDK's official `Flash_quirkQSPIEarlyFixup` recovery on failure. Symptom-wise: a wedged chip should now self-recover at the start of the very first OTA Begin, instead of needing power-cycle. |
| 96 | 2026-05-08 | **OTA: Flash_close+Flash_open retry uses SDK's official flash-recovery mechanism.** Weekend SDK deep-dive identified the actual fix path with high confidence. (1) `Flash_quirkQSPIEarlyFixup` (`flash_nor_ospi.c:1646`) is the SDK's official Cypress/Spansion flash recovery — added in SDK 12.0 per release notes ("Add flash recovery mechanism"). It runs as `bootQuirksFxn` automatically on every `Flash_open`. Steps: `OSPI_setProtocol(4,4,4,1)` → `Flash_norOspiReset` (RSTEN+RST per devCfg->resetType=0x10) → 200 µs Spansion settling → `OSPI_setProtocol(1,1,1,0)` → full protocol re-enable → SafeBoot detection via STR1V → CFR2N/3N/4N restore if config corruption detected. (2) Auto-flasher succeeds because every JTAG invocation runs `Board_driversOpen` → `Flash_open` → triggers the recovery quirk; our runtime calls Flash_open once at boot and never re-syncs. (3) Once the chip falls out of 4S-4D-4D DTR mode (any reason — partial PP, SR write, etc.), the SDK's `Flash_norOspiWaitReady` (`flash_nor_ospi.c:138`) `while ((status != SUCCESS) || (timeOut > 0))` `||`-bug turns it into an infinite loop. Fix: on `Flash_write` failure, do `Flash_close + Flash_open` then retry — Flash_open's bootQuirksFxn re-runs the full recovery sequence. (4) **Removed HwiP_disable** based on SDK 12.0 release-notes finding "OSPI Interrupt mode not supported" — OSPI runs polling-only, so masking IRQs adds latency without benefit. Cost: ~50-100 ms per Flash_open during retry; success path unchanged. Built clean, untested on bench (chip wedged from prior tests, user away until Monday). |
| 95 | 2026-05-08 | **OTA: re-test HwiP_disable around Flash_write with default pageSize.** 0.A.94 confirmed even default-pageSize 16-byte Flash_write wedges post-scheduler — `Write ENTER #1` no EXIT. Auto-flasher succeeds with same chip same SDK same code, only difference is NoRTOS context (no SysTick / no other interrupts during OSPI command sequencing). Hypothesis: a SysTick IRQ fires during the SDK's WREN+PP+cmdRead sequence and corrupts the chip's internal state — chip then stops responding to RDSR, `Flash_norOspiWaitReady` infinite-loops because of the `||` bug we identified. Test: HwiP_disable around each Flash_write call. 0.A.87 already tried HwiP_disable but had the broken pageSize=16 override active — now testing with default pageSize so the TI SDK code paths are identical to the working auto-flasher. With chunk_size=256 (page-aligned, 1 internal PP per call) the IRQ-mask window is ~3 ms per chunk, well under DWWD timeout. If first chunk's `Write EXIT #1 attempts=1 st=0` shows up, IRQ-induced state corruption is the trap and we need atomic-IRQ-mask per Flash_write call. |
| 94 | 2026-05-08 | **OTA: remove pageSize=16 override (it itself was wedging Flash_write).** 0.A.93 confirmed: even on a clean (post-cold-boot) chip, the very first 16-byte hal_flash_write call wedges — `Write ENTER #1` prints but no EXIT. Auto-flasher works with default pageSize=256 + same chip + same handle, so the override IS the breaker. Theory: Flash_norOspiWrite's loop has subtle assumption tied to pageSize (FIFO watermark, INDIRECT_WRITE_XFER setup, etc.) that breaks at pageSize=16. Removed the override; busyTimeout clamp retained. **Tradeoff:** chained 16-byte chunks at non-page-aligned offsets (0x400010, 0x400020) will now fail the SDK alignment check `if(0 != offset % attrs->pageSize)`. For now, this version tests if the FIRST chunk's single-PP write works cleanly on a clean chip — that gives us a stable foundation to design the chained-chunk strategy from. If first chunk works, 0.A.95 will do alignment-aware chunking (always start each Flash_write at a page boundary). |
| 93 | 2026-05-08 | **OTA: disable chip software-reset call (it broke OSPI/chip protocol sync).** 0.A.92 boot succeeded — auto-flasher reported `[FLASH] write 1/2 (262144 bytes) ok`, `write 2/2 (230874 bytes) ok`, `DONE - SUCCESS`, system booted, orbit polling worked, LP responsive on ping. BUT the OTA push then failed with `err=23 NovaFwUpdate_Begin` (Begin's first-block erase failed after 30 s). Root cause: my `RSTEN (0x66) + RST (0x99)` sequence reset the Cypress chip back to default 1S-1S-1S mode, but the SDK's OSPI driver continues issuing commands in 4S-4D-4D DTR mode — protocol mismatch breaks every subsequent runtime flash op. The auto-flasher succeeded because TI's Board_driversOpen runs `Flash_norOspiPhyTune` + mode-enable sequence on every JTAG re-init. To use chip-reset safely we'd need to call `Flash_close + Flash_open` immediately after the reset to re-sync. Deferred. For now, `hal_flash_init` is a no-op and the `hal_flash_chip_reset` function is retained for future explicit-recovery use. **Tradeoff:** without chip-reset, we're back to needing a cold power-cycle between failed-write tests. Tradeoff accepted to make progress on the actual OTA path. |
| 92 | 2026-05-08 | **OTA: pre-scheduler-safe `hal_flash_chip_reset` (no ClockP_usleep, plus bb_uart0_puts checkpoints).** 0.A.91 boot went silent on UART after `Board_flashOpen` — likely my `ClockP_usleep(1000)` faulted pre-scheduler (the SDK's clock-tick path may not be initialized that early). Replaced with a calibrated volatile busy-wait (~1.5 ms at 800 MHz) and added `bb_uart0_puts` checkpoints so we can see exactly where the reset path is in the boot trace: `chip_reset: enter / RSTEN sent / RST sent / done`. Also confirmed at the bench that the auto-flasher itself succeeded (`[FLASH] write 1/2 (262144 bytes) ok` for the FIRST 256 KiB) — so 0.A.91's main image landed correctly, the LP just hung in pre-scheduler init. |
| 91 | 2026-05-08 | **OTA: chip software-reset (RSTEN+RST) at boot to clear wedged state from prior session.** 0.A.90 ENTER/EXIT diagnostic confirmed the wedge is INSIDE Flash_write — `[Flash] Write ENTER #1 addr=0x400000 len=16` printed but no EXIT in 60 s. Even our `flashBusyTimeout` clamp didn't help because the SDK's `Flash_norOspiWaitReady` (`flash_nor_ospi.c:138`) has a real bug: `while ((status != SUCCESS) || (timeOut > 0))` — note the `||` not `&&`. Once the chip stops responding (cmdRead fails), `status != SUCCESS` is permanently true and the loop **NEVER exits regardless of timeOut**. The chip wedges easily because of our prior failing multi-PP writes (0.A.78-86), and the wedge persists across warm-resets (Sciclient_pmDeviceReset doesn't power-cycle OSPI). Fix: add `hal_flash_chip_reset()` that issues raw OSPI commands `RSTEN (0x66) + RST (0x99)` via `OSPI_writeCmd` — the chip accepts these even when busy. Wired into `hal_flash_init` and called from `main.c` immediately after `Board_flashOpen`. The 0.A.91 chip is guaranteed responsive at boot regardless of how badly we wedged it last session — eliminates the "must physical-power-cycle between every test" pain. Also still has the pageSize=16 + busyTimeout clamp + ENTER/EXIT logs from 0.A.89-90 to keep diagnosing the per-write success path. |
| 90 | 2026-05-08 | **OTA: clamp SDK flashBusyTimeout + per-call ENTER/EXIT log on first 3 chunks.** 0.A.89 chunk_size=16 test ended with NO chunk-progress lines AND no Write FAIL log — UART simply stops at the `pageSize override` line for the entire 2-min capture. That means hal_flash_write was called once (override fires inside it) and then the task is wedged inside Flash_write. The SDK's `gFlashDevCfg_S25HL512T.flashBusyTimeout = 6000000` (read from the generated config) means a stuck WIP poll busy-loops for ~5 minutes before returning FAILURE — that explains the silence. Two changes: (1) clamp `flashBusyTimeout` to 100000 (~3 s worst case) so wedged writes return promptly. (2) Log `[Flash] Write ENTER #N` and `[Flash] Write EXIT #N st=X attempts=Y` for the first 3 hal_flash_write calls — confirms whether we're stuck inside Flash_write vs returning quickly with failure. After 3, only the legacy FAIL/OK logs fire (so chunk_size=16 doesn't flood UART). |
| 89 | 2026-05-08 | **OTA: per-100-chunk progress UART trace.** 0.A.88 with chunk_size=16 ran for 20 s with NO `[Flash] Write FAIL` log before bridge saw ECONNRESET. Single-PP Flash_writes log nothing on first-attempt success, so we can't tell from UART whether chunks landed silently (good — the actual fix) vs got stuck somewhere. Adds a `[FwUpd] chunk progress: count=N bytes=N/total` print every 100 chunks. With chunk_size=16 + 484 KiB image = ~30000 chunks total, expect ~300 progress lines. The 0.A.88 ECONNRESET-at-20s would equate to roughly 500-2000 chunks landed (bridge-side ack-await rate-limits us); the progress trace will say exactly. **Verdict from this test:** if progress prints stop at the ECONNRESET point, chunks ARE landing fine and the close is connection-level (lwIP TCP send blocked, rx buffer overflow, etc.) — fixable with bridge-side throttling or LP rx-buffer bump. If progress prints stop earlier (e.g. only 1-2 lines), chip is wedging at single-PP-per-call level too — escalate to software reset between PPs. |
| 88 | 2026-05-08 | **OTA: revert HwiP_disable (didn't help, hurt chip recovery).** 0.A.87 bench-confirmed: HwiP_disable around Flash_write produces the SAME chunk-0 fail as 0.A.86 (no improvement) AND additionally leaves the chip in a wedged state where the next Begin's `Flash_eraseBlk` times out (`code=23` Begin error on a follow-up chunk_size=32 test). The long IRQs-masked window apparently prevents some chip-recovery handshake from running. Reverted. The remaining diagnosis is solid: the trap is **chip-side** — Cypress S25HL512T post-scheduler in 4S_4D_4D DTR mode gets WIP stuck high after some PP within a Flash_write call. Single PPs (one per Flash_write) work; multiple PPs (within one Flash_write OR across calls — never cleanly tested) — TBD. **Next test:** with 0.A.88, push with `chunk_size=16` so each OTA chunk → one `hal_flash_write` → one `Flash_write` → one internal PP. If chunks succeed all the way through (484 KiB / 16 B = ~30000 chunks at ~5-15 ms each = 2-7 minutes), we have a slow-but-functional OTA. The "20 s ECONNRESET" we saw earlier with small-chunk pushes in 0.A.86/87 was likely lp_ota_task rx buffer overflow (`LP_OTA_MAX_FRAME=2048`); if that recurs we add bridge-side rate limiting or bump the LP rx buffer. Keep `pageSize=16` override (essential for the SDK alignment check to accept 16-byte-aligned offsets). |
| 87 | 2026-05-08 | **OTA: HwiP_disable around each Flash_write attempt (re-test IRQ-mask hypothesis cleanly).** 0.A.86 boot fix verified — LP pings, version banner reads `0.A.86`, OTA listener accepts. The OTA push then failed at chunk 0 with the expected SDK FAIL: 1024-byte Flash_write at 0x400000 with pageSize=16 (64 internal PPs of 16 B each) returned SystemP_FAILURE for all 5 retries. This is the clean confirmation we never had before: the trap is **2nd+ consecutive Page Programs fail post-scheduler**, INSIDE one Flash_write call as well as across calls — not address, not call-size, not pageSize. Auto-flasher (NoRTOS) does 1024 PPs per Flash_write and succeeds because no scheduler IRQs interrupt the polling loop. Hypothesis: a FreeRTOS tick or ENET/USB IRQ fires during `Flash_norOspiWaitReady`'s WIP-poll loop, the iteration counter exhausts before WIP=0 is seen, the SDK's outer loop breaks. Test: wrap each `Flash_write(h, addr, buf, len)` call in `HwiP_disable() / HwiP_restore()`. 0.A.72 tested this at single-PP scale and called it "didn't help" — but it had no retry envelope; the post-erase first-PP transient masked the result. With our pageSize=16 + 5×50 ms retries, this should give a clean PASS/FAIL. Per-call IRQ window: 1024 B / pageSize 16 = 64 PPs ≈ ~64 ms IRQs-disabled — LpWatchdog DWWD has ~10 s margin so a heartbeat tick missed during this window is fine. |
| 86 | 2026-05-07 | **Re-enable lwIP demo apps + udp_iperf.c (load-bearing for LP→bridge UART2 TX path).** 0.A.85 didn't ping after warm-reset because back in 0.A.82 — when we needed MSRAM for the 256 KiB OTA scratch — we disabled `LWIP_TCPECHO_APP / LWIP_UDPECHO_APP / LWIP_LWIPERF_APP / LWIP_UDPERF_APP` and removed `udp_iperf.c` from the makefile. Per the existing memory file [`memories/repo/lp-msram-bloat-audit-may2026.md`](../memories/repo/lp-msram-bloat-audit-may2026.md), this combination "builds clean but silently breaks the LP→bridge UART2 TX path: LP goes silent on debug UART after boot, bridge `connected:false`, `rxFrames` frozen at 0." That's exactly what 0.A.85's UART log showed (boot trace cut at SBL `Drivers_open: 1664us` with no further output) and why ping was failing. One of the demo init calls in `test.c::apps_init()` is load-bearing for the production bridge TX path — bisecting which has not been done. Until that's investigated and fixed properly, all four LWIP_*_APP flags must stay enabled and `udp_iperf.c` must stay in the build. Cost: 288 KiB BSS for `UDP_IPERF_SND_BUF`. After 0.A.85's revert of the OTA `s_block_buf` (256 KiB), this still fits — MSRAM headroom drops from 415 KiB back to ~127 KiB. **Tomorrow's bench:** flash 0.A.86, expect clean boot + ping responsive within ~5 s of warm-reset. Then run the 0.A.85 OTA test (lazy-erase + direct chunk writes, no block buffer, pageSize=16 override) to confirm whether moderate-size single Flash_write per chunk works post-scheduler. |
| 85 | 2026-05-07 | **OTA: revert block buffer, restore direct chunk writes; pageSize=16 override retained.** 0.A.84 proved a single 262144 B Flash_write fails post-scheduler even with the SDK pageSize override (16384 internal PPs in one call → still trips). The 0.A.82 block buffer was a workaround for what 0.A.79 looked like a "consecutive Flash_write trap," but 0.A.79's actual failure was the SDK alignment check rejecting addr=0x200010 (16-byte slice into a non-page-aligned offset) — not a hardware trap. We never actually tested clean chained Flash_write calls at page-aligned offsets after a single erase. This version does that: 1024-byte chunks → one Flash_write per chunk (64 internal PPs at pageSize=16) → many calls per block, each call moderate-size. If chained calls work, OTA is functional and we recover the 256 KiB BSS the buffer was eating. If still fails, the trap is specifically "more than N PPs after a single erase" — escalate to per-chunk re-erase or sub-block fragmentation. Removes `s_block_buf` (256 KiB BSS) and `flush_block`; restores `s_erased_through` lazy-erase from 0.A.78. Keeps FW_BANK_B at 0x400000 (irrelevant — proven address-independent). |
| 84 | 2026-05-07 | **OTA: override SDK pageSize from 256 to 16 (work around full-page-PP trap).** 0.A.83 reproduced the same `Flash_write FAIL len=262144` at the new 0x400000 address — definitively ruled out address-specificity. Re-examining the cumulative bench evidence: every successful post-scheduler Flash_write the codebase has produced was either a partial-page program (16 B in 0.A.73 variant-D at 0x700000; 16 B in 0.A.79's first slice at 0x200000) OR pre-scheduler (lp_device_config 80 B at 0x600000 — runs from `LpDeviceConfig_Init` before scheduler start). Every full-page Page Program (≥256 B) post-scheduler has failed. The chip itself accepts 1-256 B per PP command — `pageSize=256` is purely an SDK abstraction the loop in `Flash_norOspiWrite` uses to step through the buffer. Workaround: `attrs->pageSize = 16` (one-shot lazy override on first hal_flash_write call). The SDK then internally splits any Flash_write into 16-byte PPs — sizes we have post-scheduler success evidence for. Cost: ~16× more PP commands; throughput chip-bound at tPP≈200 µs/PP, so a 256 KiB block flush goes from ~0.2 s to ~3-4 s. Acceptable. Block-buffered OTA (0.A.82) and FW_BANK_B at 0x400000 (0.A.83) retained — both work correctly, the only blocker was the SDK's pageSize-stride PP loop. **If 0.A.84 succeeds:** OTA fully functional; subsequent cleanup (0.A.85+) can revert block-buffer to direct chunk writes (recovering the 256 KiB BSS), revisit FW_BANK_B address, and consolidate the diagnostic logging. **If still fails:** the trap is below the SDK Page Program command itself (e.g., the OSPI controller's INDIRECT_WRITE_XFER state machine specifically dislikes our post-scheduler context) — escalate to raw OSPI_lld_writeIndirect with manually-staged WREN/PP/WIP. |
| 83 | 2026-05-07 | **OTA: move FW_BANK_B from 0x200000 to 0x400000 (probable address-specific Flash_write trap).** Critical revelation while studying the SDK source: `Flash_norOspiWrite` ALWAYS internally splits any Flash_write call into pageSize (256 B) Page Programs in a loop — auto-flasher's 256 KiB call and our 256-byte call go through *the same internal PP loop*. So write size cannot be the discriminator. What WAS shared by every failure since 0.A.74 is the address: 0x200000. Confirmed-working post-scheduler addresses: 0x080000 (auto-flasher, 256 KiB Flash_write — works), 0x600000 (lp_device_config, 80 B — works), 0x700000 (variant-D probe, 16 B — works after retry). 0x200000 is the ONLY address where Flash_write has reproducibly failed. Earlier "consecutive Flash_write fails" inference (0.A.79) was actually the SDK alignment check rejecting `addr=0x200010` (16 B slice into the same page). Hypothesis: chip-side block-protect bits, an internal address-decode/calibration issue specific to that 256 KiB block, or some PHY-tuning region. Cheap test: move Bank B to 0x400000 (clean address, far from any header / golden / lp_device_config). Also bumps FW_GOLDEN_OFFSET from 0x380000 to 0x800000 since the new Bank B at 0x400000 + 1.5 MB extends to 0x580000. Block-buffered OTA logic from 0.A.82 retained — it WORKS, the only hold-up was the first flush_block hitting the 0x200000 address. |
| 82 | 2026-05-07 | **OTA: block-buffered (one Flash_write per 256 KiB erase block, matches auto-flasher pattern).** 0.A.81's `Flash_close + Flash_open` between every Flash_write didn't return a clean PASS/FAIL — the bridge timed out at 180 s with only ONE `[Flash] Write OK addr=0x200000 len=16 attempts=17` line in the UART log, suggesting either many silent successes (functionally working but ~10× too slow to ever finish) or the reopen sequence wedged. Either way, reopen-per-write is the wrong shape — the real fix is the auto-flasher pattern: `erase block → ONE Flash_write of up to 256 KiB → next block`. `flasher_uart::do_erase_write_verify` succeeds against the same SDK + same chip in NoRTOS; `lp_device_config::Save` succeeds post-scheduler with the same one-write-per-erase shape. New design: 256 KiB MSRAM scratch (`s_block_buf`) accumulates chunks; `flush_block` issues one `hal_flash_write` per scratch fill (also handles the lazy erase of blocks 1+); chunks straddling a block boundary are split. `hal_flash.c` reverted to a clean single-Flash_write-per-call form (5×50 ms retry for the post-erase first-write transient, no internal page splitting, no Flash_close/open, no settling delay) — trust the SDK to handle multi-page on this chip per auto-flasher evidence. Memory cost: 256 KiB BSS; main MSRAM has ~0.8 MiB free, fits comfortably. If 0.A.82 succeeds end-to-end (Begin → 256 chunks land block 0 → erase block 1 → remaining chunks → final flush → Finalize CRC OK), the consecutive-write trap is closed. |
| 81 | 2026-05-07 | **OTA: Flash_close + Flash_open between consecutive writes (driver-state reset diagnostic).** 0.A.80 produced the same consecutive-write fail with no improvement from a 10 ms post-write delay — proving the trap is not a settling-time issue. Found the smoking-gun comparison in `flasher_uart/main.c:do_erase_write_verify`: the auto-flasher pattern is `erase block → ONE Flash_write of up to 256 KiB → next block`. `lp_device_config::bank_program` is the same pattern (one erase cycle, one Flash_write call). OTA is the ONLY consumer that issues multiple Flash_write calls between erases — and that's the trap. As a definitive diagnostic (and slow workaround) close + reopen the SDK handle between each Flash_write so each call gets a fresh driver state. If 0.A.81 succeeds end-to-end, the real fix is **0.A.82 = block-buffered OTA**: accumulate up to 256 KiB of chunk data in an MSRAM scratch, then issue one Flash_write per block (matching the auto-flasher pattern exactly, no driver acrobatics). If 0.A.81 still fails, the trap is below the SDK Board Flash wrapper and we go to raw `OSPI_norFlashWrite`. |
| 80 | 2026-05-07 | **OTA: 10 ms post-write settle delay between consecutive Flash_writes.** 0.A.79 produced the breakthrough log line: `[Flash] Write OK addr=0x200000 len=16 attempts=2` immediately followed by `[Flash] Write FAIL addr=0x200010 len=16 err=-1 attempts=20 prime=ffffffff`. The first 16 B write at the bank-B base SUCCEEDED (matching variant-D's pattern at 0x700000). The second 16 B write at +16 bytes FAILED all 20×50 ms retries with chip still erased clean. Conclusion: the trap was never address-specific, never write-size-specific, never multi-block-erase-related — it is **consecutive `Flash_write` calls**, full stop. Variant-D / `lp_device_config` / `flasher_uart` all succeed because each does exactly ONE write per erase cycle; OTA is the only multi-write consumer. Hypothesis: the SDK's `Flash_norOspiWaitReady` WIP=0 poll returns before the Cypress S25HL512T 4S_4D_4D DTR controller is actually ready to accept the next Page Program command. Workaround: 10 ms `ClockP_usleep` after each successful `Flash_write` so the next call enters with the chip fully settled. Cheap single-line change. If insufficient, escalate to Flash_close/Flash_open between writes (resets SDK driver state) or raw `OSPI_norFlashWrite`. Also keeps WRITE_SLICE=16 from 0.A.79 — splitting in 16-byte chunks is well-tested on this codepath now and we can revisit page-size after the post-write trap is closed. |
| 79 | 2026-05-07 | **OTA: cap Flash_write slice at 16 bytes (match variant-D's known-working size).** 0.A.78 lazy-erase probe definitively ruled out the multi-block-erase pattern: with single-block erase + same `prime=ffffffff` chip-clean signal + same 20×50 ms retry envelope, `[Flash] Write FAIL addr=0x200000 len=256 err=-1 attempts=20` reproduced exactly. Erase-time dropped from ~1800 ms (3 blocks) to ~917 ms (1 block) confirming 0.A.78 logic IS running. Variables now eliminated: settling-time, read-prime, multi-block-erase. The 0.A.73 variant-D probe wrote successfully at 0x700000 with **16 B Flash_write** (succeeded on attempt=2); OTA fails with **256 B Flash_write** at 0x200000. Splitting each `hal_flash_write` slice at 16 B regardless of page boundary isolates write-size as the variable. If it works, the SDK's full-page Flash_write path on Cypress S25HL512T 4S_4D_4D DTR is broken (or has setup state we're missing) and 16 B becomes the workaround. If still fails, address-specific to 0x200000 — next is to move FW_BANK_B (e.g. to 0x300000). Diagnostic side-test (chunk_size=64) on 0.A.78 produced no `[Flash] Write FAIL` log + ECONNRESET at 22.6 s — different failure mode (no per-write log line emitted) consistent with the small-write path getting further than the 256 B path. |
| 78 | 2026-05-07 | **OTA: lazy / interleaved block erase (mirror the only-known-good runtime erase+write pattern).** 0.A.77 read-prime probe definitively ruled out controller-state-vs-write-path: `[Flash] Write FAIL addr=0x200000 len=256 err=-1 attempts=20 prime=ffffffff` proves the chip is erased clean AND reads work, so the write path itself is broken. The only structural variable left between known-good (`lp_device_config` ~80 B at 0x600000, succeeds; 0.A.73 variant-D 16 B at 0x700000, succeeds in 2 retries) and broken (OTA 256 B at 0x200000, fails 20×) is the **erase pattern**: working paths erase ONE block then write into it; OTA was erasing 3 different blocks back-to-back upfront, then attempting the first write into the (long-since-settled) first block. New approach: `Begin` erases ONLY the first 256-KiB block of the target bank; `WriteChunk` lazy-erases the next block on demand when the chunk crosses the current erased boundary. Each lazy erase yields a tick so LpWatchdog client + lwIP keep running. Adds `s_erased_through` static + `FW_OTA_ERASE_BLOCK` constant; legacy `erase_bank_range` / `erase_bank` retained but no longer called from `Begin`. If this works, it's the actual fix (not just a diagnostic) and the prior 0.A.73→0.A.77 retry-loop / read-prime instrumentation can be unwound. If it still fails, the remaining hypothesis is address-specific (try moving FW_BANK_B from 0x200000). |
| 77 | 2026-05-07 | **OTA: prime the indirect-mode OSPI controller with a Flash_read before each write.** 0.A.76 diagnostic confirmed all 20 retries × 50 ms (1 s envelope) fail at `addr=0x200000 len=256 err=-1` — definitively NOT a settling-time issue. Differs from the 0.A.73 variant-D probe at `0x700000` (which succeeded on attempt=2 after just 1 erase block + 16-byte write) by: (a) larger erase span (3 blocks vs 1), (b) full 256-byte page write vs 16 bytes, (c) different address. Implementing the 0.A.73 variant-F hypothesis (untested at the time): the SDK's indirect-mode OSPI controller may need a Flash_read to "prime" its state machine before the first post-erase Flash_write. Adds a 16-byte Flash_read of the target page before the retry loop in `hal_flash_write`. Logs the first 4 bytes of the read in the FAIL line so we can tell if the bytes are 0xFF (erased = expected) or stale data (would indicate the prior failed writes partially landed). |
| 76 | 2026-05-07 | **OTA: instrument retry loop with attempts-used + per-attempt status.** 0.A.75 (20×50ms = 1 s envelope) still failed at chunk 0 with `code=26` (= `LP_OTA_ERR_CHUNK + rc=5` = `hal_flash_write` returned -1 → `Flash_write` returned non-SUCCESS for all 20 attempts). Test took 5.3 s, plausibly consistent with 20 retries actually executing. Adds `attempts_used` counter to the retry loop so the UART log distinguishes "all 20 failed" from "succeeded after N retries", and an OK-but-retried trace when N > 1. Will tell us whether the failure is settling (→ bigger envelope) or structural (→ different approach: pre-write Flash_read prime, post-erase explicit `Flash_norOspiQuirks`, or different chunk addressing). |
| 75 | 2026-05-07 | **OTA: bump `Flash_write` retry envelope from 5×10ms to 20×50ms (1 s).** 0.A.74 with 5×10ms retries failed end-to-end OTA push at chunk 0 (`code=26 msg="NovaFwUpdate_WriteChunk"` = `LP_OTA_ERR_CHUNK(21) + rc=5` = "Flash write failed"). The 0.A.73 evidence (`attempt=2` success after 20 ms) was a **post-write** settling case (no erase in that probe), but the OTA path hits **post-erase** settling — Cypress S25HL512T 256 KiB block-erase typical relaxation is ~600 ms, max ~2 s, much longer than post-write. Bumped `hal_flash_write` retry to 20 attempts × 50 ms = 1 s envelope. Erase still uses `Flash_eraseBlk` (which already polls WIP before returning); the additional settling is the chip's program/erase state-machine, not WIP. |
| 74 | 2026-05-07 | **OTA: post-scheduler `Flash_write` retry-with-delay workaround.** 0.A.73 variant-D probe definitively proved the failure is **transient** — `[OTAPROBE-D retry] off=0x700000 er=0 wr=0 attempt=2 rb=dcdddedf` — the 3rd `Flash_write` attempt (after two failures and 10 ms gaps) succeeded. Pre-scheduler writes never trip this because the cold-boot chip is fully idle; once FreeRTOS is up something keeps the chip busy/unsettled across the WaitReady boundary in the SDK driver. Fix in `Platform/hal_flash.c::hal_flash_write`: retry up to 5× with `ClockP_usleep(10000)` between attempts. Removed the now-redundant `vTaskSuspendAll`/`xTaskResumeAll` wrappers from both `hal_flash_write` and `hal_flash_erase_sector` (proven-not-needed in 0.A.72 probe B). Removed the variant-A/B/C/D/E/F diagnostic block from `lp_ota_task.c`. Erase still uses `Flash_eraseBlk` (256 KiB / cmd 0xDC) — that path remains correct. **Expected result on next OTA push:** Begin → erase ~1.8 s, then chunks land at 0x200000 with `[OTA] BEGIN rc=0` followed by progress to 100 %. Document trap in repo memory once verified. |
| 73 | 2026-05-07 | **OTA debug: variant D/E/F probes — retry, no-erase, read-first.** 0.A.72 confirmed IRQ masking does not unblock post-scheduler `Flash_write` (A/B both fail wr=-1 at 0x700000/0x740000; C with vTaskSuspendAll made erase fail too — likely because some chip-state-management call inside Flash_eraseBlk needs the scheduler running). Three new variants: D — erase + retry write 5x with 10ms gaps; E — write to (presumably-erased) block without re-erasing first; F — Flash_read first (warming the indirect path), then erase, then write. If D succeeds on attempt > 0, transient driver state. If E succeeds, the eraseBlk just before write is the corruptor. If F succeeds, the indirect-read path "primes" the controller for indirect-write. |
| 72 | 2026-05-07 | **OTA debug: bisect post-scheduler `Flash_write` failure (IRQ vs scheduler).** 0.A.71 result: `[OTAPROBE] off=0x700000 er=0 wr=-1 rd=0 rb=ffff…` — bare post-scheduler `Flash_write` fails at any offset (erase succeeds, read succeeds, only WRITE fails). Pre-scheduler writes (lp_device_config) work fine. Hypothesis: an interrupt fires between the WREN command (0x06) and the Page-Program command (0x02) in `Flash_norOspiWrite`, clearing the Write-Enable Latch (WEL) bit, and Page-Program is rejected. Probe runs three variants in sequence at 0x700000/0x740000/0x780000 (separate erase blocks): (A) bare, (B) HwiP_disable around `Flash_write` only, (C) `vTaskSuspendAll` + HwiP_disable. If B succeeds but A fails → IRQ between WREN and PP is the bug. If C succeeds but B fails → unlikely, would mean a scheduler-side hook is touching OSPI. If all three fail → something deeper (chip status register block-protect bits, quirk function, OSPI controller state). |
| 71 | 2026-05-07 | **OTA debug: post-scheduler `Flash_write` probe in `lp_ota_task`.** 0.A.70 pre-scheduler test in `LpDeviceConfig_Init` produced no output — USB CDC re-enumerates after SoC reset and Windows misses everything between SBL and post-scheduler `[LpSettings] Restored bank A`. Moved the probe (`Flash_eraseBlk` + `Flash_write` + `Flash_read` at `0x700000`, 16-byte `0xD0..0xDF` pattern) to right after `[OTA] Phase 1A listening...` so it runs in the same post-scheduler context as the failing OTA Begin/Chunk path. Logs as `[OTAPROBE] off=0x700000 blk=N er=N wr=N rd=N rb=...`. If `wr=0` and `rb=d0d1...df`, the OTA Begin/Chunk path itself is the problem (likely interaction with bank-B header sectors or chunk size > probe size). If `wr=-1`, post-scheduler `Flash_write` is broken regardless of offset. Removed the dead pre-scheduler test from `LpDeviceConfig_Init` (its `bb_uart0_puts` output is in the USB-re-enum blackout window). |
| 70 | 2026-05-07 | **OTA debug: pre-scheduler `Flash_write` diagnostic.** 0.A.69 SuspendAll wrap did not fix the post-scheduler `Flash_write` failure (still `err=-1`). Added a one-shot erase+write+read at `0x700000` at the end of `LpDeviceConfig_Init` (pre-scheduler, same context that the working device-config writes use). Logged via `bb_uart0_puts` (DebugP_log stalls pre-scheduler). Look for `[BB] PRESCH-FLASH off=0x700000 blk=N er=N wr=N rd=N rb=...`. If `wr=0` and `rb=d0d1...df` then the post-scheduler context is the problem; if `wr=-1` the entire driver setup is broken (chip protect bits, quirk fxn not invoked, opcode mismatch, etc.). |
| 69 | 2026-05-06 | **OTA: wrap `Flash_write` / `Flash_eraseBlk` with `vTaskSuspendAll`/`xTaskResumeAll`.** 0.A.68 (block-erase) made the erase phase clean (8 × 256 KiB blocks in 1.78 s) but `Flash_write` at bank-B still returned `SystemP_FAILURE` immediately. Audit shows: (a) `LpDeviceConfig_Save` is the only other caller of `Flash_write` in the codebase, and grep proves it is only invoked from `LpDeviceConfig_Init` — i.e. **always pre-scheduler**; (b) `flasher_uart` runs NoRTOS — also no scheduler; (c) syscfg has `intrEnable=FALSE`, `dmaEnable=FALSE`, `phyEnable=FALSE`, so the TI Flash driver path is fully polled (busy-loop on WIP bit, `ClockP_usleep` for delays, no semaphores). Conclusion: `Flash_write` had never been proven to work post-scheduler in this codebase; OTA was the first attempt. The polled WIP loop and OSPI register manipulation get corrupted by FreeRTOS context switches, so wrap each call in `vTaskSuspendAll`/`xTaskResumeAll` (safe because the polled path has no SemaphoreP_pend, IRQs still fire for ClockP). | 
| 68 | 2026-05-06 | **OTA: switch erase from `Flash_eraseSector` (0x21) to `Flash_eraseBlk` (0xDC).** Selftest in 0.A.67 (UART: `JEDEC mfg=0x34 dev=0x2a1a` confirms Cypress S25HL512T 64 MB chip matches syscfg) ran an erase+write+read at fresh address `0x700000`. Result: `er=0 wr=-1 rd=0 rb=ffff…` — the 4-KiB sector-erase opcode `0x20/0x21` returned success but **left the OSPI controller in a stuck state** where every subsequent `Flash_write` AND raw `OSPI_norFlashWrite` returned `SystemP_FAILURE` immediately, and `Flash_readId` came back `0xffffffff` after the failed writes (chip unresponsive until power-cycle). Auto-flasher (`flasher_uart/main.c`) and `lp_device_config.c` both write successfully against the same syscfg-generated handle — both use `Flash_eraseBlk` (256-KiB block-erase opcode `0xDC`). Conclusion: the Cypress chip in 4S_4D_4D DTR mode (per syscfg `protocolCfg`) does not honor the legacy 4-KiB sector-erase opcodes. Fix: `Platform/hal_flash.c::hal_flash_erase_sector` now uses `Flash_eraseBlk` (256 KiB granularity); `Platform/nova_fw_update.c::erase_bank_range` rounds up to and steps by 256 KiB. Selftest harness removed from `lp_ota_task.c`. **Known follow-up:** `set_bank_header` would now wipe an entire 256-KiB block on each call, destroying the adjacent header — header layout needs to be redesigned (single block holding bootmeta + both bank headers, or move headers into the `lp_device_config` ping-pong region). Tracked separately; doesn't block Begin/Chunk testing. |
| 67 | 2026-05-06 | **OSPI write self-test in `NovaFwUpdate_Init`.** 0.A.65 + 0.A.66 fixed page-splitting + sector-vs-block-erase but `Flash_write` at bank-B `0x200000` still returns `SystemP_FAILURE` immediately on the OTA path, while `lp_device_config` at `0x600000` reportedly succeeds. Added a one-shot non-destructive erase+write+read at offset `0x700000` (well outside any active region) at the end of `NovaFwUpdate_Init` so we can see whether OSPI writes work at all from the post-`Board_driversOpen` post-scheduler context. UART log: `[FwUpd] selftest@0x700000 er=N wr=N rd=N rb=...`. If `rb` shows the `0xA0..0xAF` pattern, the OSPI write path is healthy and the OTA bug is path-specific (likely concurrent access from another task). If `rb` shows `0xFF`s or junk, device-config has been silently failing all along and the OSPI driver setup needs deeper investigation. |
| 66 | 2026-05-06 | **OTA: switch `hal_flash_erase_sector` from `Flash_eraseBlk` (256 KiB) to `Flash_eraseSector` (4 KiB).** The SDK Flash driver's `eraseFxn` (block) maps blockNum→`blockSize=262144` granularity, so iterating 4 KiB steps across a 256 KiB region issued the 256-KiB Block-Erase command 64× against the same block. Wasted work *and* — verified on bench — left the OSPI controller in a state where the first `Flash_write` after the erase burst returned `SystemP_FAILURE` immediately (no chip-busy timeout, just a synchronous fail). Switching to `eraseSectorFxn` (cmd 0x21, real 4 KiB granularity) makes each erase touch a unique sector AND clears the post-erase write trap. Bonus: Begin time drops from ~110 s (121 redundant 256-KiB block erases) to ~6 s (121 real 4-KiB sector erases) so we no longer need the 180 s OTA ack timeout. |
| 65 | 2026-05-06 | **OTA: split `hal_flash_write` across NOR Page-Program boundaries.** Bench bringup of OTA push (post-0.A.64) showed the very first `FwDataChunk` write to bank-B `0x200000` returning `SystemP_FAILURE` (-1) from the TI Board Flash driver even though the bank was freshly erased and the handle was valid. Root cause: TI's `Flash_write` does NOT internally split a write across the W25Q64JV's 256-byte page boundaries — multi-page writes (our chunks are 1 KiB = 4 pages) are rejected outright. `lp_device_config.c` writes ~80 B records, so it never tripped this; OTA is the first multi-page consumer. Fix: `Platform/hal_flash.c::hal_flash_write` now walks the buffer in `PAGE_SIZE`-bounded slices, advancing across the page boundary each iteration. No callers needed to change. |
| 64 | 2026-05-06 | **OTA: erase only the sectors we'll actually write + Begin trace.** Previous Begin handler called `erase_bank()` which iterated the full `FW_BANK_MAX_SIZE = 1.5 MB` regardless of the incoming `total_size`. For a typical 491 KB image that's 3× the necessary erase work (384 sectors vs 123) — pushing wallclock past the bridge's reasonable ack-timeout window and starving orbit Modbus polling. New helper `erase_bank_range(bank, nbytes)` walks only the sectors the image will occupy (rounded up to 4 KB), still yielding via `vTaskDelay(1)` per sector so LpWatchdog client + lwIP keep running. Added UART trace `[OTA] BEGIN size=... — erasing` / `[OTA] BEGIN rc=... erase_ms=...` so we can measure the actual cost from the bench instead of guessing. The legacy `erase_bank()` wrapper still exists for any non-Begin caller. |
| 63 | 2026-05-06 | **OTA: feed watchdog during full Bank B erase.** `NovaFwUpdate_Begin` synchronously erases the 1 MB staging bank (~496 sectors × ~40 ms ≈ 20 s of OSPI-bound work on the OTA task). Without yielding, LpWatchdog's client task can't run during that window and the DWWD fires mid-flash, bricking the OTA. Fix: `Platform/nova_fw_update.c::erase_bank()` now calls `vTaskDelay(1)` every 8 sectors (~320 ms), guarded by `xTaskGetSchedulerState() != taskSCHEDULER_NOT_STARTED` so pre-scheduler callers (`NovaFwUpdate_BootValidation` from `main()`) still work. The staging-copy activate path already yields per-sector; this closes the matching gap on the Begin-time erase. |
| 62 | 2026-05-06 | **OTA Phase 1B + 2: real flash writes, bounded staging-copy activate.** `Platform/nova_fw_update.c` + `Platform/hal_flash.c` linked into `nova_lp` build (`+5.3 KB` to 491,018 B). `main.c` calls `NovaFwUpdate_Init()` after `LpDeviceConfig_Init` (read-only OSPI access — populates cached bank headers + boot meta). `lp_ota_task.c` Phase 1A `NOT_IMPL` branch replaced with proper Begin/Chunk/Finalize handlers backed by the platform API (writes to Bank B at `0x200000`). Activate path implements the Phase 2 staging-copy hack: erase `0x080000-0x180000` (1 MB) in 4 KB sectors with `vTaskDelay(1)` yields, copy verified Bank B → main slot in 1 KB chunks, then warm-reset via `Sciclient_pmDeviceReset`. **Bounded by `LP_OTA_MAIN_MAX_BYTES = 0x100000`** (the gap between main and watchdog at `0x180000`) so an oversized image is rejected upfront in `FwBegin` and the activate copy can never clobber the watchdog mcelf. `FwBankInfo` now reflects real header data + `boot_count` / `boot_reason` from `FwBootMeta` instead of the static placeholder. Hand-rolled proto3 decoders for `FwBeginUpdate` / `FwDataChunk` / `FwFinalizeUpdate` / `FwActivateBank` keep nanopb out of the LP build. Watchdog OTA explicitly out of scope (still JTAG-only via `Flash-LP.ps1`). Pre-existing `orbitOtaPush.ts` + Level-1 version row keep working unchanged — bridge sees firmware as already-implementing the protocol. Phase 3 (custom stage-2 SBL chooser, eliminating the staging copy) remains the production gate. Design notes in `docs/LP-AM2434-OTA-Update-Plan.md` (revised today). |
| 61 | 2026-05-06 | **Watchdog (R5FSS1_0) split into its own OSPI partition.** Root cause for the 0.A.59 / 0.A.60 boot failures: TI's `sbl_ospi_multi_partition` SBL expects each cluster's appimage at a separate OSPI offset (R5FSS0_0=0x80000, R5FSS1_0=0x180000). We were bundling both cores into a single signed mcelf at 0x80000 and writing nothing at 0x180000 — SBL parsed the bundle for R5FSS0_0, then read junk from 0x180000 for R5FSS1_0 and corrupted main's MSRAM in the process. Main's R5FSS0_0 then jumped to PC `0x701a53fe` (inside its own `.bss:ENET_DMA_PKT_MEMPOOL` — a wild branch from corrupted code). Diagnosis bench-confirmed: the same `nova_lp.release.out` repackaged as a single-core mcelf (no `--core-img=6`) at 0x80000 boots cleanly and pings on the LAN. **Fix:** `ti-arm-clang/makefile` now produces TWO signed mcelfs — `nova_lp.release.mcelf.hs_fs` (R5FSS0_0 only, ~485 KB) and `watchdog.release.mcelf.hs_fs` (R5FSS1_0 only). `Flash-LP.ps1` flashes both: main at 0x80000, watchdog at 0x180000. If watchdog mcelf is missing, Flash-LP gracefully skips it (main still boots, R5FSS1 stays in reset). Memory carve-out (main MSRAM `0x70080000-0x701DFFBF`, heartbeat `0x701DFFC0-0x701DFFFF`, watchdog MSRAM `0x701E0000-0x701FFFFF`) is unchanged from 0.A.59. Trap documented in `/memories/repo/lp-am2434-watchdog-mcelf-trap.md`. |
| 60 | 2026-05-05 | **REVERTED.** Linked `Platform/nova_fw_update.c` + `hal_flash.c`, called `NovaFwUpdate_Init` + `BootValidation` from main pre-scheduler, and wired `ConfirmBoot` into the watchdog client at the 30 s healthy mark. CONTROLLER booted SBL fine but app went silent before the first `[BB] main entered` UART marker (or COM4 missed it) and stayed unreachable on LAN. Reverted all four changes (makefile, main.c boot calls, watchdog-client healthy tracker) and stayed on 0.A.59. Hypothesis for next attempt: pre-scheduler call into `NovaFwUpdate_BootValidation` may be touching SDK Flash machinery that isn't ready (`Board_init` is empty in our generated config and we never call `Board_driversOpen`, so `gFlashHandle[CONFIG_FLASH0]` was always NULL). Even though `hal_flash.c` null-guards, the call path runs from a thin pre-scheduler stack and we should defer it until after scheduler start — same rule as `DebugP_log`. Will re-attempt as a post-scheduler init task. |
| 60 | 2026-05-05 | **F2c step 1+2: OTA Bank infrastructure linked into LP build.** Foundational scaffold for the custom stage-2 SBL chooser (full design: `docs/lp-am2434-f2c-sbl-chooser-design.md`). Three changes that don't touch what's flashed at any specific OSPI offset yet — pure software-side prep so the next session can fork TI's `sbl_ospi_multi_partition` example without simultaneously linking new files. (1) `Platform/nova_fw_update.c` + `Platform/hal_flash.c` now build into `nova_lp.release.mcelf.hs_fs` (+1.2 KB to 537,766 B). `hal_flash.c` rewritten to share the SDK-managed `gFlashHandle[CONFIG_FLASH0]` (same access pattern as `lp_device_config.c`) instead of opening a second OSPI handle that fights the existing one. Bare `*0x44000000 = 1` system-reset write in `NovaFwUpdate_Activate` replaced with `Sciclient_pmDeviceReset(SystemP_WAIT_FOREVER)` — same DMSC path the JTAG auto-flasher and `CMD_REBOOT_SOC` already use (verified working 2026-05-03). `NovaFwUpdate_ConfirmBoot` made idempotent (skips OSPI write when `watchdog_strikes==0 && boot_reason==0`). (2) Canonical OSPI map finalised in `nova_fw_update.h`: header sectors moved to `0x060000` (bank A meta + boot meta) and `0x070000` (bank B meta) — the previous claim of "header at 0x000000" was wrong, that's the SBL load address that AM243x ROM hard-codes. Bank A app stays at `0x080000` (matching where TI's stock SBL hardloads today, so first F2c flash needs zero app-image migration); bank B at `0x200000`; golden at `0x380000`. Bank max size 1.5 MB. (3) `main.c` calls `NovaFwUpdate_Init` + `NovaFwUpdate_BootValidation` right after `LpDeviceConfig_Init`. `lp_watchdog_client.c` heartbeat task now tracks 30 s of continuous "all required alive bits + WD peer alive" and calls `NovaFwUpdate_ConfirmBoot()` at the milestone (idempotent so it doesn't grind a sector every 30 s of uptime). Once the stage-2 chooser ships, this becomes the rollback-clearance signal. **Not yet flashed to bench** — verifying compile only this session; bench testing is gated on the SBL fork in the next session. |
| 59 | 2026-05-05 | **Independent dual-core watchdog (R5FSS1_0).** Currently we use only `R5FSS0_0` (one of four R5F cores). Step 1 of using the spare cores: a dedicated NoRTOS watchdog image now runs on `R5FSS1_0` (separate cluster, own clock, own DWWD peripheral). Architecture (full design in `docs/lp-am2434-watchdog-design.md`): main core writes a 64-byte heartbeat struct (`LpWatchdogShm`) to MSRAM `0x701DFFC0` once per second; watchdog core polls at 100 Hz and only strobes its DWWD when the heartbeat is fresh AND all required subsystems (`ALIVE_MODBUS / ALIVE_SYSTEMSTATUS / ALIVE_LWIP_LINK / ALIVE_ENGINE_TICK`) report alive. If the main core hangs (kernel deadlock, runaway loop, BSS corruption flipping its own gate flag, stuck IRQ at higher priority than the strobe task), the watchdog stops strobing → DWWD expires within 10 s → SoC warm reset. Cluster-1 placement gives true independence — no shared resource main can scribble to silence the watchdog short of writing the heartbeat itself (which the watchdog re-checks for staleness). Files: `Nova_Firmware/lp_am2434/lp_watchdog_ipc.h` (shared struct + bit defs); `lp_watchdog_client.c` (heartbeat producer task on R5FSS0_0); `watchdog_core/{main.c, example.syscfg, ti-arm-clang/makefile}` (R5FSS1_0 image). Build system extended to bundle both cores into one `nova_lp.release.mcelf.hs_fs` via `MCELF_IMAGE_GEN --core-img=4:nova_lp.release.out --core-img=6:watchdog.release.out`. Main MSRAM region in `example.syscfg` shrunk from `0x160000` → `0x15FFC0` to carve out the 64 B heartbeat region. Three producer hooks added: `LpWatchdog_Ping(LP_WD_ALIVE_ENGINE_TICK)` after `lp_engine_tick()`, `LpWatchdog_Ping(LP_WD_ALIVE_SYSTEMSTATUS)` after each SystemStatus emit, `LpWatchdog_Ping(0x01 /* MODBUS */)` at top of `orbit_modbus_tcp_task` loop. Lwip link is auto-detected via `netif_default->flags & NETIF_FLAG_LINK_UP`. **Healthy milestone integration** (`FwUpdateHealthy_Mark` for OTA rollback) is *not* wired this version — that's gated on F2c (custom stage-2 SBL chooser) and a `LpWatchdog_PeerAlive()` helper is exported but unused. Recovery path if watchdog code is broken: flash any pre-0.A.59 mcelf — SBL just leaves R5FSS1 in reset, no DWWD configured, system runs as before. |
| 58 | 2026-05-05 | **PidLogStream typed proto channel.** Replaces the long-standing `PIDLogWrite` no-op stub in `lp_engine_shim.c`. Engine task pushes records into a 32-entry SPSC ring (per-loop monotonic sequence counter for gap detection); data-exchange task drains one record per 100 ms tick, encodes a `PidLogRecord` (envelope tag 72), and sends via `NovaProto_SendRaw`. Bridge already had the receive side wired (`MSG_PID_LOG_RECORD = 72` → `novaLogDataStore.insertPidLog` → SQLite `pid_log` table → `/api/pidlog` REST + Level-2 PID-tuning page graphs). Also added `mirror_pid_log()` so `Settings.Log.PID.{Door,Refrig}` actually loads from operator picks (same shadow-mirror bug class as 0.A.47 / 0.A.50 / 0.A.56) — without it the `Pid()` body's logging gate stays at BSS zero and nothing ever calls `PIDLogWrite` regardless of what the operator selected. Date/time strings come from the LP RTC (`localtime` + `strftime` MM/DD/YYYY + HH:MM:SS to mirror the legacy SD-card schema); when the RTC isn't authoritative yet the strings are omitted and the bridge timestamps the row on insert. Drop counter (`s_pidlog_dropped`) reserved for future `/health` exposure. |
| 57 | 2026-05-05 | **Burner Auto / Off / Manual operator control wired through to the 4-20 mA output.** Two bugs fixed at once: (1) `mirror_burner` was copying `lp->burner.manual` into `Settings.Burner.Threshold` (an unrelated economy-mode crossover field) and leaving `Settings.Burner.Manual` at BSS zero, so even legacy `BURNER_MANUAL` Settings-page mode was running the burner at 0%. Now copies into both slots (proto doesn't yet model `threshold` separately). (2) Burner has no `SW_BURNER_MANUAL` bit in the legacy CPLD shift-register enum (`SerialShift.h` line 107: `SW_BURNER_AUTO` has no `_MANUAL` twin), and `ApplyManualOverrides` only calls `OutputOn(EQ_BURNER)` — fine for a digital coil, useless for a 4-20 mA PWM. New post-`SetMode` override block (modeled on the doors override added in 0.A.55) forces `PwmChannel[PWM_BURNER].Output` to `PercentToPWMVal(Settings.Burner.Manual)` when the operator sets the burner row to MANUAL, and to `PWM_MIN_VALUE` (4 mA) on OFF. AUTO leaves the engine in control. UI `EquipmentRow` for burner already passes `allowManual: true` (default), so no UI change needed. |
| 56 | 2026-05-05 | **Fan PWM now actually loads operator settings.** Same class of bug as 0.A.47 (Refrig PID gains) and 0.A.50 (Plenum alarm thresholds): the data was on the LpSettings side, nobody was bridging it across to the AS2-engine `Settings.*` shadow. `Settings.Fan.{Min,Max,Refrig,Recirc}Speed`, `UpdatePeriod`, `TempDiff`, `TempRef1/2`, `UpdateMode` (and the FanBoost block) were all sitting at BSS zero, so `CtrlFan` saw `MaxSpeed=0` → `PercentToPWMVal(0) → PWM_MIN_VALUE` and the fan output stayed pinned at 4 mA / 0% no matter what the engine asked for. New `mirror_fan()` helper in `lp_engine_shim.c` runs every tick alongside the other mirrors. `Settings.Fan.PrevSpeed` is seeded from the persisted snapshot once on first tick (the engine then owns it across cooling-mode entries). The runtime accumulators (`DailyRunTime`, `TotalRunTime`, `TotalRefrigTime`, etc.) are deliberately NOT touched — `FanRunTimer` populates them and we'd clobber the running tallies if we mirrored them every tick. |
| 55 | 2026-05-05 | **Fresh-Air Doors row: AUTO / CLOSE / OPEN.** The Equipment Control "Fresh Air Doors" row previously showed AUTO / OFF / MANUAL but the MANUAL state did nothing useful (no `RO_DOORS` slot in the legacy `Settings.RemoteOff[]` enum, so `ApplyManualOverrides` had no hook). Wired end-to-end now: (a) `lp_engine_shim.c::build_switch_state` keeps `SW_FRESHAIR_AUTO` asserted under both AUTO and MANUAL so the legacy `CtrlDoorsPulsed` gate doesn't pulse-close. (b) Post-`SetMode` override in `lp_engine_tick` reads `lp->remote_off.state[EQ_DOORS]`: OFF (1) → `PwmChannel[PWM_DOORS].Output = PWM_MIN_VALUE` (force fully closed even if engine PID had written a partial value); MANUAL (2) → `PWM_MAX_VALUE` + `OutputOn(EQ_DOORS)` so `CtrlDoorsPulsed` pulses fully open. (c) UI relabels the dropdown for the door row only: AUTO / Close / Open (same wire values 0/1/2 — pure cosmetic; doors are an actuator, position labels are more intuitive than off/manual). New `offLabel` / `manualLabel` props on `EquipmentRow.svelte` so other actuator rows can opt-in later if needed. No new `RO_DOORS` enum slot required — uses the eq-indexed `lp->remote_off` table directly. |
| 54 | 2026-05-05 | **System Stop button now correctly reaches UI_SYSTEM_REMOTEOFF (and Start works).** v0.A.53 wired CMD_SYSTEM_STOP=5 to write `RemoteOff[EQ_FAN]=3 (SYSSTOP)`, but `build_switch_state` mapped value 3 to *neither* `SW_FAN_AUTO` nor `SW_FAN_MANUAL`. The legacy `nova_states.c::CheckSystemStatus` ordering checks `!FAN_AUTO && !FAN_MANUAL → ST_FAN_OFF (UI=13)` *before* `RemoteOff[RO_FAN]==3 → ST_SYSTEM_REMOTEOFF (UI=20)`, so the engine landed on UI_FAN_OFF=13 — and the home-page button label (which checks `CurrentMode === 20` to flip "Stop" → "Start") stayed stuck on "Stop" with no way to restart. Fix: keep `SW_FAN_AUTO` asserted under SYSSTOP so the engine falls through to the dedicated SYSSTOP gate, which correctly latches `ST_SYSTEM_REMOTEOFF`. Now Stop → button shows "Start"; clicking Start clears the latch (existing CMD_CLEAR_ALARM extension) and resumes. |
| 53 | 2026-05-05 | **System Start/Stop button now actually works.** The home-page bottom-right "System Start/Stop" tile POSTs `remoteStop=Stop`/`Start`, which the bridge translates to `CMD_SYSTEM_STOP` (5) / `CMD_CLEAR_ALARM` (1). Cmd_type 5 was unhandled in the LP firmware — fell through to the `default:` "not handled" log line and silently dropped. New handler: `LpSettings_SetRemoteOff(EQ_FAN, 3 /* SYSSTOP */)` + persist; `nova_states.c::CheckSystemStatus` already reads `Settings.RemoteOff[RO_FAN]==3` to enter `ST_SYSTEM_REMOTEOFF` → `CurrentMode = UI_SYSTEM_REMOTEOFF (20)`, and the existing `mirror_remote_off` carries the lp slot → legacy shadow. The CMD_CLEAR_ALARM=1 handler was extended to also clear the SysStop latch (slot back to 0=AUTO) when it sees value 3, so "Start" really resumes; OFF (1) and MANUAL (2) values that the operator set on the Equipment Control page are left untouched. Latch persists across reboot. UI label change shipped same day: button shows "Stop" while running and "Start" while in UI_SYSTEM_REMOTEOFF — was "Remote Off" / "Start". |
| 52 | 2026-05-05 | **Cure switch wired to operator Equipment Control (Onion mode).** Previously `build_switch_state` hardcoded `bits \|= SW_BIT(SW_CURE_AUTO);` so the Cure tile on the Equipment Control page was a no-op — flipping it to OFF was ignored by every `CheckInputs(SW_CURE_AUTO)` gate (and `Settings.RemoteOff[RO_CURE]` was never populated either). Wired via virtual remote_off slot 63 (the legacy enum has no `EQ_CURE`; slot 63 lives in the spare tail of the 64-wide `lp->remote_off.state[]` table, well past `EQ_ESTOP=60`): (a) Bridge `equipmentIds.ts` maps `cureBtn → 63`, so the existing MSG_EQUIPMENT_CMD handler in `main.c` persists it through `LpSettings_SetRemoteOff` like any other slot. (b) `lp_engine_shim.c::kRemoteOffMap` mirrors slot 63 → `Settings.RemoteOff[RO_CURE]`. (c) `build_switch_state` gates `SW_CURE_AUTO` on `lp->remote_off.state[63] != 1` (default 0=AUTO sets bit; 1=OFF clears; 2=MANUAL treated as AUTO defensively — UI hides the option). (d) `build_equipment_status_envelope` emits a synthetic `EquipState{eq=63, remote_off}` when `system_mode == Onion` so the UI tile reflects persisted state after reload. (e) UI `EquipmentRow.svelte` gained an `allowManual` prop; cure passes `false` so the dropdown shows AUTO/OFF only — cure mode itself reshapes the device UI (cure-mode label, burner schedule), so a forced "manual cure" is meaningless. Burner already worked correctly through the existing `EQ_BURNER` → `RO_BURNER` mirror + `ApplyManualOverrides` MANUAL path; no firmware change needed there. |
| 51 | 2026-05-05 | **Sensor-startup grace window.** Cold boot used to flash the red "Plenum Sensor Failure" header for the first 5-15 s while the orbit boards were still booting and hadn't yet pushed analog values to HR 200+. `CheckSystemStatus` saw `PlenumTempAvg == SENSOR_VAL_UNDEFINED` and immediately latched `ST_FAILURE` → `UI_FAILURE`. Fixed in `lp_engine_tick` by gating `SetSystemState`/`SetMode` for the first 30 s of `uptime_sec`, OR until `PlenumTempAvg` becomes valid (whichever happens first). During the gate, `SystemState` stays at its prior value (`ST_UNDEFINED` from `lp_engine_init`, which the case-default in `nova_states.c::SetMode` maps to `UI_STANDBY`). One-shot — never re-enters the gate after release, so a mid-run sensor disconnect still raises the warning normally. |
| 50 | 2026-05-05 | **Plenum temp deviation alarms now load operator settings.** Symptom: "high plenum temperature failure" trips almost immediately the moment the fan output asserts. Root cause: `mirror_basic_setpoints` in `lp_engine_shim.c` was not copying `lp->temp_alarm.{low_temp,low_timer,high_temp,high_timer}` into `Settings.Plenum.{Low,High}AlarmTemp/Timer` (the legacy struct fields read by `nova_failures.c::PlenumTempFail{High,Low}Chk`). With those fields stuck at zero, the time gate `Counter - AlarmTimer >= HighAlarmTimer * T_MINS` is satisfied on the first tick (0 minutes), and the temp gate `PlenumTempAvg > RefTemp + HighAlarmTemp` trips as soon as plenum exceeds the reference (0° deviation). Fix is a four-line copy in `mirror_basic_setpoints`. Defaults already correct in `LpSettings_DataInit` (5° / 10 min low + high) — they just weren't reaching the engine. Same class of bug as 0.A.47 PID coefficients: the data was on the LpSettings side; nobody was bridging it across to the AS2-engine `Settings.*` shadow. |
| 49 | 2026-05-05 | **Master engine clock now ticks.** Real root cause for "PWM outputs never build" (Refrig/Door/Burner/Climacell/Humidifier all stuck at 0%): the legacy AS2 engine schedules every PID via `XTimer()`, which latches `uptime_sec` into `XTimerVal`. `uptime_sec` was `extern`-declared in nova_timer.c + hal.h but **never defined or incremented anywhere** in the LP build (likely surviving link as COMMON, leaving it permanently 0). Result: `unsigned int Counter = XTimerVal;` always read 0, so the PID re-arm gate (`Timer = Counter + Settings.X.PID.U;` then `if (Counter < Timer) return;`) latched `Timer = 0+U` once and then refused to fire forever. (a) Defined `uptime_sec` in `lp_engine_shim.c`. (b) `lp_engine_tick` now bumps `uptime_sec` once per call (1 Hz from `lp_engine_task`) and copies it into `XTimerVal` directly so all `XTimerVal`-reading sites — PID arms, alarm timers, fan-boost / pulse-door interval timers, every `unsigned int Counter = XTimerVal;` site in `nova_controls.c` — finally see monotonic time. The 0.A.47 PID-coefficient fix is what made this bug visible: with Kp/Ki/Kd correctly loaded, the PID would now have built output, but the scheduler still gated it out. |
| 48 | 2026-05-05 | **`SystemStatus.fan_speed` now emitted** (proto field 11). 1:1 port of legacy `UI_Messages.c::FormatFanSpeed()`: `pwm_raw_to_pct(PwmChannel[PWM_FAN].Output)` when `SW_FAN_AUTO+CheckOutputs(EQ_FAN)`, `"Manual"` for `SW_FAN_MANUAL`, `"Off"` otherwise (and unconditionally `"Off"` when `CurrentMode==UI_FAILURE`, mirroring the legacy comment about the board cutting output power during low-temp / airflow faults even though the fan output bit stays asserted to keep the close-pulse-door coil energised). The PWM-channel-to-physical-AO routing stays operator-configurable on the Level 2 PWM page; this string is just the engine's commanded duty for `PWM_FAN`. Closes the System Monitor "Current Fan Speed --%" tile. |
| 47 | 2026-05-05 | **PID coefficients now actually load.** Root cause for "Refrigeration Output stuck at 0%" (and same for Door/Burner/Climacell/Humidifier): `PID_Init()` was never called from `lp_engine_init`, so `PIDCtrl[].Kp/Ki/Kd/RangeMin/RangeMax` were all zero (BSS). Even after fixing that, `mirror_refrig` / `mirror_burner` / `mirror_door` were not copying the operator's P/I/D/U from the LpSettings shadow into the `Settings.{Refrig,Burner,Door}.PID` legacy structs the engine reads, so the gains stayed zero anyway. (a) Extended the three mirror functions to also populate `Settings.X.PID.{P,I,D,U}` from `lp->X.{p_gain,i_gain,d_gain,u_limit}`. Settings.Climacell.PID was already mirrored. (b) New `refresh_pid_coefficients()` helper, called every `lp_engine_tick`, copies Kp/Ki/Kd + RangeMin/RangeMax into `PIDCtrl[]` so operator changes on the Level 2 PID page take effect on the next tick (no controller reboot). Range = `[PWM_MIN_VALUE, PWM_MAX_VALUE]` for Door/Refrig/Burner; `[0,100]` for Climacell/Humidifier. Humidifier shares Climacell coefficients per legacy AS2 (no separate Humid PID page). (c) New `seed_pid_state_once()` helper clears `Timer/PrevError/IntError` exactly once on the first tick so the integrator starts clean; cannot use legacy `PID_Init()` because it would also wipe the integrator state on every call. |
| 46 | 2026-05-05 | **Removed CPLD economizer auto-fresh-air bypass.** The legacy `SetStateCooling` gate requires `SW_FRESHAIR_AUTO`; that bit was being auto-asserted in `lp_engine_shim::build_switch_state` whenever Misc.RefrigMode=Economizer + DOORS had an output mapped, because the UI had no DOORS selector. The Level 1 Equipment Control page now exposes a Fresh-Air Doors row at level≥1 (UI change shipped same day), so the bypass is deleted — `SW_FRESHAIR_AUTO` now reflects only the operator's explicit AUTO selection on EQ_DOORS. Side effect: any panel previously relying on the bypass to enter cooling will now show "Cooling Output 0%" until the operator flips DOORS to AUTO on Equipment Control. |
| 45 | 2026-05-05 | **`SystemStatus.cool_label` / `cool_output` / `burner_output` now emitted** (proto fields 13/12/14). 1:1 port of legacy `UI_Messages.c::FormatCoolingOutput()`: `cool_label`="1" in {ST_REFRIG, ST_REFRIGDEHUMID, ST_DEFROST}, else "0"; `cool_output` is the appropriate PWM% string (refrig in refrig states, doors in cooling states, "Off" otherwise); `burner_output` only emitted in *_DEHUMID states. Closes the System Monitor home page bug where the output-row label stayed on "Cooling Output" even when the engine was in REFRIG mode (root cause: empty `cool_label` string → `main[16]==''` → page fell through to the cooling default). Onion + cure-auto burner-label path deferred until the cure controller / SW_CURE_AUTO replacement lands. Bumped `inner[]` scratch buffer in `build_system_status_envelope` from 96→128 B to fit the new strings comfortably. |
| 44 | 2026-05-05 | **Engine PWM → orbit AO write loop wired (Phase A4 follow-up).** New `equipment_ao_sync_task` (CONTROLLER-only, 1 Hz, mirrors `equipment_output_sync_task` but FC06 instead of FC05). Walks `Settings.AoEquip[slot][channel]`, converts the selected `PwmChannel[PWM_FAN/DOORS/REFRIGERATION/BURNER].Output` (raw 55..277) to 0..100 %, and writes to the orbit's HR0/HR1. Per-(slot, ch) last-written cache means an idle PID generates zero traffic. E-Stop master gate forces every assigned AO to 0 % whenever `Nova_GetEStopActive()`. Failed writes leave the cache invalid so orbit reconnect self-heals on the next tick. Closes the gap from v0.A.29 where engine PWM values appeared in SystemStatus diag fields but never reached the physical 4-20 mA outputs. |
| 1 | 2026-05-03 | Initial alpha versioning scheme. Captures Phase-D + STORAGE/CONTROLLER bringup state at end of May 2026 bench session. |
| 2 | 2026-05-03 | OrbitRoleAssign now persists to OSPI (LpSettings field 78). `bridge_rx_callback` writes via `LpSettings_SetOrbitRole` + `LpSettings_Save`; `build_orbit_board` emits OrbitBoardStatus.role / zone_id / refrig_stage. Level 2 IO Config orbit-role dropdown now sticks across reboot. |
| 3 | 2026-05-03 | (a) New `CMD_REBOOT_SOC` (=50) SystemCmd: firmware acks then calls `Sciclient_pmDeviceReset(SystemP_WAIT_FOREVER)` after 50 ms UART drain. Same DMSC path the JTAG auto-flasher uses; bridge POST `/iot/system/reboot` is the OTA Activate primitive (no JTAG, no operator). (b) `build_orbit_status_envelope` now always emits â€” even with empty `boards[]` â€” so the bridge cache (`dataCache.orbitBoards`) doesn't freeze on the last non-empty snapshot when all orbits go offline. |
| 4 | 2026-05-03 | `Flash-LP.ps1` now force-removes `obj/release/main.obj` before gmake so the `-DLP_FW_VERSION=...` CFLAG actually takes effect. Previously gmake skipped main.c rebuild whenever its timestamp was unchanged, so wire tag 22 silently reported `0.A.1+local` (the lp_version.h fallback) regardless of what `Flash-LP.ps1` claimed to flash. Verified end-to-end via `/proto/stream` tag 22 ASCII decode. |
| 5 | 2026-05-03 | `lp_settings.c` SYSTEM_MODE constants realigned to AS2 enum (`MODE_NONE=0, POTATO=1, ONION=2, BEE=3, ALL=4`); previously `M_ALL=1` collided with UI's `mode===1 â†’ potato` predicate, causing the Level 2 IO Config equipment dropdowns to show only "None". `io_definition_seed_defaults()` now refreshes structural fields (mode/io_type/renamable) on every call (preserving user-renamed `name`), and is invoked after `LpSettings_Deserialize` so OSPI blobs saved with the old enum get hot-fixed on next boot. |
| 6 | 2026-05-03 | (a) `s_io_defaults[]` extended to 59 entries to match AS2's full `EQUIPMENT_IO` enum â€” added EQ_AUX8 at index 32, shifting POWER/STANDBY/AIR_FLOW/LOW_TEMP/REDLIGHT/YELLOWLIGHT/PULSEDOOR_* and all 16 SW_* switch slots by +1. Wire layout now exactly mirrors legacy AS2. (b) UI hardcoded English `DEFAULT_IO_LABELS` table replaced by typed `equipmentMeta.ts` (`EQ_LABEL_KEYS` â†’ i18n keys; `entryAppliesToSystem` predicate; `IoOptionEnum` / `SystemModeEnum` constants). All 59 equipment + switch labels added to en.json + zh.json under `equipment.*`. Mirrors AS2 design where firmware never owned localized labels (StoreIoTranslate in StorePostData.c:1242). |
| 7 | 2026-05-03 | (a) Indices 33-42 (POWER, REMOTE_STANDBY, REFRIG_STANDBY, AIR_FLOW, LOW_TEMP, REDLIGHT, YELLOWLIGHT, PULSEDOOR_POWER/OPEN/CLOSE) flipped from M_NONE â†’ M_ALL so they appear in the IO Config dropdowns. AS2 hid them because legacy auto-assigned them; Constellation lets the operator place them on any free port. (b) Two Constellation-new equipment slots appended: 59=EQ_AUX_LOW_LIMIT (renamable input, per-aux low-temp interlock), 60=EQ_ESTOP (input, hard-wired E-Stop). Table now 61 entries; switches stay at 43-58 (wire-compatible with legacy). New entries also added to `equipmentMeta.EQ_LABEL_KEYS` and en/zh locale bundles. |
| 8 | 2026-05-03 | `LpSettings_ApplyIoConfig` now accepts the indexed `{slot,value}` submsg format on fields 3/4 (wire=2) used by the Level 2 IO Config save path. Wire-type disambiguates from existing `field 3 wire=0` board_count. Previously LP silently ignored the indexed submsgs (only ts-proto packed `output_map`/`input_map` on fields 1/2 were honoured), so picking e.g. `Red Light` for DO1 reverted to None on the next IoConfig re-emit. |
| 9 | 2026-05-03 | Two equipment slots are now hardware-pinned in firmware: EQ_LOW_TEMP (37) â†’ DI1 (port 1), EQ_ESTOP (60) â†’ DI11 (port 11). New `io_config_pin_hardware_inputs()` enforces this on every cold boot AND at the tail of `LpSettings_Deserialize` so a stale OSPI blob can't repurpose the pin. Both EQ entries marked `M_NONE`/`IO_NONE` in `s_io_defaults` so they vanish from the Level 2 IO Config dropdowns; the InputCell.svelte lock displays "Low Temp*" / "E-Stop*" non-editable. Bridge decoder also gained packed-varint support for IoConfig (`novaDataStore.decodeIoConfig` now reads both wire=0 and wire=2). |
| 10 | 2026-05-03 | Hardware-pin reshuffle: DI1 is now pinned to **EQ_AUX_LOW_LIMIT (59)** instead of the deprecated EQ_LOW_TEMP (37); DI11 stays pinned to EQ_ESTOP (60). EQ_LOW_TEMP is fully retired â€” forced clear in `io_config_pin_hardware_inputs()`, slot left in `s_io_defaults` only for wire-compat, label key blanked in `equipmentMeta.EQ_LABEL_KEYS`, removed from en/zh locale bundles, and dropped from the legacy `nova_thread_overrides.c` EquipIo seed table. EQ_AUX_LOW_LIMIT flipped from M_ALL/IO_IN/renamable=1 â†’ M_NONE/IO_NONE so it disappears from operator dropdowns (the InputCell `*` lock shows it non-editable on DI1). New soft default: cold boot seeds **EQ_POWER (33) â†’ DI2** in `LpSettings_DataInit`; operator can reassign and the OSPI replay overrides on next deserialize. |
| 11 | 2026-05-03 | **Set To Defaults button now actually works.** Previously the Level 2 IO Config "Set To Defaults" button posted SystemCmd cmd_type=8 (CMD_RESET_IO_CONFIG) but the LP firmware silently logged "not handled" and did nothing. Added `LpSettings_ResetIoConfig()` (clears `output_map[]` + `input_map[]`, re-seeds POWERâ†’DI2 soft default, re-pins DI1=AUX_LOW_LIMIT and DI11=ESTOP, preserves operator equipment renames in `io_definition.entries[]`) and dispatched it from `main.c` SystemCmd case 8 followed by an OSPI persist. Also refreshed the legacy `nova_settings.c::EquipIoInit()` seed table to match: dropped the dead EQ_LOW_TEMPâ†’DI10 mapping, shifted EQ_POWER off DI1 onto DI2, and shifted EQ_REMOTE_STANDBY onto DI3 (avoids the new hardware pin on DI1). |
| 12 | 2026-05-03 | `LpSettings_ResetIoConfig()` extended from a 1-port stub to the full AS2-derived factory map: DO1=Red Light, DO2=Yellow Light, DO3=Fan/Green Light, DO10=Pulse Door Close, DO11=Pulse Door Open, DI2=Power, DI3=Fan feedback, DI9=Air Flow, plus POTATO-mode adds DO4/DI4=ClimaCell, DO5/DI5=Humidifier 1 Head, DO6=Humidifier 1 Pump. EQ_REMOTE_STANDBY left unassigned (operator picks if needed; AS2's DI2 default collided with EQ_POWER anyway). DI1=AUX_LOW_LIMIT and DI11=ESTOP still re-pinned at the tail. `LpSettings_DataInit()` now routes through this same function so cold boot, Save Settings "Restore Factory Defaults" (cmd_type=4), and IO Config "Set To Defaults" (cmd_type=8) all produce the identical map. |
| 13 | 2026-05-03 | **Panel-default save/restore actually works.** Previously `SystemCmd cmd_type=2` (CMD_SET_DEFAULT, "Save as Panel Default") and `cmd_type=3` (CMD_PANEL_DEFAULT, "Restore Panel Default") were both wired to the same handler that just re-saved the current active bank â€” there was no separate snapshot. Added a third in-RAM bank `s_panel_bank` to `lp_settings_store` plus four new APIs (`LpSettings_SavePanelDefaults`, `LpSettings_HasPanelDefaults`, `LpSettings_LoadPanelDefaults`, `LpSettings_TakePanelBlob`, `LpSettings_RestorePanelDefaults`). Power-cycle persistence: new envelope field 33 (LPâ†’bridge panel snapshot push, mirrors field 32 wire format) and field 92 (bridgeâ†’LP replay on firmware-ready, mirrors field 91). Bridge stores it to `~/.constellation/lp_panel_defaults.bin`. cmd_type=2 now snapshots the active bank into the panel bank; cmd_type=3 loads the panel bank, deserializes into `s_data`, and re-saves to flip a new active generation. cmd_type=4 (factory defaults) unchanged. |
| 14 | 2026-05-03 | **Phase E1 â€” Equipment Control wired to LP firmware.** (a) Added `LpRemoteOff` mirror to `LpSettingsData` (eq-indexed `state[64]`) plus persist as save-blob top-level field 79. (b) `bridge_rx_callback` handles envelope tag 80 (`MSG_EQUIPMENT_CMD`): decodes `{eq_index, new_state}`, calls `LpSettings_SetRemoteOff`, persists if changed, and flags `s_equip_status_dirty`. (c) Replaced empty `EquipmentStatus` skeleton emitter with `build_equipment_status_envelope()` that walks `io_definition.entries[]` filtered by current SystemMode and emits one EquipState per populated entry carrying `(eq_index, output_on=manual?, remote_off)`. (d) Bridge `equipmentIds.ts` BUTTON_TO_RO map switched from legacy AS2 RO_* (20-entry) to true EQ_* (EQUIPMENT_IO ordinals) â€” fixes long-standing silent collision where e.g. `refrigBtn` sent index 1 (=EQ_DOORS) instead of 2 (=EQ_REFRIGERATION). `output_on` mirrors REMOTE_MANUAL today; real orbit DO writes land in 0.A.15. |
| 15 | 2026-05-03 | **Phase E2 â€” Operator overrides actually drive STORAGE DOs.** (a) Added `OrbitClient_WriteCoil()` (Modbus FC05 over a fresh short-lived TCP socket so it never collides with the per-orbit FC03 polling task). (b) New CONTROLLER-only `equipment_output_sync_task` (250 ms cadence) walks `LpSettings.io_config.output_map[] Ã— remote_off.state[]`, decodes port_id 1..11 â†’ STORAGE coil 0..10 (legacy `SS_PORT_ID_MULTIPLIER=12` rule), and dispatches FC05 writes â€” only when the per-coil cached state actually changes (idle systems generate zero extra Modbus traffic). (c) For 0.A.15 the desired state is purely operator REMOTE_MANUAL; full PID/scheduler/cross-couple lands in 0.A.16+. Failure to write does NOT mark the cache valid â€” next tick re-tries automatically, so an orbit reconnect self-heals. |
| 16 | 2026-05-03 | **Phase E3 â€” Bench DI injection + DO readback.** (a) STORAGE: new HR `41000..41014` window in `orbit_storage.c` round-trips `s->di[0..9]` / `s->e_stop` / `s->dc24v[0..3]` (R via FC03, W via FC06). Live `LpOrbitIo_LatchInputs` GPIO sweep (when wired) overwrites injected values on next tick â€” transparent, no production-build gate needed. (b) `sensor-injector` mbtcp client gained FC01 (read coils) + FC02 (read discrete inputs). (c) New `/api/eq/state` (FC01 coils 0..9 + FC02 discretes 0..14 in one round-trip) and `/api/eq/di` POST (FC06 to HR 41000+idx). (d) Panel UI gained tabbed nav: existing **Sensors** view unchanged; new **Equipment I/O** view shows live DO LEDs (read-only, driven by CONTROLLERâ€™s 0.A.15 FC05 path) + DI/E-stop/DC24V toggles that bench-inject into STORAGE. Polls at 1 Hz when active. |
| 17 | 2026-05-03 | **Phase E3.1 â€” Closed-loop EquipmentStatus (firmware half).** (a) New `EquipState.input_on = 6` (bool) added to `proto/agristar/equipment.proto`; C+TS bindings regen'd. (b) `build_equipment_status_envelope` rewritten to consume the existing FC01/FC02 cache (`OrbitClient_GetSample(0).do_bitmap` / `di_bitmap`, cached on every successful orbit poll cycle). For each populated mode-matching IoEntry it now decodes `io_config.output_map[i]`/`input_map[i]` with the same `port_id 1..11 â†’ STORAGE bit 0..10` rule the FC05 write path uses, and emits **real** `output_on` from the cached DO bitmap and `input_on` from the cached DI bitmap. (c) Fallback: when the eq slot is unmapped OR the orbit hasn't completed its first FC01+FC02 cycle, `output_on` reverts to the pre-Phase-E3 REMOTE_MANUAL-derived value (preserves cold-boot UX); `input_on` stays `false` (an unmapped equipment genuinely has no input contact). (d) UI rewrite of `equipmentStatus.ts` / `frontMatterComposite.buildPanel` deferred to the next non-firmware task â€” see `proto-direct-redesign-plan.md`. |
| 18 | 2026-05-04 | **Pulse-door defaults dropped from CONTROLLER factory map.** LP-AM2434 hardware exposes DO1..DO10 (10 outputs) and DI1..DI11 (11 inputs); the prior `EQ_PULSEDOOR_OPEN â†’ DO11` default pinned to a non-existent port and the matching `EQ_PULSEDOOR_CLOSE â†’ DO10` consumed a slot the operator usually wants free. Both are now left unassigned in `LpSettings_ResetIoConfig()`; operator picks valid ports if a pulse door is wired. `output_count` drops 43â†’40 to match the new highest assigned slot (EQ_YELLOWLIGHT). UI side: `EQ_PULSEDOOR_POWER` (eq 40) hidden from the Level 2 IO Config dropdown (internal-only â€” the open/close pair drives the latching pulse door); pending-echo guard added to the IO Config save path so a stale firmware broadcast can't clobber a just-saved optimistic UI state, with a 1 s ceiling so genuine firmware rejections (out-of-range port, hardware-pinned slot conflict) surface promptly instead of looking like a successful save. |
| 19 | 2026-05-04 | **Humidifier headâ†’pump pairing on Equipment Control MANUAL.** When the operator forces EQ_HUMID_HEAD{1,2,3} to MANUAL on the Level 1 Equipment page, `equipment_output_sync_task` now also drives the matching EQ_HUMID_PUMP{1,2,3} output ON for the duration of the manual override, regardless of the pump's own remote_off state. The pump's persisted remote_off stays AUTO so normal duty-cycle control resumes when the head is taken back to AUTO, and the operator can still independently flip the pump to OFF to silence a stuck pump while leaving the heated head energised. Mirrors AS2 behaviour where the head is a continuous-load element and the pump is the actual misting actuator. |
| 20 | 2026-05-04 | **Phase A1 â€” AS2 equipment engine wired into LP build (read-only) + .text/.rodata moved to OSPI XIP.** Two big changes shipped together because the engine wouldn't fit otherwise. (a) **XIP migration:** `example.syscfg` `section3.load_memory` flipped from MSRAM to FLASH (0x60100000 region), so all `.text` + `.rodata` (~452 KB) now live in OSPI XIP instead of competing for MSRAM with BSS/heap/stack. ISR/boot/MPU/cache code (`section2`) intentionally stays in MSRAM for latency. Frees ~410 KB of MSRAM (was 0x7e0 = 2 KB free, now 0x670e0 = 412 KB free). Build now produces TWO artefacts: small `nova_lp.release.mcelf.hs_fs` (~27 KB, MSRAM-only loader stub) flashed at OSPI 0x80000, and `nova_lp.release.mcelf_xip` (~452 KB ELF wrapping the XIP PT_LOAD segment) which `Flash-LP.ps1` now extracts and flashes at OSPI 0x100000 BEFORE the hs_fs flash. New `UNIFLASH_NO_RESET=1` env var on `uniflash_run.js` suppresses the post-flash SoC reboot for the first (XIP) flash so both segments are in place before the second (hs_fs) flash triggers the reset. (b) **Engine link:** new `lp_engine_shim.c` mirrors `LpSettings` + orbit sensor sample into the legacy `Settings`/`IoBoard[]`/`OutsideTemp`/`PlenumTempAvg` shadow the engine reads, and a 1 Hz `lp_engine_task` drives `SetSystemState()`â†’`SetMode()`. Engine read-only this phase: `OutputOn/Off` writes go to `IoBoard[].OutputState` shadow, real coil drive stays in `equipment_output_sync_task`. `CurrentMode` (AS2 UI_* numbering) emitted in `SystemStatus.current_mode` (proto field 16). Switch state derived from operator AUTO/OFF/MANUAL on the Equipment Control page. RunTimes forced to RC_STANDBY across all 48 slots until Phase A4 â€” engine should land on UI_STANDBY by default. Engine sources linked: `nova_states/modes/timer/pwm/serialshift/warnings/failures/controls/rtc.c`. `nova_settings/vfd/hw_stubs/pidlogs.c` deliberately NOT linked; `lp_engine_shim.c` provides their handful of needed symbols as stubs (UI_SendRuntimes, nova_vfd_any_faulted, PIDLogWrite, hal_pwm_pulse_width_set). Legacy migrated TUs get relaxed `-Wno-{missing-braces,logical-not-parentheses,switch,unused-but-set-variable,implicit-const-int-float-conversion,parentheses}` globally so the bit-for-bit AS2 ports compile under `-Werror` without touching their bodies. |
| 21 | 2026-05-04 | **0.A.20 reverted â€” XIP boot path doesn't work with our `sbl_ospi`.** Flashing the mcelf_xip's PT_LOAD segment as raw bytes at OSPI offset (paddr âˆ’ 0x60000000) succeeded mechanically (uniflash status=0, post-reset triggered) but the SBL never handed control to the XIP entry point â€” bridge saw zero UART2 traffic, RX frame counter froze pre-flash. The TI multi-partition example uses `--operation=flash-mcelf-xip` (a uniflash op our auto-flasher doesn't implement), which appears to also patch metadata in the hs_fs at 0x80000 referencing the XIP region. Rolling back: (a) `example.syscfg` `section3.load_memory` back to MSRAM. (b) `Flash-LP.ps1` two-stage XIP+hs_fs replaced with the original single-image flash at 0x80000. (c) `uniflash_run.js` keeps the `UNIFLASH_NO_RESET` gate (harmless when unused). To still fit the engine in MSRAM: heap shrunk from 34000 â†’ 16384 (saves ~17 KB; lwIP works fine on 16 KB), `.data` section moved from MSRAM into TCMA (32 KB block, ~20 KB used). Engine link reduced to 7 TUs: dropped `nova_controls.c` and `nova_rtc.c` from the build; their referenced symbols (`PIDClear`, `PIDCtrl[]`, `CtrlFan/Refrig/Burner/Doors/Heat/Aux/...`, `PWMValToPercent`, `ApplyManualOverrides`, `ClimacellClockMode`, `GetTime`) are stubbed in `lp_engine_shim.c`. Mode selection in `nova_modes.c` doesn't depend on Ctrl* return values â€” Ctrl* are output drivers, mode logic flows from sensors+setpoints+RunTimes â€” so CurrentMode still computes correctly. Real OSPI XIP migration deferred to a future alpha that implements the proper `flash-mcelf-xip` operation in `flasher_uart`. |
| 22 | 2026-05-04 | **0.A.21 reverted further â€” engine link broke LP boot regardless of XIP.** Flashing 0.A.21 produced ~50 RX frames over 5 s then total UART2 silence (bridge `rxFrames` froze, never reconnected). Bisect with `-DLP_ENGINE_DISABLE=1` (engine task body removed at compile time, all other 0.A.21 changes intact) reproduced the freeze, ruling out the engine task itself and pointing the finger at one of: (a) `.data` section moved to TCMA â€” TCMA may not be writable from non-privileged code at the time C-runtime's `.data` copy runs, OR (b) heap shrink 34000â†’16384 starving lwIP/orbit pool init. Restoring both to the 0.A.19 settings (`.data â†’ MSRAM`, `heap_size = 34000`) and dropping the engine link entirely (no `lp_engine_shim.c`, no `nova_states/modes/timer/pwm/serialshift/warnings/failures.c`, no `LP_ENGINE_*` task in `main.c` â€” gated under `LP_ENGINE_DISABLE`). This restores 0.A.19's known-good footprint with only the version string bumped. Phase A1 (engine wiring) is shelved until we either (a) get OSPI XIP working properly via TI's `flash-mcelf-xip` uniflash op, or (b) shrink the engine's static footprint enough to fit in the 0x740 (1.8 KB) of free MSRAM the 0.A.19 build had â€” likely by trimming the `Settings` shadow in `lp_engine_shim.c` to only the fields the engine actually reads instead of mirroring the full 17 KB struct. |
| 25 | 2026-05-04 | **0.A.24 reverted â€” XIP boot path still broken.** Mechanically the v0.A.24 multi-segment flash worked (auto-flasher loop confirmed, manifest produced 2 entries: 430 KB XIP at 0x100000 + 27 KB hs_fs at 0x80000, both reported `status=0`), but the LP went silent on boot â€” bridge `[NovaBridge] No firmware data for 15s` reconnect storm, ping `10.47.27.1` 100% loss. Same symptom as v0.A.20. Diagnosis: **our flow is missing three things** vs TI's working `xip_benchmark` example: (1) `flash-phy-tuning-data` op writes OSPI clock calibration data needed for high-speed XIP-mode reads â€” we don't program it. (2) TI re-flashes `sbl_ospi.release.hs_fs.tiimage` at 0x0 every time; ours is whatever stale SBL was first written, which may not be XIP-aware (we never confirmed). (3) The `mcelf_xip` ELF's note segment may need to be merged into the hs_fs's note segment so SBL knows to enable OSPI memory-mapped XIP mode before jumping to the entry point â€” currently they are two independent ELFs and the hs_fs's note doesn't reference the XIP region. Recovery: `example.syscfg` `section3.load_memory` reverted FLASHâ†’MSRAM, version bumped 0.A.24â†’0.A.25 (no other code changes). The XIP tooling (Build-XipManifest.ps1, manifest mode in uniflash_run.js, multi-flash loop in flasher_uart) is preserved â€” it works correctly and will be reusable once we figure out the SBL hand-off. Real OSPI XIP migration is shelved pending a separate investigation: probably build TI's stock `xip_benchmark` as-is and flash via TI's `uart_uniflash.py --operation=flash-mcelf-xip` to confirm the hardware can do XIP at all, then trace what `flash-mcelf-xip` does that our flow doesn't. Meanwhile v0.A.23's save_blob dedup (199 KB free MSRAM) is enough headroom for Phase A1 engine without XIP. |
| 24 | 2026-05-04 | **OSPI XIP migration â€” second attempt, this time with the proper SBL semantics understood.** v0.A.20's XIP failure was diagnosed as a flashing-tool deficiency, not a SBL incompatibility â€” `sbl_ospi.release.hs_fs.tiimage` (what we already use) is XIP-aware, and `sbl_ospi_multi_partition` is for multi-core IPC, NOT XIP. Build now produces TWO files: small `nova_lp.release.mcelf.hs_fs` (~27 KB, non-XIP segments + cert) flashed at OSPI 0x80000, and `nova_lp.release.mcelf_xip` (~430 KB ELF wrapping a single PT_LOAD at p_paddr=0x60100000) whose PT_LOAD segments are flashed at `(p_paddr & 0x07FFFFFF)` (= 0x100000) BEFORE the hs_fs lands. Three new pieces of tooling support multi-segment flashing in ONE DSS session: (a) `flasher_uart/main.c` rewritten with a multi-flash loop â€” after each flash completes the firmware waits for DSS to clear `g_flash_request.magic`, then loops back to wait for the next MAGIC_GO (was: one-shot, then park). (b) New `ospi_flash/Build-XipManifest.ps1` â€” PowerShell ELF parser walks the `mcelf_xip` PT_LOAD entries, writes each to its own `.bin`, emits a `manifest.txt` that ends with the hs_fs entry. **PowerShell trap discovered:** `-shl` on a `byte` value silently returns 0 â€” must use `[System.BitConverter]::ToUInt32($b, $o)` for ELF parsing. (c) `ospi_flash/uniflash_run.js` gained `UNIFLASH_MANIFEST` env-var mode â€” reads the manifest, drives MAGIC_GO + clear-magic for each entry sequentially, then triggers SoC warm reset on the last one. `Flash-LP.ps1` auto-detects: if `mcelf_xip` is >4 KB it builds the manifest and uses manifest mode; otherwise falls back to the legacy single-file flash. syscfg knob: `section3.load_memory` = FLASH (was MSRAM); `section1`/`section2` (vectors + ISR/boot/MPU/cache code) and `section4`/`section5` (.data + sysmem/stack) intentionally stay in MSRAM. MSRAM savings: ~430 KB freed (`.text` 354 KB + `.rodata` 76 KB), so combined with v0.A.23's save_blob dedup the engine has ~19x headroom for Phase A1 retry. Phase A engine work itself remains paused under `LP_ENGINE_DISABLE` â€” see `/memories/repo/phase-a-engine-plan-paused.md` for the resume plan. |
| 23 | 2026-05-04 | **MSRAM bloat audit (partial) â€” recovered 180 KB without touching XIP.** User flagged that AS2 fit in 512 KB total and questioned how a not-yet-engine-running firmware was already out of room. Audit of `nova_lp.release.map` showed `.bss` was 847 KB (84% of 1 MB MSRAM). Two whales identified, one fixed: **`bridge_rx_callback` had 45 copies of `static uint8_t save_blob[LP_SETTINGS_MAX_BLOB_SIZE]` (~4 KB each) â€” one per save-handling switch-case branch.** Compiler can't merge these because each `static` in nested block scope has unique linkage. Fix: hoist a single file-scope `save_blob[]` above the function (`main.c:436`); strip all 45 in-block declarations via regex. References inside cases bind to the file-scope buffer; size unchanged because callback is single-threaded on the bridge UART RX path. Saves **~180 KB** (`.bss` 847 â†’ 667 KB). The other whale â€” `udp_iperf.c::UDP_IPERF_SND_BUF` (288 KB, force-placed via `__attribute__((section(".bss:UDP_IPERF_SND_BUF")))`) â€” is reachable via `LWIP_LWIPERF_APP=1` / `LWIP_UDPERF_APP=1` / `LWIP_TCPECHO_APP=1` / `LWIP_UDPECHO_APP=1` in `enet/lwipcfg.h`. Disabling all four flags + dropping `udp_iperf.c` from the makefile freed an additional 290 KB but **broke LPâ†’bridge UART2 TX entirely** (bridge `connected:false`, `rxFrames=0` indefinitely; UART2 RX from bridge still worked, settings saves still committed to OSPI). Something in `test.c::apps_init()` is load-bearing for the UART2 TX path â€” possibly task creation order, a sys_thread side-effect, or a lwIP-init-as-side-effect-of-app-init. Reverted lwipcfg.h + makefile and shipped only the save_blob dedup. Engine still gated under `LP_ENGINE_DISABLE`. The lwIP demo-removal investigation is filed under "audit for bloat later" â€” even with demos still in, the 180 KB save_blob fix puts free MSRAM from ~19 KB up to ~199 KB, which is more than enough for the engine. |
| 26 | 2026-05-04 | **Phase A1 â€” AS2 equipment engine wired into LP build (read-only mode display).** v0.A.25's 199 KB free MSRAM is enough headroom; engine reintegrated WITHOUT the failed XIP path. Re-added 7 legacy engine TUs to `FILES_common` (`nova_states/modes/timer/pwm/serialshift/warnings/failures.c`) plus `lp_engine_shim.c`. `LP_ENGINE_DISABLE` build-time gate fully removed (engine now permanent â€” no parallel disabled path). `lp_engine_task` runs CONTROLLER-only at 1 Hz after a 15 s warm-up for OrbitClient + LpSettings. `build_system_status_envelope` re-emits `current_mode` (proto field 16, AS2 UI_* numbering) from `lp_engine_get_current_mode()`. Outputs still routed by `equipment_output_sync_task` â€” engine OutputOn/Off writes go to `IoBoard[]` shadow only this phase. Smoke test: bridge journal steady RX, debug UART prints `[ENGINE] init` then `[ENGINE] mode 0 â†’ N` per transition; UI home page shows numeric mode. |
| 27 | 2026-05-04 | **Phase A2 â€” engine drives outputs over Modbus TCP.** `equipment_output_sync_task` `want_on` source switched from operator-only `remote_off==MANUAL` to `CheckOutputs(eq) || (remote_off==MANUAL fallback)`. The fallback covers Constellation-only slots the engine doesn't track (PULSEDOOR_*, REDLIGHT, YELLOWLIGHT, POWER, REMOTE_STANDBY, etc.). `lp_engine_shim.c`: (a) initialises `IoBoard[MAIN].Output[1..10]` bit table in `lp_engine_init` (legacy MAIN layout, unique-per-port â€” actual bit value irrelevant since OutputState is a desired-state register, not a shift-reg image); (b) new `mirror_output_map()` copies `LpSettings.io_config.output_map[eq]` â†’ `Settings.EquipIo[eq].Output` each tick so engine `OutputOn/Off` find the right port; (c) real `ApplyManualOverrides()` body ported from legacy `Controls.c` â€” honours operator MANUAL on FAN/HEAT/CAVITY_HEAT/BURNER/CLIMACELL/HUMIDIFIER1-3/LIGHTS1-2/AUX1-8 with the legacy alarm gates (`SystemAlarm[]` is BSS-zero so all gates pass for now; alarm wiring lands in A3 with the real Ctrl* drivers). Humidifier headâ†’pump pairing now lives entirely in `ApplyManualOverrides` (mirrors AS2); the duplicate eq==8/10/12 fixup in `equipment_output_sync_task` removed. `RC_STANDBY` schedule still forced in `mirror_basic_setpoints`, so mode-driven outputs all come from the standby branch (HEAT/CAVITY_HEAT off, fan off) with operator MANUAL the only thing that energises a coil. Output authority now flows: UI â†’ LpSettings.remote_off â†’ engine ApplyManualOverrides â†’ IoBoard[].OutputState bit â†’ CheckOutputs(eq) in equipment_output_sync_task â†’ OrbitClient_WriteCoil FC05 â†’ STORAGE coil. |
| 28 | 2026-05-04 | **Phase A3 - PID + Ctrl* drivers live.** Linked `nova_controls.c` (71 KB - ~5 KB MSRAM cost, 149 KB free remaining) into FILES_common. Removed all Ctrl*/PIDClear/PWMValToPercent/ClimacellClockMode/ApplyManualOverrides stubs from `lp_engine_shim.c` plus duplicate `PIDCtrl[]` global; engine `SetMode()` now calls real fan ramp + refrig stage cycler + heat hysteresis + burner DutyCycle + climacell + humidifier duty + bay lights + aux program evaluator + cavity heat + outside-air blend + door PID, all with AS2 timing constants intact. Two thin no-ops remain in shim: `PIDLogWrite` (would pull 80 KB nova_messages.c - PID stream replaces it later via typed proto) and `hal_pwm_pulse_width_set` (CONTROLLER LP has no on-board PWM; AO writes go via Modbus to orbit boards, wired in A4). With `RC_STANDBY` still forced in `mirror_basic_setpoints`, mode logic still parks at UI_STANDBY and operator MANUAL fallback in `equipment_output_sync_task` is the only thing energising a coil - same observable behaviour as A2, but engine is now genuinely deciding (just with a standby schedule). Phase A4 lifts the schedule force and PWM AO routing. |
| 29 | 2026-05-04 | **Phase A4 - Schedule + Cure/Refrig/OutsideAir mirror + RTC time live.** `mirror_basic_setpoints()` no longer forces `RC_STANDBY` across all 48 slots - it now walks `lp->runtime.entries[0..count-1]` and copies `entry.mode` into `Settings.RunTimes[entry.slot]` (LpSettings runtime mode values are the same legacy `RC_*` enum, so a direct copy is correct; unmapped slots default-fill `RC_STANDBY`). New mirrors added: `mirror_cure` (StartTemp/HumidRef/StartHumid/HumidHighLimit + TempLowLimit/TempHighLimit from LpCureLimit), `mirror_refrig` (Mode + Limit; PID gains and stage list stay engine-internal), `mirror_outside_air` (CtrlMode/Diff/AboveBelow/TempRef/CalcHumidMax - drives the cooling-vs-refrig decision in `SetStateCooling` and the calc-humid gate in `SetStateOnion`), `mirror_burner` (Mode/On/Low + TempSet pinned to plenum setpoint per AS2 burner-economy convention), `mirror_door` (CoolAirCycle + ActuatorTime), `mirror_misc` (HeatTempThresh). `GetTime()` stub replaced with real implementation backed by `LpRtc_IsAuthoritative()` + `localtime()` - engine `RunClockMode()` now picks the correct half-hour slot from the live wall-clock; falls back to 0,0,0 (slot 0) until the bridge sends a DateTimeUpdate, which the operator's default `RC_STANDBY` schedule handles safely. Cure-mode 48-slot schedule (legacy `Settings.Cure.RunTimes[]`) memcpy'd from the main schedule until LpCure grows its own runtimes[] field. Sensor mirror was already correct (outside_temp pointed at `Settings.AnalogBoard[0].Sensor[2].Value`, plenum_humid mirrored on humid board ch 1) - no changes needed. End result: with a non-STANDBY slot in the current half-hour window AND outside conditions warranting it, engine now legitimately transitions through `SetStateOnion`/`SetStatePotato` decision tree to RECIRC/REFRIG/COOLING/CURE/HEATING modes per AS2's bit-for-bit logic. |
| 30 | 2026-05-04 | **CMD_CLEAR_ALARM (cmd_type=1) wired in firmware.** Level 1 alarm-monitor popup "Clear" button POSTs `/iot/button` with `ClearAlarm=ClearAlarm`; bridge already forwards as `novaBridge.sendSystemCmd(1)`, but `main.c` SystemCmd dispatcher silently dropped cmd_type=1. Added handler that calls legacy `ClearAlarms(0)` (zeros `SystemAlarm[]` + `AlarmTimer[]`) and `WarningsClear()` (resets `s_warning[]` backing store). Engine's per-equipment FailChk re-evaluates on the next 1 Hz tick \u2014 transient alarms (sensor blip, NoBroadcast) stay cleared and SystemMode returns to a normal state; persistent fail conditions immediately re-trigger so the operator can't accidentally mask a real fault. RemoteOff[] is intentionally NOT touched \u2014 operator-forced OFFs are sticky and cleared from the Equipment Control page. (Auto-popup of the alarm window on the UI side is already wired in `routes/+layout.svelte` lines 292-305 \u2014 it triggers when `AlarmData.length > 0` and `canShowAlarm`, with a 5-min cooldown after hide. The remaining gap is firmware-side `WarningReport` envelope emit so `AlarmData` actually populates; deferred to a later alpha.) |
| 31 | 2026-05-04 | **WarningReport envelope (tag 12) emits real engine alarms.** Replaced the empty-body skeleton emitter with `build_warning_report_envelope()` that walks `s_warning[NUM_WARNINGS]` via `WarningStatus(i)` and emits one `Warning{code=i, severity=status}` submsg per active index. Cadence unchanged (every 5 s, tick%50==35). Bridge `decodeWarningReport` already maps `code` \u2192 `WARN_*` key via the UI-side `WARNING_KEYS[]` table; UI `frontMatterComposite::buildAlarmData` produces `AlarmData = ['WARN_KEY=Human text', \u2026]` from there. Combined with v0.A.30's CMD_CLEAR_ALARM handler, the full failure-popup loop is now closed end-to-end: engine `WarningsSet()` \u2192 LP envelope tag 12 \u2192 bridge proto store \u2192 UI `frontMatterStore.AlarmData` \u2192 auto-popup in `+layout.svelte` \u2192 operator clicks Clear \u2192 `/iot/button {ClearAlarm}` \u2192 `sendSystemCmd(1)` \u2192 `ClearAlarms(0) + WarningsClear()`. Message strings come from the UI's `DEFAULT_WARNING_TEXT` table by key (no `Warning.message` sent over the wire \u2014 saves ~30 B per active alarm and avoids duplicating the localizable strings in firmware). |
| 32 | 2026-05-04 | **Equipment input mirror + DI bitmap merge \u2014 fixes false-positive WARN_AIRFLOW / WARN_AUXLOWPLENTEMP at boot.** Two bugs: (a) `IoBoard[MAIN].Input[port]` bit-mask table was never initialized in `lp_engine_init`, so every `CheckInputs(eq)` was evaluating `(InputState & 0) == 0` which is always true \u2014 the engine saw every wired equipment input as asserted regardless of physical state. (b) `Settings.EquipIo[eq].Input` was never populated from `LpSettings.io_config.input_map[eq]`, so even the bit-table fix would have looked at the wrong port. New `mirror_input_map()` runs every tick alongside `mirror_output_map()`, identical encoding (`port_id 1..10 \u2192 IO_*`), with the three bypass slots (POWER/REFRIG_STANDBY/REMOTE_STANDBY) explicitly forced to IO_UNDEFINED so their engine-internal \"assume present\" branch fires. `Input[]` bit-mask table is encoded `port p \u2192 bit (p-1)` so it directly aligns with `OrbitSample.di_bitmap` (DI1=bit0). `build_switch_state` now ORs in `storage_sample.di_bitmap & 0x3FF` (10 bits) on top of the operator-switch high bits, so EQ_AIR_FLOW / EQ_LOW_TEMP / etc. only assert when the real STORAGE coil reads them as on. ESTOP (DI11/bit10) deliberately masked off \u2014 IoBoard.Input[] only sizes to 11 elements (port 0..10), and AS2 has no alarm gate for ESTOP anyway; ESTOP enforcement remains a Constellation-only path. |
| 33 | 2026-05-04 | **3-position switch logic fix - SW_*_AUTO no longer asserted under MANUAL.** `build_switch_state()` `EQ_AUTO` macro was `state != 1 (OFF)` which matched both AUTO (0) and MANUAL (2), so SW_FAN_AUTO was set whenever the operator picked MANUAL. AS2 physical switches are 3-position (OFF/AUTO/MANUAL) and mutually exclusive: only one of SW_*_AUTO and SW_*_MANUAL is ever asserted. Tightened to `state == 0`. With the fix, MANUAL forces the coil ON via `ApplyManualOverrides`+SW_*_MANUAL but the engine cannot enter automated states like COOLING / REFRIG that gate on SW_FAN_AUTO - matching AS2 behaviour. (To reach COOLING: set fan to AUTO, set Doors to AUTO, ensure the current half-hour scheduler slot is RC_COOLING / RC_RECIRC / RC_REFRIG, OutsideTemp valid.) |
| 34 | 2026-05-04 | **Auto-assert SW_FRESHAIR_AUTO when Misc.refrig_mode=Economizer + door output mapped.** Constellation has no physical fresh-air-door selector switch, so `SetStateCooling`'s `CheckInputs(SW_FRESHAIR_AUTO)` gate would never pass. `build_switch_state()` now auto-asserts SW_FRESHAIR_AUTO when (a) `LpMisc.refrig_mode == 0` (Economizer on the Level 1 Misc page), (b) `io_config.output_map[EQ_DOORS] != 0` (door output wired in IO Config), and (c) the operator hasn't explicitly OFF'd DOORS via Equipment Control. Operator's AUTO/OFF/MANUAL on DOORS still wins via the existing EQ_AUTO/EQ_MAN paths above; the bypass only fires the AUTO path for them. Means COOLING state can now engage in economizer mode with: Fan=AUTO, Misc=Economizer, DOORS in IO Config + AUTO, scheduler slot in RC_COOLING/RC_RECIRC/RC_REFRIG, OutsideTemp valid + below StartTemp. |
| 35 | 2026-05-04 | **Fix DI/switch bit collision in `IoBoard[MAIN].InputState`.** `SW_BIT(sw)` macro started at bit 0 (`(sw - SW_START_STOP)`), but the STORAGE orbit DI bitmap merge OR's bits 0..9 into the same word. A floating/wired DI2 on STORAGE asserted SW_FAN_AUTO; DI3 asserted SW_FAN_MANUAL — which is why the engine was stuck in `ST_FAN_MANUAL` on v0.A.34 even with the UI Equipment Control set to AUTO. Shifted switches to bits 16+ (`1u << (16 + (sw - SW_START_STOP))`). Settings.EquipIo[SW_*].Input still equals SW_BIT (port-id is just a unique uint to CheckInputs); IO_UNDEFINED=0xFFFFFFFF is unaffected. |
| 36 | 2026-05-04 | **Port `SetStartTemp` + `WetBulbDepression` to LP — Cooling Available Temp + outside-air control now compute on real hardware.** Legacy `States.c::SetStartTemp` (and its WB helper) wasn't linked into the LP build, so `StartTemp` ("Cooling Available Temperature" on the home page) stayed at `SENSOR_VAL_UNDEFINED` and `SetStateCooling` could never compare `*OutsideTemp` against it — outside-air cooling was effectively dead. Re-implemented `lp_wet_bulb_depression()` + `lp_calc_cooling_available()` in `lp_engine_shim.c` (verbatim algorithm from AS2; SM_POTATO gate dropped per Platform precedent so onion/cure benefit too), added `mirror_climacell()` to populate `Settings.Climacell.{Efficiency,Altitude,AltitudeUnits,PID,Times}` from `LpClimacell`, and emit `start_temp` as SystemStatus field 7 (float, user units) so the UI tile finally renders a number. Calc runs end-of-tick after `SetMode()`. |
| 37 | 2026-05-04 | **Fix `lp_settings.c` emit_* helpers — proto field keys must varint-encode.** All five emit helpers (`emit_string`, `emit_uint32`, `emit_float`, `emit_uint32_force`, `emit_sint32`) wrote field keys as a single byte (`buf[pos++] = (field<<3)|wire`), which works for fields ≤15 but corrupts the wire for fields ≥16 (key value ≥128 sets the varint continuation bit). FailureSettings (tag 53, fields up to 22) tripped this — the UI store decode threw silently, the composite stayed `null`, and Level 2 → Failures 1 page spun forever on the loading state. New `emit_key()` helper at lp_settings.c:51 now varint-encodes; all five helpers route through it. Verified wire bytes `88 01 01 a0 01 01 b0 01 01` for fields 17/20/22. Added `level2/failures1` and `level1/equipment` page optional-chain hardening for `pwmConfig[N]?.split` against empty channel arrays. See `/memories/repo/lp-emit-helper-varint-key.md`. |
| 38 | 2026-05-04 | **PWM page (Level 2 → 4-20 mA Output Setup) save round-trip wired end-to-end.** Bridge has been sending `MSG_AO_EQUIP_ASSIGN` (envelope tag 123) for weeks, but the LP firmware had no handler — every operator pick silently timed out the bridge ack and never persisted. Added `LpAoEquipSet` to `LpSettingsData` (16 slots × 2 channels, uint8 each), implemented `LpSettings_{Set,Get,BuildBody,ApplyBlob}_AoEquip` (save-blob top-level field 80; entry submsg `{slot,channel,equip}` skipped when equip==0). New `bridge_rx_callback` arm for tag 123 decodes the assignment, calls the setter, persists if changed. `build_orbit_board` now emits `OrbitBoardStatus.ao_equip` (orbit.proto field 15, packed `[ch0, ch1]`) so the UI re-syncs after refresh / reconnect. Bridge `decodeOrbitBoardStatus` extended to accept BOTH packed and unpacked-repeated wire formats for field 15 (mirrors `decodeIoConfig` pattern), so a future encoder change won't silently drop the array. No envelope-side broadcast of the whole table — it rides per-orbit-status like role does. |
| 39 | 2026-05-04 | **AoEquip persistence fix.** v0.A.38 saved [1,3] in RAM correctly but lost the data across reboot: `LpSettings_BuildAoEquipBody` used `emit_uint32` for the inner `slot` and `channel` fields, which proto3-zero-suppresses. Slot 0 / channel 0 are valid coordinates — suppressing them made `LpSettings_ApplyAoEquipBlob` see entries with no slot or channel and reject them. Switched both to `emit_uint32_force`; equip stays zero-suppressed (entries with equip==0 are still skipped by the outer `if (e==0) continue`). Same trap as the bridge `pb_uint32_force` invariant in `docs/firmware-bridge-protocol.md` §0. |
| 40 | 2026-05-04 | **SystemStatus diagnostic fields (CPLD-elimination Phase A) — initial attempt, wrong encoder.** Added six force-emitted fields to envelope tag 10: `18 system_state`, `19 run_clock_now`, `20 pwm_doors_pct`, `21 pwm_refrig_pct`, `22 estop_active`, `23 ro_bits`. ⚠️ Edits went into `Platform/nova_messages.c::NovaMsg_SendSystemStatus`, which is **dead code on the LP build** — wire frames are produced by `lp_am2434/main.c::build_system_status_envelope` instead. v0.A.40 reports the new version on tag 22 but tag 10 still shows the legacy 9-field layout. Fixed in v0.A.41. |
| 41 | 2026-05-04 | **SystemStatus diag fields — moved to the right encoder.** Same 6 fields (18-23) re-implemented in `lp_am2434/main.c::build_system_status_envelope` after Settings.h / States.h / PWM.h / hal_orbit.h were added to main.c includes. Inner scratch buffer bumped 64→96 B to accommodate the extra ~20 B. Each field bounds-checks against `sizeof(inner)` and bails to `diag_done:` rather than corrupting the envelope. Field 22 (`estop_active`) was deferred because hal_orbit.c is not linked into LP. v0.A.42 wires it. |
| 42 | 2026-05-04 | **E-Stop adoption (CPLD-elimination Phase B, revised scope).** STORAGE orbit's DI11 (di_bitmap bit 10) now drives a file-static `s_estop_active` latch in `lp_engine_shim.c::build_switch_state`. When asserted, the synthesized `SW_BIT(SW_START_STOP)` bit is cleared from `IoBoard[MAIN].InputState` — every `CheckInputs(SW_START_STOP)` site in the AS2-derived engine (nova_states.c et al.) now respects E-Stop without any per-site edit. Fail-safe: orbit unreachable → E-Stop treated as asserted. Exported via `Nova_GetEStopActive()`; SystemStatus inner field 22 (`estop_active`) emits the value live. Phase C/D (`CheckInputs(SW_*)` sweep + `Nova_ReapplySwitchInputs` strip) intentionally skipped — `build_switch_state` already translates RemoteOff → SW_* bits, so the sweep would change zero behavior. |
| 43 | 2026-05-04 | **E-Stop polarity + master output gate.** (a) DI11 polarity inverted: per spec, E-Stop is the only physical switch, normally-closed contact — closed/energized (bit=1) means *run permitted*, open/dead (bit=0) means *shutdown*. (b) `equipment_output_sync_task` now ANDs `!Nova_GetEStopActive()` over both the engine intent and the MANUAL fallback, so an operator-forced "Manual On" cannot energize anything while E-Stop is open. Belt-and-suspenders to the hardware safety relay that physically de-energizes the output rail. PWM channels naturally drop to PWM_MIN_VALUE (= 4 mA) via `ModeShutdown()`'s `Ctrl*(CTRL_OFF)` calls + `CtrlDoorsClose()` for pulse-doors. |

## How to bump

1. Append a row to the table above with a brief description of what
   changed since the previous alpha.
2. Update the **Current** field to the new number.
3. Commit. The next `Flash-LP.ps1` run picks it up automatically.

## How the version string is composed

```
0.A.<n>+<git-sha>[-dirty]
   ^   ^         ^
   |   |         â””â”€ git working tree was dirty at flash time (NEVER ship)
   |   â””â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€ first 8 hex chars of HEAD commit
   â””â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€ alpha number from this doc
```

`Flash-LP.ps1` refuses to flash a dirty tree without `-Force`. Production
hardware must never receive a `-dirty` build.

## Where the version surfaces

- **Boot UART:** `[VER] 0.A.<n>+<sha>` line in main.c boot banner (TODO)
- **Bridge `/health`:** `nova.firmwareVersion`
- **UI Level-1 Version page:** binds to `versionInfo` proto store (tag 22)
- **OTA `firmware.proto` Hello.version field**

See `Nova_Firmware/lp_am2434/lp_version.h` for the macro definition and
`Flash-LP.ps1` for the build-time injection.
